---
phase: 07-spanish-localization
plan: 16
type: execute
wave: 5
depends_on: ["07-13", "07-14", "07-15"]
files_modified:
  - e2e/containment.spec.ts
  - .planning/phases/05-mobile-responsive-retrofit/deferred-items.md
  - .planning/phases/07-spanish-localization/07-VERIFICATION-EVIDENCE.md
autonomous: true
requirements: [I18N-01, I18N-02, I18N-03, I18N-04, I18N-05, I18N-06, I18N-07]

must_haves:
  truths:
    - "The full Playwright matrix passes for both locales across all 8 projects, including touch-iphone in CI"
    - "All 16 routes are still statically prerendered — none silently became dynamic"
    - "Every English URL still resolves unchanged with no redirect; / is still the only URL that negotiates"
    - "No user-facing string remains hardcoded in any component, on either locale"
    - "Every namespace is translated — no es file left at {}"
    - "Every containment suppression is either a Phase 5 defect present in both locales or a deliberately logged design-owner finding; nothing this phase introduced is suppressed"
  artifacts:
    - path: ".planning/phases/07-spanish-localization/07-VERIFICATION-EVIDENCE.md"
      provides: "Measured evidence for each of the phase's eight success criteria"
      contains: "I18N-06"
  key_links:
    - from: "e2e/containment.spec.ts"
      to: ".planning/phases/05-mobile-responsive-retrofit/deferred-items.md"
      via: "every KNOWN_UNFIXED ref traced to a written finding"
      pattern: "KU-"
---

<objective>
Prove the phase, rather than assert it. Run the complete both-locale audit
matrix, re-measure the three criteria that fail silently, close every finding the
translation plans deliberately deferred, and assemble the evidence and human
read-throughs the phase's success criteria require.

Purpose: three of this phase's requirements have a failure mode with no error
message. I18N-06 (static prerendering) breaks silently if any `setRequestLocale`
was lost or any `generateMetadata` pulled a route dynamic. I18N-02 and I18N-04
break silently in the sense that a redirect looks like a working page. I18N-01 is
only true if nothing was missed across ~390 strings, which no reviewer can
confirm by reading.

Waves 3 and 4 also deliberately deferred any containment offender that could not
be fixed by shortening copy, because the plans that found them owned one file
each. This is the plan that owns the whole tree and can close them.

Output: a green matrix, a written evidence file, a reconciled suppression list,
and the end-of-phase human verification pack.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-spanish-localization/07-CONTEXT.md
@.planning/phases/07-spanish-localization/07-RESEARCH.md
@.planning/phases/07-spanish-localization/07-UI-SPEC.md
@.planning/phases/05-mobile-responsive-retrofit/05-VERIFICATION.md
@.planning/phases/05-mobile-responsive-retrofit/deferred-items.md
@CLAUDE.md

@e2e/containment.spec.ts
@e2e/support/pages.ts
@playwright.config.ts

<interfaces>
Phase 7 success criteria to evidence, from ROADMAP.md:
  1  / negotiates by browser language; no other URL auto-redirects
  2  switcher present in nav and footer on every page; choice persists and
     outranks the browser header
  3  every English URL unchanged, no redirect
  4  hreflang en + es + x-default on every page; sitemap covers both locales
  5  per-locale title, description and OG/Twitter tags
  6  all 8 routes x 2 locales prerender as static
  7  full Playwright matrix passes for both locales including touch-iphone in CI
  8  no hardcoded user-facing string remains in a component

Phase 5 constraints that must still hold on BOTH trees (05-VERIFICATION.md):
  8/8 criteria plus accepted override OVR-01. Desktop frozen (RESP-08).

Known environment limitation carried from Phase 5 (deferred-items.md): the
`touch-iphone` WebKit project cannot launch on this workstation — Playwright's
WebKit build targets the Ubuntu 24.04 ABI and Arch ships newer sonames. This is
not a code defect. Criterion 7 names CI specifically because CI is where that
project is real coverage; assert it from the CI run, not from a local run.

Findings deliberately deferred to this plan: read the summaries of 07-06 through
07-15 and collect every offender recorded as "left as a finding for the final
verification plan", plus any entry those plans added to deferred-items.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full both-locale matrix and re-assert the three silent criteria</name>
  <read_first>playwright.config.ts, e2e/support/pages.ts, scripts/verify-locale-routing.mjs, .planning/phases/05-mobile-responsive-retrofit/05-VERIFICATION.md</read_first>
  <files>.planning/phases/07-spanish-localization/07-VERIFICATION-EVIDENCE.md</files>
  <action>
Run the complete audit against a production build and record measured evidence,
not assertions, in
`.planning/phases/07-spanish-localization/07-VERIFICATION-EVIDENCE.md`.

STATIC PRERENDERING (I18N-06, criterion 6). Assert all 16 route paths appear in
`.next/prerender-manifest.json`. This is the load-bearing check of the phase:
`07-RESEARCH.md` §3.2 documents that a missing `setRequestLocale` produces no
error and simply drops a page to dynamic rendering, and plan 07-15 added
`generateMetadata` bodies that can do the same. Record the full route list as
evidence.

Note in the evidence file that Next reports routes with `generateStaticParams`
under the SSG marker rather than the plain static marker used before this phase.
ROADMAP criterion 6 is written as "prerender as static", and the
prerender-manifest check is the version-independent way to evidence it — record
both the manifest result and the build-output markers so a reader can see the
symbol changed while the property did not.

ROUTING (I18N-02, I18N-04, criteria 1 and 3). Run `npm run verify:routing` and
record its output: the 8 English URLs returning 200 with no `location` header
under a Spanish `Accept-Language`, `/` redirecting to `/es` only for a
Spanish-preferring browser, `/` serving English with no header at all, and the
`NEXT_LOCALE` cookie outranking the header.

FULL MATRIX (criterion 7). Run `npm run audit:responsive` across all 16 routes
and every launchable project. Record pass counts per project. The `touch-iphone`
project cannot launch on this workstation for the platform reason recorded in
Phase 5's deferred items — run it in CI and cite the CI run, rather than marking
the criterion unverified or pretending a local skip is a pass.

PHASE 5 NON-REGRESSION. Confirm `npm run audit:overflow`,
`npm run audit:targets` and `npm run audit:a11y` pass on both trees, and that
`npm run audit:a11y` still reports zero axe violations with `target-size`
enabled. Phase 5's 8/8 criteria must hold on the Spanish tree too — a Spanish
string that breaks a touch target or a contrast ratio is a Phase 7 regression of
a Phase 5 guarantee.

SEO SIGNALS (I18N-05, criteria 4 and 5). Sample the rendered HTML of one English
and one Spanish page per route and record that each carries its own title and
description, `hreflang` for `en`, `es` and `x-default`, and the correct
`og:locale`. Record the sitemap URL count.

If anything fails, fix it — this plan owns the whole tree, which is exactly why
the deferred findings were routed here. Record what was fixed.
  </action>
  <verify>
    <automated>npm run build && node -e "const r=Object.keys(require('./.next/prerender-manifest.json').routes);const en=['/','/features','/manifesto','/changelog','/mcp','/pricing','/privacy','/terms'];const want=[...en,...en.map(p=>p==='/'?'/es':'/es'+p)];const missing=want.filter(x=>!r.includes(x));if(missing.length){console.error('NOT PRERENDERED:',missing);process.exit(1)}console.log('prerendered '+want.length+'/'+want.length)" && npm run verify:routing && npm run audit:responsive</automated>
  </verify>
  <acceptance_criteria>
    - All 16 route paths present in `.next/prerender-manifest.json`, recorded in the evidence file
    - `npm run verify:routing` passes and its output is recorded
    - `npm run audit:responsive` passes on all 16 routes across every launchable project, with per-project counts recorded
    - `touch-iphone` evidenced from a CI run, cited by run reference, not skipped silently
    - Phase 5's overflow, touch-target and a11y suites pass on both trees with zero axe violations
    - hreflang, per-locale title/description and `og:locale` sampled and recorded for both trees
    - Sitemap URL count recorded as 16
  </acceptance_criteria>
  <done>Every mechanically checkable success criterion has measured evidence written down.</done>
</task>

<task type="auto">
  <name>Task 2: Audit for residual hardcoded strings, close deferred findings, and reconcile every suppression</name>
  <read_first>e2e/containment.spec.ts, .planning/phases/05-mobile-responsive-retrofit/deferred-items.md, the SUMMARY files of plans 07-06 through 07-15</read_first>
  <files>e2e/containment.spec.ts, .planning/phases/05-mobile-responsive-retrofit/deferred-items.md, .planning/phases/07-spanish-localization/07-VERIFICATION-EVIDENCE.md</files>
  <action>
NO HARDCODED STRINGS (I18N-01, criterion 8). Sweep `src/` for user-facing string
literals that never reached the catalogue. Grep is the starting point, not the
answer — check each of these categories deliberately, because they are the ones
an extraction pass misses:
  - JSX text nodes between tags;
  - `aria-label`, `alt`, `title` and `placeholder` attribute values;
  - default parameter values on component props (the pattern `CopyButton`'s
    `label = "Copy"` used);
  - strings inside data modules under `src/data/`;
  - error and empty-state strings in client components.
Exclude what is legitimately not copy: the `/mcp` code constants, MCP tool
identifiers, provider proper nouns, version numbers, ISO dates, tag keys, class
names and token names. Record the exclusion list in the evidence file so the
criterion is auditable rather than merely claimed.

Run `npm run i18n:gates:strict` and confirm it exits 0 — that is the assertion
that no `es` namespace was left at `{}`. The default gate only warns for those,
by design; the strict form is what makes "every namespace translated" a fact.

CLOSE DEFERRED FINDINGS. Read the summaries of plans 07-06 through 07-15 and
collect every containment offender recorded as "left as a finding for the final
verification plan". Those plans each owned a single file and could only fix by
shortening copy; this plan owns the tree. For each finding, fix it at source.
The permitted fixes, in order of preference: shorten the Spanish copy; adjust a
mobile-only value behind a `md:` restore point; add a per-instance wrapper
adjustment. What is NOT permitted, unchanged from the rest of the phase: editing
a frozen `.display` clamp or a per-component `leading-*`, introducing a
locale-specific type scale, or adding `data-reflow-exempt` to suppress rather
than fix. If a finding needs one of those, it is a design-owner decision — log it
in `deferred-items.md` with measured numbers and leave it.

RECONCILE SUPPRESSIONS. Walk `KNOWN_UNFIXED` in `e2e/containment.spec.ts` entry
by entry and classify each:
  - still reproducing on both trees as a Phase 5 defect -> keep, and update its
    `reason` with the Spanish numbers plans 07-08, 07-09 and 07-10 measured;
  - no longer reproducing on the Spanish route because the Spanish copy is
    shorter (KU-1 is the likely case) -> keep the entry but record in the
    evidence file that it is currently unused on that route, so a future reader
    does not mistake an unused suppression for a hidden defect;
  - no longer reproducing at all -> delete both twins together, never one;
  - added during this phase for something this phase introduced -> that is not
    allowed. Remove the suppression and fix the defect.
Every remaining `ref` must trace to a written finding in `deferred-items.md`.
Add the Spanish measurements to those entries so the debt is recorded at its
current size, not at its 2026-07 size.

Update `deferred-items.md` with a Phase 7 section listing anything this phase
found and deliberately did not fix, each with the measured numbers and the reason
it is a design-owner decision.
  </action>
  <verify>
    <automated>npm run i18n:gates:strict && npm run build && npm run audit:containment && node -e "const s=require('fs').readFileSync('e2e/containment.spec.ts','utf8');const refs=[...s.matchAll(/ref:\s*\"([^\"]+)\"/g)].map(m=>m[1]);const d=require('fs').readFileSync('.planning/phases/05-mobile-responsive-retrofit/deferred-items.md','utf8');const orphan=[...new Set(refs)].filter(r=>!d.includes(r));if(orphan.length){console.error('suppression with no written finding:',orphan);process.exit(1)}console.log('ALL REFS TRACED')"</automated>
  </verify>
  <acceptance_criteria>
    - No user-facing string literal remains in `src/`; the exclusion list is written down and justified
    - `npm run i18n:gates:strict` exits 0 — no `es` namespace left at `{}`
    - Every finding deferred by plans 07-06 through 07-15 is either fixed at source or logged in `deferred-items.md` with measured numbers
    - Every `KNOWN_UNFIXED` ref traces to a written finding; `/es` twins were never kept or deleted independently of their English original
    - No suppression exists for a defect this phase introduced
    - No frozen clamp or `leading-*` edited; no `data-reflow-exempt` added
    - `npm run audit:containment` green on all 16 routes
  </acceptance_criteria>
  <done>I18N-01 is auditable rather than asserted, and every suppression in the harness is traceable to a written, currently-measured finding.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble the end-of-phase human verification pack</name>
  <read_first>.planning/phases/07-spanish-localization/07-VERIFICATION-EVIDENCE.md, the SUMMARY files of plans 07-05 and 07-10</read_first>
  <files>.planning/phases/07-spanish-localization/07-VERIFICATION-EVIDENCE.md</files>
  <action>
Prepare everything a human needs to complete the read-throughs D-05 and D-08
require, so the checks are a sitting-down-and-reading exercise rather than a
research exercise.

Start a production build on a known port and record the exact URLs to visit.
For each human check below, write the specific thing to look for and the specific
sentence or element to look at — a checklist that says "read the Spanish pages"
gets a rubber stamp, and a checklist that names the hook line gets a real answer.

Assemble the pack around four checks:
  - `/es/manifesto` register re-read (D-05, D-06). Plan 07-05 already took a
    blocking decision checkpoint on this before Wave 3 propagated the register;
    this is the confirmation that it still reads correctly now that the rest of
    the site was written against it. Name the hook line explicitly.
  - `/es/privacy` and `/es/terms` read-through (D-08). Reproduce plan 07-10's
    four-point check: clause parity, no `usted` or formal `su`, the
    governing-language notice reading unambiguously and appearing in both
    locales, and no data-handling claim differing in meaning.
  - Language switcher behaviour (I18N-03, D-11, D-12). Nav switcher visible from
    `md:` up and absent from the mobile hamburger panel — that absence is the
    locked tradeoff, not a bug; footer switcher present at every width; switching
    on a deep route preserves the route in both directions; the choice survives a
    reload and outranks the browser header.
  - Spanish rendering on a real phone. Phase 5 closed with a physical-iPhone
    check, and accented uppercase display type plus 15-25% longer strings is
    exactly the combination an emulator can flatter. Name the two or three
    surfaces worth looking at: the hero H1, a `.display` heading with an accented
    capital, and the pricing plan badge.

Record the pack in the evidence file alongside the measured results, so one
document carries both the machine and the human verification for the phase.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/07-spanish-localization/07-VERIFICATION-EVIDENCE.md','utf8');for(const need of ['/es/manifesto','/es/privacy','/es/terms','switcher','I18N-06']){if(!s.includes(need)){console.error('evidence pack missing:',need);process.exit(1)}}console.log('PACK COMPLETE')"</automated>
    <human-check>
      1. Read `/es/manifesto`. The hook must read `Un metodo para llegar a ser
         quien eres`. Read the seven principle cards: no written subject
         pronoun, no `usted`, `tu` where the reader is addressed.
      2. Read `/es/privacy` and `/es/terms` in full: clause parity with English,
         no `usted` or formal `su`, the English-governs notice unambiguous and
         present on both pages in both locales, no data-handling claim changed
         in meaning.
      3. Switcher: visible in the nav from `md:` up and in the footer at every
         width; switching on `/es/pricing` lands on `/pricing` and back; the
         choice survives a reload and beats the browser header.
      4. On a real phone, load `/es` and `/es/pricing`: no clipped accent on an
         uppercase display heading, no badge spilling out of its card, no
         sideways scroll.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Evidence file contains the running-build URLs and a named, specific checklist for all four checks
    - The manifesto check names the hook line verbatim
    - The legal check reproduces plan 07-10's four points
    - The switcher check states that nav-switcher absence in the mobile panel is expected
    - The device check names the specific surfaces to inspect
    - Machine evidence and human checklist live in one document
  </acceptance_criteria>
  <done>The end-of-phase human verification is a specific, answerable checklist backed by the measured evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification evidence -> phase sign-off | recorded results are what the phase is accepted on |
| suppression list -> CI signal | a suppression silently removes a failure from CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-16-01 | Repudiation | a suppression hiding a defect this phase introduced | mitigate | Every `KNOWN_UNFIXED` ref is asserted to trace to a written finding in `deferred-items.md`; refs added for phase-7-introduced defects are removed and the defect fixed |
| T-07-16-02 | Repudiation | claiming criterion 7 without the touch-iphone project | mitigate | The workstation cannot launch WebKit for a recorded platform reason; the criterion is evidenced from a cited CI run rather than from a local skip |
| T-07-16-03 | Repudiation | asserting I18N-01 without an audit | mitigate | The residual-string sweep enumerates the categories checked and records the exclusion list, so the claim is auditable by a third party |
| T-07-16-04 | Denial of Service | a route silently dropping to dynamic rendering | mitigate | Prerender-manifest assertion re-run after every prior plan has landed, including plan 07-15's metadata generation |
| T-07-16-05 | Tampering | evidence recorded from a dev build | mitigate | All measurements are taken against a production build, matching `playwright.config.ts`'s deliberate `next build` + `next start` webServer |
| T-07-16-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
1. `npm run build` succeeds; 16 route paths in `.next/prerender-manifest.json`.
2. `npm run verify:routing` passes.
3. `npm run audit:responsive` passes on 16 routes across every launchable
   project; `touch-iphone` evidenced from CI.
4. `npm run i18n:gates:strict` exits 0.
5. Every `KNOWN_UNFIXED` ref traces to a written finding.
6. Human verification pack complete and answered.
</verification>

<success_criteria>
- All eight ROADMAP success criteria have measured, written evidence
- Phase 5's 8/8 criteria hold on both locale trees
- Every deferred finding is fixed or logged with current numbers
- No suppression covers anything this phase introduced
- Human read-throughs are specific enough to produce a real answer
</success_criteria>

<output>
Create `.planning/phases/07-spanish-localization/07-16-SUMMARY.md` when done.
Record: per-criterion evidence pointers, the residual-string exclusion list, the
final state of every KU ref, anything logged as a design-owner decision, and the
CI run cited for `touch-iphone`.
</output>
