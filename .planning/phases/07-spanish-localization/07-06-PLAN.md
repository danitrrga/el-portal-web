---
phase: 07-spanish-localization
plan: 06
type: execute
wave: 3
depends_on: ["07-03", "07-04", "07-05"]
files_modified:
  - src/components/Hero.tsx
  - src/components/hero/VCDSection.tsx
  - src/components/hero/McpIntegrationSection.tsx
  - src/messages/en/home.json
  - src/messages/es/home.json
autonomous: true
requirements: [I18N-01, I18N-07]

must_haves:
  truths:
    - "The top three sections of /es render in Spanish: hero headline, the Version/Cycle/Day explainer, and the MCP integration section"
    - "The hero H1 wraps to more lines in Spanish where needed but never exceeds its own box at 320px"
    - "Hero CTAs read 'Abrir El Portal' and 'Leer la metodología'"
    - "Product nouns match the shipped app exactly: Versiones, Ciclo, Día, and Trends stays English"
    - "Version, Cycle and Day each resolve to ONE catalogue key, referenced from both the Caption and the BandLabel call sites, so the two renderings of the same word cannot drift"
    - "/ renders byte-identical English copy to before extraction"
  artifacts:
    - path: "src/messages/en/home.json"
      provides: "Hero, VCD and MCP-integration English copy"
      contains: "hero"
    - path: "src/messages/es/home.json"
      provides: "Spanish mirror of the home namespace"
      contains: "Abrir El Portal"
  key_links:
    - from: "src/components/Hero.tsx"
      to: "src/messages/en/home.json"
      via: "useTranslations('home') in a Client Component"
      pattern: "useTranslations"
    - from: "src/components/hero/McpIntegrationSection.tsx"
      to: "src/i18n/navigation.ts"
      via: "locale-aware Link for the /mcp CTA"
      pattern: "@/i18n/navigation"
---

<objective>
Translate the first three sections of the home page: `Hero`, `VCDSection` and
`McpIntegrationSection`.

Purpose: the home page is the surface a negotiated Spanish visitor lands on
first, and the hero H1 is the single most typographically constrained string on
the site. Phase 5 tuned its clamp to measured values
(`clamp(1.625rem,3.571vw+0.911rem,2.625rem) md:clamp(42px,4.2vw,58px)`, confirmed
at `Hero.tsx:114`) and that value is frozen in both locales — Spanish must fit it
by being written to fit, not by the type scale bending.

`VCDSection` is where the derived glossary earns its place: Version, Cycle and
Day are the product's core primitives and the app already ships
`Versiones` / `Ciclo` / `Día` for them. Inventing alternatives here would be
visible to any reader who then opens the app.

Output: a `home` namespace in both locales, with English rendering unchanged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-spanish-localization/07-CONTEXT.md
@.planning/phases/07-spanish-localization/07-UI-SPEC.md
@.planning/phases/07-spanish-localization/07-PATTERNS.md
@.planning/codebase/design/SPANISH-VOICE.md
@.planning/codebase/design/TYPOGRAPHY.md
@src/messages/GLOSSARY.md
@CLAUDE.md

@src/components/Hero.tsx
@src/components/hero/VCDSection.tsx
@src/components/hero/McpIntegrationSection.tsx

<interfaces>
All three components are Client Components -> use `useTranslations('home')`
from `next-intl`, not `getTranslations`. Messages arrive through the
`NextIntlClientProvider` that 07-01 added to the locale layout.

THERE IS NO REGISTER OBJECT IN THESE FILES. `07-PATTERNS.md` confirms by direct
read that `Hero.tsx` and `VCDSection.tsx` hold their copy as inline JSX and as
string literals passed to local components — there is no content model, no
`description` prop contract, and no array to enumerate against. Extract by
LIFTING THE LITERAL STRINGS. Do not go looking for a register; you will not find
one, and the time spent looking is the time an earlier draft of this plan cost.

VERIFIED STRING SITES (07-PATTERNS.md, direct read 2026-08-02). If a line number
has moved, trust the surrounding symbol and the quoted text, not the number:

  Hero.tsx (204 lines)
    announcement pill, inline JSX, lines 80-110. Two separately styled runs
      inside ONE span: the accent run `Early access` (line 89) and the trailing
      `Launching soon - First 30 signups get Pro for life`. Extract as two keys
      or as one `t.rich()` message; never concatenate two translated fragments
      around a separator, because Spanish may need a different order.
    H1 text under the frozen clamp at line 114:
      `The final operating system for high-performers.`
    sub-copy paragraph, lines 123-128.
    CTA labels: `Open El Portal` (line 148) and `Read the methodology`
      (line 159), each inside a `text-nowrap` span.
    The ONLY hrefs in this file are `/pricing` (the pill, line 81), `APP_URL`
      (primary CTA) and `/features` (secondary CTA, line 158). There is NO
      `/mcp` link in Hero.tsx — do not model one in the catalogue contract.

  VCDSection.tsx (319 lines)
    section heading `Three horizons. One hierarchy.` and its paragraph
      `Versions plan identity. Cycles run focus. Days carry the score.`,
      inline JSX, lines 29-37.
    THREE `<Caption icon label body />` calls, lines 44-58, inside a
      `grid gap-8 sm:grid-cols-3` (line 43). Three, not four. There is no fourth
      caption and this plan does not add one — the phase translates, it does not
      author content.
    `BandLabel` inside `LayeredStrata` (lines 146-189) repeats the SAME three
      labels `Version` / `Cycle` / `Day` a second time (lines 146, 154, 158).
      Six label props, three distinct strings. Extract each label ONCE into
      `home.json` and reference it from both call sites; do not let the same word
      become two keys that can drift.

  McpIntegrationSection.tsx (132 lines)
    h2 `Your agent keeps the record.` (lines 84-91)
    body paragraph (lines 92-93), which names Claude, Cursor and MCP —
      proper nouns and the protocol name stay English.
    CTA link text `View the MCP server ->` at line 97, wrapping an INTERNAL
      `/mcp` href. This is the only `/mcp` link on the home page and the only
      internal Link in these three files besides Hero's `/pricing` and
      `/features` — it must move to `@/i18n/navigation` or Gate 3 fails.

OUT OF SCOPE — `src/components/DashboardPreview.tsx` (256 lines) is DEAD CODE.
`grep -rn DashboardPreview src` returns only its own definition; nothing imports
it (confirmed in 07-PATTERNS.md). Do not extract a single string from it, do not
add it to the catalogue, and do not let a residual-string grep pull you into it.
It also carries banned `animate-pulse` and `rgba(30,64,175,...)` patterns —
deleting it is a housekeeping item for another phase, not this plan's business.

Locked CTA translations (07-UI-SPEC Copywriting Contract):
  Open El Portal        -> Abrir El Portal        (Hero.tsx:148, inside text-nowrap)
  Read the methodology  -> Leer la metodología    (Hero.tsx:159, inside text-nowrap)

Frozen typography (07-UI-SPEC "Locked"):
  Hero.tsx:114 H1 clamp and the shared `.display` line-height: 1.02.
  Not editable in either locale's render path.

Accented display headings: read SPANISH-VOICE.md's `ACCENTED DISPLAY TYPE`
section before writing a `.display` heading in Spanish. Plan 07-05 filled it with
a measured verdict; if that verdict was outcome C, Spanish H1/H2 strings must
avoid accented capitals and the hero headline is directly affected.

Glossary rows that appear on these surfaces:
  Versions -> Versiones   Cycle -> Ciclo   Day -> Día
  Trends -> Trends (kept English)   Pulse -> Pulse (kept English)

Gate 2 behaviour (plan 07-03): translation status is INFERRED from the catalogue,
not configured anywhere. A namespace whose `es` file is exactly `{}` warns; the
moment it has any Spanish key it is fully enforced. Key identity is enforced in
BOTH directions, so an `es` key with no `en` counterpart fails too. There is no
allowlist to edit, and this plan must not touch `.github/workflows/` or
`scripts/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Hero, VCDSection and McpIntegrationSection into the home namespace</name>
  <read_first>src/components/Hero.tsx, src/components/hero/VCDSection.tsx, src/components/hero/McpIntegrationSection.tsx, .planning/phases/07-spanish-localization/07-PATTERNS.md (Hero, VCDSection sections), src/messages/en/common.json</read_first>
  <files>src/components/Hero.tsx, src/components/hero/VCDSection.tsx, src/components/hero/McpIntegrationSection.tsx, src/messages/en/home.json</files>
  <action>
Find the strings mechanically rather than from a structure that does not exist.
For each of the three files: grep for JSX text nodes (a `>` followed by a capital
letter and lowercase text) and for quoted string literals passed as `label=`,
`body=`, `title=` or `description=` props, and lift every hit. The VERIFIED
STRING SITES list in `<interfaces>` is the checklist; the grep is the audit that
the checklist was complete. Neither alone is sufficient — the list can go stale,
and the grep alone will not tell you which of two identical `Version` strings is
the Caption and which is the BandLabel.

Move every hit into `src/messages/en/home.json` under semantic keys grouped by
component — `hero.*`, `vcd.*`, `mcpIntro.*` (D-04: semantic, never
content-derived).

Where a component holds an ordered list of items (VCD's three captions, the MCP
section's feature list if it has one), keep it as an array in the catalogue and
map over the raw value, rather than flattening to `item1` / `item2` keys. Array
length is something plan 07-03's parity gate can check; numbered scalar keys are
not.

DEDUPLICATE THE THREE HORIZON LABELS. `Version`, `Cycle` and `Day` each appear
twice in `VCDSection.tsx` — once as a `Caption label` and once as a `BandLabel`
inside `LayeredStrata`. Give each one a single key (`vcd.version.label` and so
on) and reference it from both call sites. Two keys holding the same word is two
places for a future translator to disagree with themselves, and the parity gate
cannot see that kind of drift.

Replace the literals with `useTranslations('home')`. Do not convert any of these
files away from `"use client"`, and do not add `getTranslations` — they run in
the client tree by design (Framer Motion entrance animations).

Swap internal `Link` imports to `@/i18n/navigation`: Hero's `/pricing` pill and
`/features` secondary CTA, and McpIntegrationSection's `/mcp` CTA. Leave
`APP_URL`-based absolute external links on `next/link`.

Do not touch the shared chrome. `Navbar`, `Footer` and `CTASection` are owned by
plan 07-04 and already resolve through the `common` namespace; if a string looks
shared, it belongs in `common`, not duplicated here. `Open El Portal` in
particular already exists in `common` from 07-04 — reference it rather than
creating a second `home` key for the same words.

Do not touch `src/components/DashboardPreview.tsx`. See the OUT OF SCOPE note.

Change no class name, no clamp, no `leading-*` value, no animation variant, no
grid definition and no `text-nowrap` wrapper. This task is a string move.

Capture the rendered text of `/` before and after and diff it. English must be
byte-identical, including typographic apostrophes and any non-breaking spaces —
carry those into the catalogue verbatim rather than normalising them.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && node -e "const fs=require('fs');for(const f of ['src/components/Hero.tsx','src/components/hero/VCDSection.tsx','src/components/hero/McpIntegrationSection.tsx']){const s=fs.readFileSync(f,'utf8');if(!/useTranslations/.test(s)){console.error('no useTranslations in',f);process.exit(1)}if(!/use client/.test(s)){console.error('client directive lost in',f);process.exit(1)}}const en=require('./src/messages/en/home.json');const blob=JSON.stringify(en);for(const t of ['Three horizons','Early access','Your agent keeps the record']){if(!blob.includes(t)){console.error('expected string not lifted into the catalogue:',t);process.exit(1)}}if(fs.readFileSync('src/components/DashboardPreview.tsx','utf8').includes('useTranslations')){console.error('dead component DashboardPreview.tsx was modified — it is out of scope');process.exit(1)}console.log('HOME_EXTRACTION_OK')" && npm run build && npx playwright test containment.spec.ts a11y.spec.ts -g "overflow: /$"</automated>
  </verify>
  <acceptance_criteria>
    - A post-extraction grep for JSX text nodes and for `label=`/`body=`/`title=`/`description=` string-literal props across the three files returns no remaining English copy
    - Every site in the VERIFIED STRING SITES list is accounted for in `src/messages/en/home.json`
    - `Version`, `Cycle` and `Day` each resolve to a single catalogue key referenced from both the Caption and the BandLabel call sites — the same word does not appear under two keys
    - VCDSection still renders exactly three captions in its `sm:grid-cols-3` grid; no fourth item was added
    - Ordered content is stored as catalogue arrays, not numbered scalar keys
    - `useTranslations` used; all three files remain Client Components
    - Hero's `/pricing` and `/features` links and McpIntegrationSection's `/mcp` link import from `@/i18n/navigation`; `APP_URL` links stay on `next/link`; Gate 3 passes
    - `src/components/DashboardPreview.tsx` is unmodified
    - Rendered English text of `/` diffs clean against the pre-extraction capture
    - No class, clamp, `leading-*`, animation variant, grid or `text-nowrap` wrapper changed
  </acceptance_criteria>
  <done>The top three home sections read English copy from the catalogue with no visual change, with every horizon label deduplicated to one key.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Spanish home copy and prove it fits</name>
  <read_first>src/messages/en/home.json, .planning/codebase/design/SPANISH-VOICE.md (including the ACCENTED DISPLAY TYPE section 07-05 filled), src/messages/GLOSSARY.md, .planning/phases/07-spanish-localization/07-UI-SPEC.md (Text-expansion contract)</read_first>
  <files>src/messages/es/home.json, src/components/Hero.tsx, src/components/hero/VCDSection.tsx, src/components/hero/McpIntegrationSection.tsx</files>
  <action>
Write the Spanish mirror into `src/messages/es/home.json`, key-identical to the
English file with matching array lengths and placeholder sets. The moment this
file stops being `{}`, plan 07-03's Gate 2 enforces the namespace fully — a
half-finished file fails the build, which is intended. Key identity runs in both
directions, so a leftover `es` key with no `en` counterpart fails too.

Apply `SPANISH-VOICE.md` without exception: no written subject pronoun,
infinitive for the two CTAs, `tu` conjugation for prose addressing the reader,
`tu X` rather than the definite-article dodge, no `usted`, no formal `su`, no
exclamation marks, none of the 16 anti-words. Use the two locked CTA
translations verbatim.

Read SPANISH-VOICE.md's `ACCENTED DISPLAY TYPE` section BEFORE writing the hero
headline. If plan 07-05 recorded outcome C, `.display` headings in Spanish must
avoid accented capitals and the hero H1 is the first string that constraint
touches. If it recorded A or B, write freely.

Resolve every product noun through `src/messages/GLOSSARY.md`. `Versions`,
`Cycle` and `Day` translate; `Trends` and `Pulse` do not. The split is not
guessable, so look up rather than infer, and append any term the app has no
string for to the glossary's marketing-only section (D-02b) instead of coining it
inline. `Claude`, `Cursor` and `MCP` in the integration section are proper nouns
and a protocol name — they stay as they are in both locales.

Then measure. The hero H1 is the tightest constraint on the site: run the
containment sweep for `/es` at 320, 360, 390 and 430 and confirm no text is wider
than its own box.

The text-expansion contract governs what counts as a problem. A Spanish heading
wrapping to MORE lines than English is acceptable and expected — `text-balance`
and `max-w-*` exist to reflow. Text wider than its own box is not acceptable.
When a Spanish string does not fit, shorten the copy; the register's infinitive
constructions are naturally compact. Shrinking a clamp to make a long string fit
on one line is explicitly not an acceptable fix, and neither is a locale-specific
type scale — two rhythm systems for one layout is itself a design regression.

Both hero CTA labels sit inside `text-nowrap` spans, so they grow rather than
wrap. `Abrir El Portal` and `Leer la metodología` are comparable length to their
English originals, but confirm the button row at 320px anyway; below `md:` the
buttons stack `flex-col`, so the pressure is a desktop-width concern.

The three VCD captions sit in a `sm:grid-cols-3` grid, so the Spanish caption
bodies share a third of the row each — the tightest horizontal budget on this
surface after the H1. Check those three cells specifically at 390 and 430.
  </action>
  <verify>
    <automated>npm run i18n:gates && npm run build && npx playwright test containment.spec.ts touch-targets.spec.ts a11y.spec.ts -g "/es$"</automated>
  </verify>
  <acceptance_criteria>
    - `home` en/es key-identical, equal array lengths, equal placeholder sets, no orphaned `es` key
    - `npm run i18n:gates` passes with `home` fully enforced by Gate 2 (no warning remains for this namespace)
    - Containment passes on `/es` at 320, 360, 390 and 430 with no new suppression
    - The three VCD caption cells were checked specifically at 390 and 430
    - The hero headline complies with SPANISH-VOICE.md's `ACCENTED DISPLAY TYPE` verdict
    - Product nouns match `GLOSSARY.md`; `Trends` and `Pulse` remain English; `Claude`, `Cursor` and `MCP` unchanged
    - The two locked CTA translations appear verbatim
    - No clamp, `leading-*` or type-scale value changed; any fit problem was solved by shortening copy
    - No file under `.github/workflows/` or `scripts/` was modified
    - `/` and `/es` both still prerendered
  </acceptance_criteria>
  <done>The Spanish home hero and explainer sections read correctly and are proven to contain themselves at every mobile width.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalogue -> rendered home page | translated values become the site's primary landing content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | `home` catalogue values | mitigate | Rendered as React text nodes via `useTranslations`; no `dangerouslySetInnerHTML` introduced |
| T-07-06-02 | Spoofing | hero and MCP CTA destinations | mitigate | `APP_URL` external CTAs keep absolute origins on `next/link`; the three internal CTAs (`/pricing`, `/features`, `/mcp`) use `@/i18n/navigation`, so no locale prefix is ever prepended to an external URL and no internal link strands a Spanish reader on the English tree |
| T-07-06-03 | Information disclosure | catalogue content | accept | Marketing copy only; no secrets, no configuration, no PII enters the message files |
| T-07-06-04 | Repudiation | the same product noun diverging between its two render sites | mitigate | `Version`/`Cycle`/`Day` are deduplicated to one key each and referenced twice, so the Caption and the BandLabel cannot disagree |
| T-07-06-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
1. `npx tsc --noEmit`, `npm run lint`, `npm run i18n:gates` pass.
2. `npm run build` succeeds; `/` and `/es` both in `.next/prerender-manifest.json`.
3. Containment, touch-target and a11y specs pass on `/` and `/es`.
4. Rendered English text of `/` diffs clean against the pre-extraction capture.
5. A residual grep over the three files finds no JSX text node or
   `label=`/`body=`/`title=` literal left untranslated.
</verification>

<success_criteria>
- Hero, VCD and MCP-intro sections fully catalogue-driven in both locales
- English rendering unchanged; no content added or removed while translating
- Spanish contains itself at all four mobile widths with no clamp edited
- Product vocabulary matches the shipped app, with each horizon label held in one key
</success_criteria>

<output>
Create `.planning/phases/07-spanish-localization/07-06-SUMMARY.md` when done.
Record: any Spanish string shortened to fit, the hero H1 line count in Spanish at
320/360/390/430, which `ACCENTED DISPLAY TYPE` outcome constrained the headline,
and any term appended to the glossary's marketing-only section.
</output>
