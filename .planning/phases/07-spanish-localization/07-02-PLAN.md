---
phase: 07-spanish-localization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extract-glossary.mjs
  - src/messages/GLOSSARY.md
  - .planning/codebase/design/SPANISH-VOICE.md
  - .planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md
  - src/messages/glossary-additions/README.md
autonomous: true
requirements: [I18N-07]

must_haves:
  truths:
    - "The glossary source is verified to exist and to use the locale code es before a single term is derived from it — the plan halts rather than improvising if it does not"
    - "A translator (human or agent) can look up any El Portal product noun and get the exact Spanish term the shipped app already shows users"
    - "The glossary records where each term came from and on what date, so a later app change can be detected rather than silently contradicted"
    - "Marketing-only terms the app has no string for are visibly separated from derived terms"
    - "The Spanish register rules, anti-words and punctuation conventions exist as one binding document every translation plan reads"
    - "A translator who cannot carry an English string's wordplay into Spanish has one named place to record it — a file of that plan's own, never a table shared with plans running beside it — so the design owner receives a list to rewrite rather than a silently flattened site"
    - "No two plans running in the same wave ever write to the same register or glossary file, so a parallel append cannot be lost to a merge"
    - "Re-running the extraction script reproduces the derived section without hand-editing"
  artifacts:
    - path: "scripts/extract-glossary.mjs"
      provides: "Mechanical en.json <-> es.json key-pair extraction from the sibling app repo"
      min_lines: 40
    - path: "src/messages/GLOSSARY.md"
      provides: "Committed product-vocabulary snapshot with provenance date"
      contains: "Versiones"
    - path: ".planning/codebase/design/SPANISH-VOICE.md"
      provides: "Binding Spanish register, anti-words and punctuation contract"
      contains: "usted"
    - path: ".planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md"
      provides: "The lost-in-translation register: header, row template, permanent Example, and the empty Aggregated rows table plan 07-16 fills from the per-plan files"
      contains: "What was lost"
    - path: "src/messages/glossary-additions/README.md"
      provides: "The per-plan drop directory for marketing-only terms, so parallel plans never both edit GLOSSARY.md"
      contains: "glossary-additions"
  key_links:
    - from: "scripts/extract-glossary.mjs"
      to: "/home/danitrrga/dev/Projects/el-portal/src/messages/es.json"
      via: "read-only filesystem read at authoring time, never at build time"
      pattern: "el-portal/src/messages"
    - from: ".planning/codebase/design/SPANISH-VOICE.md"
      to: ".planning/codebase/design/BRAND.md"
      via: "Spanish equivalent of the English anti-words list"
      pattern: "anti-word"
---

<objective>
Produce the two reference artifacts every Spanish translation in this phase is
written against: the **derived product glossary** and the **Spanish voice
contract** — after proving the source they derive from is actually there.

Purpose: D-01/D-02 are locked — the glossary is DERIVED from the El Portal app's
production `es.json`, never authored. The app's split is not guessable
(`Versions` translates but `Trends` and `Dashboard` stay English), so a
translator inventing terms will sell a reader "Tendencias" and then hand them an
app that says "Trends". Deriving it mechanically also means the next locale is a
data change rather than a research project.

The cross-AI review flagged this plan as resting on the phase's largest
unverifiable premise: every claim about the sibling repo (its locale codes, its
key layout, its Spanish values) sat outside what any reviewer could read. Task 1
now closes that — it asserts the source's shape and HALTS the plan if the
premise is false, instead of deriving a vocabulary from a file that may not be
the file this phase assumed.

The voice contract exists because `BRAND.md`'s anti-words list is English-only,
and `07-RESEARCH.md` §7's register rules currently live in a research document
that no translation plan is obliged to read line by line. This plan turns both
into one binding file.

Output: `src/messages/GLOSSARY.md` (a snapshot with provenance) and
`.planning/codebase/design/SPANISH-VOICE.md`. No product code is touched.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-spanish-localization/07-CONTEXT.md
@.planning/phases/07-spanish-localization/07-RESEARCH.md
@.planning/phases/07-spanish-localization/07-UI-SPEC.md
@.planning/codebase/design/BRAND.md
@CLAUDE.md

<interfaces>
Read-only sibling repo (never a build-time dependency of this repo):
  /home/danitrrga/dev/Projects/el-portal/src/messages/en.json   (English catalogue)
  /home/danitrrga/dev/Projects/el-portal/src/messages/es.json   (Spanish catalogue, in production)
  /home/danitrrga/dev/Projects/el-portal/src/lib/i18n.ts        (exports the locales array)

DO NOT ASSERT A KEY COUNT. 07-CONTEXT.md records "1,711 keys" from an earlier
pass; a flattening run on 2026-08-02 produced 1,670. The difference is an
artefact of how nested objects and arrays are counted, not evidence that a file
changed. The invariant that matters is that the two catalogues are
key-comparable and in the low thousands — Task 1 reports the observed number and
records it, and nothing in this phase gates on a specific figure.

WHAT DOES MATTER, and is asserted in Task 1: the locale code is `es`, not
`es-ES`. The locked `hreflang` decision in 07-CONTEXT.md rests on it, and if the
app used `es-ES` the glossary derivation, the routing config and the SEO
alternates would all need re-targeting before anything else in this phase runs.

Confirmed split already extracted in 07-CONTEXT.md D-01 — the extraction must
reproduce it, and any disagreement is a signal the script is wrong:
  Versions->Versiones  Cycle->Ciclo  Day->Día  Habits->Hábitos
  Goals->Objetivos  Score->Puntuación  Archives->Archivos
  Trends->Trends  Dashboard->Dashboard  Cinema->Cinema  Pulse->Pulse
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the glossary source before deriving anything from it</name>
  <read_first>/home/danitrrga/dev/Projects/el-portal/src/lib/i18n.ts, .planning/phases/07-spanish-localization/07-CONTEXT.md (D-01, D-02, D-02b), .planning/phases/07-spanish-localization/07-PATTERNS.md (Glossary-gated product nouns)</read_first>
  <files>(no file written — this task either clears the way for Task 2 or halts the plan)</files>
  <action>
Assert, do not assume, that the sibling app repo still has the shape every
downstream plan's "copy verbatim" step depends on. Nine of this phase's plans
resolve product nouns through the artifact Task 2 produces, and every one of
them inherits whatever is wrong here.

Check five things and report each with its observed value:
  1. `/home/danitrrga/dev/Projects/el-portal/src/messages/en.json` exists and
     parses as JSON.
  2. `/home/danitrrga/dev/Projects/el-portal/src/messages/es.json` exists and
     parses as JSON.
  3. `/home/danitrrga/dev/Projects/el-portal/src/lib/i18n.ts` exports a
     `locales` array CONTAINING the bare code `es`, and does NOT use `es-ES`.
     This is the load-bearing check: the phase's locked `hreflang` decision, the
     `[locale]` segment values and the message directory names are all `es`.
  4. Both catalogues flatten to a comparable key count in the low thousands.
     REPORT the two numbers; do not assert a specific figure and do not fail on
     one. See `<interfaces>` for why 1,711 and 1,670 are both plausible readings
     of the same file.
  5. Three of D-01's eleven rows resolve inside `es.json`: one translated
     (`Objetivos`), one kept-English (`Trends`), one accented (`Día`). All three
     must be findable as values.

If check 1, 2, 3 or 5 fails, STOP. Do not hand-author a glossary, do not
substitute a machine translation, and do not continue to Task 2. Report the plan
as BLOCKED naming the failed check and its observed value, so the locale code or
the source path can be corrected before roughly 390 strings are written against
the wrong vocabulary. This is the one failure in this phase that is cheap to
catch now and phase-wide to catch later — the whole point of putting it in Wave
1 ahead of everything.

If check 4 disagrees with any number written in this plan or in 07-CONTEXT.md,
that is NOT a failure. Record the observed counts in the summary and continue.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const base='/home/danitrrga/dev/Projects/el-portal/src/messages/';let en,es;try{en=JSON.parse(fs.readFileSync(base+'en.json','utf8'));es=JSON.parse(fs.readFileSync(base+'es.json','utf8'))}catch(err){console.error('GLOSSARY SOURCE UNREADABLE:',err.message);process.exit(1)}const cfg=fs.readFileSync('/home/danitrrga/dev/Projects/el-portal/src/lib/i18n.ts','utf8');if(/es-ES/.test(cfg)){console.error('BLOCKED: app uses es-ES, not es — the locked hreflang and [locale] decisions must be revisited before this phase continues');process.exit(1)}if(!/['\"]es['\"]/.test(cfg)){console.error('BLOCKED: bare locale code es not found in the app i18n config');process.exit(1)}const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>v&&typeof v==='object'?flat(v,p+k+'.'):[p+k]);const ke=flat(en).length,ks=flat(es).length;console.log('observed key counts — en:',ke,'es:',ks);if(ke<500||ks<500){console.error('BLOCKED: catalogue far smaller than expected — wrong file?');process.exit(1)}const blob=JSON.stringify(es);for(const t of ['Objetivos','Trends','Día']){if(!blob.includes(t)){console.error('BLOCKED: expected term missing from the app es.json:',t);process.exit(1)}}console.log('GLOSSARY_SOURCE_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Both app catalogues parse as JSON and the observed key counts are printed and recorded in the summary
    - The app's locale config contains the bare code `es`; the string `es-ES` does not appear in it
    - `Objetivos`, `Trends` and `Día` are all findable as values in the app's `es.json`
    - The gate prints `GLOSSARY_SOURCE_OK` before Task 2 begins
    - If any check failed, no glossary was authored and the plan reported BLOCKED naming the failed check and its observed value
  </acceptance_criteria>
  <done>The premise every downstream translation plan rests on is proven true, or the phase is stopped before it can be built on a false one.</done>
</task>

<task type="auto">
  <name>Task 2: Derive the product glossary mechanically from the app's shipped catalogues</name>
  <read_first>/home/danitrrga/dev/Projects/el-portal/src/messages/en.json, /home/danitrrga/dev/Projects/el-portal/src/messages/es.json, .planning/phases/07-spanish-localization/07-CONTEXT.md (D-01, D-02, D-02b)</read_first>
  <files>scripts/extract-glossary.mjs, src/messages/GLOSSARY.md</files>
  <action>
Write `scripts/extract-glossary.mjs` — a Node script that reads the two sibling
catalogues by absolute path, flattens both to dotted key paths, and emits every
key present in both files whose English value is a short noun-phrase candidate:
one to three words, no ICU placeholders (`{`), no sentence-ending punctuation,
and starting with a capital letter or matching a known product term. Deduplicate
by the English value, and for each surviving pair emit English value, Spanish
value, the source key path, and whether the two values are identical (the
"kept English" case).

Sort output so identical-value pairs group separately from translated pairs —
the split IS the finding, and 07-CONTEXT.md D-01 warns it is not guessable.

Take the sibling repo path from an env var with the current absolute path as
default, so the script still runs if the checkout moves. The script must be
invoked by hand; it must never be wired into `npm run build`, a pre-commit hook,
or CI. D-02 is explicit: this repo does not gain a dependency on the app repo.
Read only those two JSON files — do not walk the app repo's source tree, do not
read its `.env`, and do not copy anything except English/Spanish string pairs.

Run the script and write its output into `src/messages/GLOSSARY.md` with three
sections:

  1. `## Derived from the app` — the generated table, with a provenance line
     recording the source paths, the app's next-intl version, the extraction
     date, and the key counts Task 1 observed. State plainly that this is a
     copied snapshot, that the app repo is authoritative, and that re-running the
     script is how it gets refreshed. State the locale code explicitly as `es`
     (Task 1 proved it), so a later reader does not have to re-derive it.
  2. `## Kept English` — the terms whose Spanish value equals the English one
     (`Trends`, `Dashboard`, `Cinema`, `Pulse` and anything else the extraction
     finds). Add the one-line rule: any term not in this document must be looked
     up in the app's `es.json`, never inferred from the pattern.
  3. `## Marketing-only (decided here)` — D-02b. Terms the marketing copy needs
     that the product UI never names. Seed it with the ones the site demonstrably
     uses and the app does not, and flag each as marketing-only so a future app
     change cannot silently contradict it.

     THIS SECTION IS NOT APPENDED TO IN PLACE BY LATER PLANS. Waves 3 and 4 run
     in parallel, and several plans coining a term each would either conflict on
     this file or silently lose one another's appends across isolated worktrees.
     Instead, create `src/messages/glossary-additions/` with a `README.md`
     stating the convention: a translation plan that needs a term the app has no
     string for writes `src/messages/glossary-additions/07-NN.md` — one file per
     plan, named for the plan — as a table with columns
     `| English | Spanish | Why the app has no string | Plan |`. Plan 07-16
     merges every such file into this `## Marketing-only (decided here)` section
     once the last translation wave has landed. State in this section that any
     term appearing here after phase 7 arrived through that merge, and that
     editing this section directly during Waves 3-4 is a defect.

     Any plan MAY read the whole `src/messages/glossary-additions/` directory to
     see what a sibling plan coined; no plan may write to another plan's file.

Cross-check the generated output against the eleven-row table in 07-CONTEXT.md
D-01. If any row disagrees, the script's filter is wrong — fix the script, do not
hand-edit the table. Record the reconciliation in the summary.
  </action>
  <verify>
    <automated>node scripts/extract-glossary.mjs > /tmp/glossary-check.txt && grep -q "Versiones" /tmp/glossary-check.txt && grep -q "Objetivos" /tmp/glossary-check.txt && grep -v '^#' src/messages/GLOSSARY.md | grep -c "Trends" | grep -qv '^0$' && echo GLOSSARY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/extract-glossary.mjs` runs standalone and reproduces the table without hand-editing
    - `src/messages/GLOSSARY.md` contains all eleven rows from 07-CONTEXT.md D-01 with matching Spanish values
    - The "kept English" set is a distinct section, not a footnote
    - A provenance line records source paths, extraction date, the locale code `es`, and the key counts observed in Task 1
    - Nothing in `package.json`, `next.config.ts` or CI references the sibling repo
    - The script reads exactly two files from the app repo and copies only string pairs
  </acceptance_criteria>
  <done>Any translator can resolve an El Portal product noun to the exact term the shipped app uses, and refresh it mechanically.</done>
</task>

<task type="auto">
  <name>Task 3: Write the binding Spanish voice contract</name>
  <read_first>.planning/phases/07-spanish-localization/07-RESEARCH.md section 7, .planning/phases/07-spanish-localization/07-UI-SPEC.md (Copywriting Contract), .planning/codebase/design/BRAND.md, .planning/codebase/design/README.md</read_first>
  <files>.planning/codebase/design/SPANISH-VOICE.md</files>
  <action>
Create `.planning/codebase/design/SPANISH-VOICE.md` as a peer of the other design
docs, using the same LOCKED / CURRENT / DEBT convention documented in
`.planning/codebase/design/README.md`. It is the one file every translation plan
in Waves 2-4 reads, so it must be self-contained — a reader must not need
07-RESEARCH.md open to apply it.

Contents, all marked LOCKED:

REGISTER — reproduce 07-RESEARCH.md section 7's six priority-ordered rules
verbatim in substance, with its worked examples: never write a subject pronoun;
infinitive for CTAs, buttons and links; nominal headings where natural and `tu`
conjugation where not; `tu` conjugation for prose addressing the reader; `your X`
becomes `tu X` rather than the colder definite-article dodge; never `usted` or
formal `su` anywhere including legal copy; `vosotros` when plural address is
needed, `ustedes` never. Include the three-row right/wrong table
(`Empieza tu primera Version` / `Tu empiezas tu primera Version` /
`Empiece su primera Version`) because it is the fastest way to check a sentence.

Record explicitly that `vosotros` is ALLOWED and must never appear in a
hard-fail list — plan 07-03 builds the CI gate from this document, and including
`vosotros` there would fail the project's own sanctioned copy.

Record that `su` / `sus` is ADVISORY ONLY, because it is also the legitimate
third-person possessive ("sus tendencias"), so it warns rather than fails.

REGIONAL VARIANT — peninsular Spanish is intended; `ordenador`, `movil`, `vale`
are fine. Locale code stays `es`, not `es-ES` (Task 1 verified the app uses the
same bare code): the copy is Spain-flavoured but `hreflang="es"` targets Spanish
speakers everywhere, and a Latin-American reader is better served by peninsular
Spanish than by English. Note that these are two separate decisions and must not
be conflated.

CONFIRMED CTA TRANSLATIONS — the four locked strings from 07-UI-SPEC's
Copywriting Contract (`Abrir El Portal`, `Leer la metodología`,
`Iniciar sesión`, `Crear cuenta`), with the note that all three primary-CTA call
sites wrap their label in a `text-nowrap` span, so any re-wording of those exact
strings must be re-measured against button width before merge.

SPANISH ANTI-WORDS — the list from 07-UI-SPEC verbatim (moderno, elegante,
estilizado, desatar, elevar as a verb of ambition, de ultima generacion,
revolucionario, que cambia las reglas del juego, potente/poderoso, hermoso,
inmersivo, fluido/sin fricciones as marketing filler, vanguardista, potenciar as
hype, desbloquear as hype, empoderar). State that it extends rather than replaces
BRAND.md's English list, and that both bind.

PUNCTUATION — no exclamation marks anywhere, which bans the inverted `!` in
Spanish too since it is inseparable from an exclamation; `?` questions are
permitted in prose but rare on this declarative site, and when used BOTH marks
are required (dropping the opening one to save width is not a stylistic option);
straight double quotes as today, never guillemets, because the site renders
Spanish and English chrome side by side; correctly-used em/en dashes stay, but
never as a substitute for a comma.

PRODUCT VOCABULARY — a pointer to `src/messages/GLOSSARY.md` as the single
lookup, with the rule that inventing a term is a defect and that the sanctioned
alternative is writing the coinage to this plan's own
`src/messages/glossary-additions/07-NN.md` file, never editing `GLOSSARY.md`'s
marketing-only section in place. Name the reason in one clause: parallel plans,
one file each.

ACCENTED DISPLAY TYPE — create this section now as a placeholder marked
`CURRENT — pending 07-05`, with one line stating that whether Special Gothic
Expanded One renders `Á É Í Ó Ú Ñ` under the `.display` uppercase treatment is
UNVERIFIED at the time of writing, that plan 07-05 Task 2 measures it, and that
07-05 writes the outcome and any resulting copy rule back into this section.
Every Wave 3 and Wave 4 plan writes display headings against this file, so the
question must be visible here rather than buried in one plan's summary.

WORDPLAY ESCALATION — the rule that keeps a lost pun visible. Spanish copy on
this site is a re-write in the brand voice, not a literal translation, so a
translator is expected to depart from the English. The failure mode is the
quiet one: an English line whose effect depends on a pun, a rhythm, a double
meaning or a term of art with no Spanish equivalent gets rendered as a flat,
accurate sentence, the catalogue looks complete, and nobody ever learns the line
died. State the rule as LOCKED:

  If carrying an English string into Spanish costs its effect — not its meaning
  — the translator ships the best Spanish available AND records the loss in
  `.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md`. Shipping a
  flattened line without recording it is a defect. Leaving the key untranslated
  or empty is also a defect: parity gates fail, and a blank is not a flag.

Name the four triggers so it is a check, not a judgement call: (1) the English
turns on a pun, idiom or double meaning; (2) the effect is carried by rhythm or
sound that the Spanish loses; (3) the term has no Spanish equivalent the
GLOSSARY resolves and no marketing-only coinage feels right; (4) the Spanish
that preserves the meaning breaks the length budget the display clamp allows.

Record that the register is a phase deliverable read by the design owner, not a
scratch file. NINE plans write Spanish copy and therefore owe a count — 07-04,
07-05, 07-06, 07-07, 07-08, 07-09, 07-10, 07-13, 07-14. 07-04 is on that list
because it writes the shared chrome (Navbar, Footer, CTASection labels and the
cross-locale hint), and I18N-07 covers all user-facing strings, chrome included;
leaving it off would make the site's most-repeated copy the one surface with no
translation-quality owner. Each of the nine must end its summary with the line
`TRANSLATION FLAGS: n`, where `n` is the number of rows it wrote into its own
`TRANSLATION-FLAGS.07-NN.md`. Zero is a legitimate answer and is a claim the
plan's author is making; no line at all is an unanswered question, and plan
07-16 fails on the missing line rather than on a low count. That is deliberate:
a gate that demanded rows would manufacture them.

Add a short "How this is enforced" section naming the two mechanisms: the CI
gate from plan 07-03, and the human read-throughs D-05/D-08 require for
`/manifesto`, `/privacy` and `/terms`.

Finally, add a one-line entry to `.planning/codebase/design/README.md`'s index if
that file lists the design docs, so the new document is discoverable the same way
the others are.
  </action>
  <verify>
    <automated>test -f .planning/codebase/design/SPANISH-VOICE.md && grep -v '^#' .planning/codebase/design/SPANISH-VOICE.md | grep -qi "vosotros" && grep -v '^#' .planning/codebase/design/SPANISH-VOICE.md | grep -qi "usted" && grep -v '^#' .planning/codebase/design/SPANISH-VOICE.md | grep -qi "empoderar" && grep -qi "ACCENTED DISPLAY TYPE" .planning/codebase/design/SPANISH-VOICE.md && grep -qi "WORDPLAY ESCALATION" .planning/codebase/design/SPANISH-VOICE.md && grep -q "TRANSLATION-FLAGS.md" .planning/codebase/design/SPANISH-VOICE.md && echo VOICE_OK</automated>
  </verify>
  <acceptance_criteria>
    - Document is self-contained: applying it requires no other open file except GLOSSARY.md
    - `vosotros` is explicitly marked ALLOWED and excluded from any hard-fail list
    - `su` / `sus` is explicitly marked ADVISORY with the third-person-possessive rationale
    - All 16 Spanish anti-words present
    - Inverted `!` explicitly banned; both-marks rule for `?` stated
    - The four locked CTA translations present with the `text-nowrap` caveat
    - An `ACCENTED DISPLAY TYPE` section exists, marked pending 07-05, naming who fills it and why it is here rather than in a plan summary
    - A `WORDPLAY ESCALATION` section exists, marked LOCKED, naming its four triggers, pointing at `TRANSLATION-FLAGS.md`, and stating that a flattened-but-unrecorded line and an empty key are both defects
    - Uses the LOCKED / CURRENT / DEBT convention of the sibling design docs
  </acceptance_criteria>
  <done>One binding document defines Spanish voice for the whole phase, the CI gate in 07-03 can be built directly from it, and the one open typographic question has a named home.</done>
</task>


<task type="auto">
  <name>Task 4: Seed the lost-in-translation register and the per-plan drop directories</name>
  <read_first>.planning/codebase/design/SPANISH-VOICE.md, .planning/codebase/design/BRAND.md, src/messages/GLOSSARY.md</read_first>
  <files>.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md, src/messages/glossary-additions/README.md</files>
  <action>
Create `.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md` — the one
file the design owner reads to find every English line whose effect did not
survive into Spanish, so those lines can be re-written by hand rather than
shipped flat.

This task creates it EMPTY of findings and complete in structure.

IT IS NOT A SHARED WRITE TARGET. Waves 2-4 run nine copy-writing plans, several
of them in parallel, and a single shared markdown table under parallel execution
is a git conflict at best and a silently dropped append at worst — "append-only"
constrains what a plan may do to a row, not what a merge does to a file. So:

  - Each of the nine copy-writing plans (07-04, 07-05, 07-06, 07-07, 07-08,
    07-09, 07-10, 07-13, 07-14) writes its own
    `.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.07-NN.md`,
    created from the header template below, even when it has zero rows.
  - Plan 07-16 aggregates every per-plan file into the `## Aggregated rows`
    table of THIS file after the last translation wave lands, and reconciles the
    row totals against each plan summary's `TRANSLATION FLAGS: n` line.
  - No plan other than 07-16 writes to `TRANSLATION-FLAGS.md`.

State that contract explicitly in the file's header, naming the nine plans and
naming 07-16 as the only aggregator, so an executor reading only this file
cannot get it wrong.

Header, stating in two sentences: what the file is, that a row is a request for
a human rewrite and not a bug report, and that an empty file at the end of the
phase means the escalation rule was never applied rather than that nothing was
lost.

A `## Aggregated rows` section holding one table with exactly these columns, in
this order — created empty, filled only by plan 07-16:

  | Key | English | Spanish shipped | What was lost | Trigger |

Emit that header row verbatim (including the pipes and spacing shown above) so
each per-plan file can copy it byte-for-byte and 07-16's aggregation can match
on it.

  Key              namespace-qualified catalogue key, e.g. `home.hero.headline`
  English          the source string verbatim
  Spanish shipped  what is actually in `es/*.json` right now — never blank, never
                   a placeholder; the site must read correctly with no rewrite
  What was lost    one sentence, concrete: the pun, the rhythm, the double
                   meaning, the term of art. "Sounds worse" is not an entry
  Trigger          which of SPANISH-VOICE.md's four wordplay triggers fired

Below it, a `## Rewritten` section the design owner moves rows into once a line
has been hand-written, so the working queue stays short and the history is not
lost. Create it empty.

Below that, a SEPARATE `## Example (never counted, never deleted)` section
holding exactly one worked row. It goes under its own heading — not in
`## Aggregated rows` — because 07-16 reconciles the aggregated row count against
nine summaries that may legitimately all report `TRANSLATION FLAGS: 0`, and an
example row sitting in the counted table would make a correct zero total look
like a miscount. State in the heading line that this section is excluded from
reconciliation and is never deleted; the earlier instruction to delete it when a
real row arrives is withdrawn, because deleting it would also contradict the
no-row-is-ever-removed rule.

The example row uses the manifesto hook already resolved in 07-RESEARCH.md
section 7: `A method for becoming yourself` -> `Un método para llegar a ser
quien eres`. Write `método` WITH the acute accent. The unaccented `metodo` in an
earlier draft was a typo, and this phase's own diacritics requirement makes an
unaccented example the worst possible thing to hand a translator as a model.

Do not seed speculative rows for strings no plan has translated yet. Guessing
which lines will break is exactly the judgement the four triggers replace.

Finally, create `src/messages/glossary-additions/README.md`: one short paragraph
stating that a translation plan needing a term the app has no string for writes
`src/messages/glossary-additions/07-NN.md` (one file per plan) with the columns
`| English | Spanish | Why the app has no string | Plan |`, that plan 07-16
merges them into `src/messages/GLOSSARY.md`'s marketing-only section, that any
plan may READ every file in the directory, and that no plan may write to another
plan's file or to `GLOSSARY.md` during Waves 2-4.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f='.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md';if(!fs.existsSync(f)){console.error('register missing');process.exit(1)}const s=fs.readFileSync(f,'utf8');for(const need of ['| Key | English | Spanish shipped | What was lost | Trigger |','## Aggregated rows','## Rewritten','## Example (never counted, never deleted)','Un método para llegar a ser quien eres','TRANSLATION-FLAGS.07-']){if(!s.includes(need)){console.error('register missing: '+need);process.exit(1)}}if(/Un metodo/.test(s)){console.error('example row still uses the unaccented Un metodo');process.exit(1)}const agg=s.split('## Aggregated rows')[1].split('## Rewritten')[0];const rows=agg.split('\n').map(l=>l.trim()).filter(l=>l.startsWith('|')&&!l.startsWith('| Key |')&&!/^\|\s*-/.test(l));if(rows.length!==0){console.error('Aggregated rows must start empty, found '+rows.length+' row(s)');process.exit(1)}for(const n of ['04','05','06','07','08','09','10','13','14']){if(!s.includes('07-'+n)){console.error('register header does not name plan 07-'+n);process.exit(1)}}if(!fs.existsSync('src/messages/glossary-additions/README.md')){console.error('glossary-additions/README.md missing');process.exit(1)}console.log('FLAGS_SEEDED_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The file exists with the five named columns in the stated order, emitted verbatim as `| Key | English | Spanish shipped | What was lost | Trigger |`
    - `## Aggregated rows` exists and contains ZERO data rows
    - `## Rewritten` exists and is empty
    - `## Example (never counted, never deleted)` exists, holds exactly one row, and its Spanish reads `Un método para llegar a ser quien eres` — the string `Un metodo` appears nowhere in the file
    - The header names all nine copy-writing plans (07-04, 07-05, 07-06, 07-07, 07-08, 07-09, 07-10, 07-13, 07-14), names their per-plan filename pattern `TRANSLATION-FLAGS.07-NN.md`, and names 07-16 as the only plan permitted to write this file
    - The header states that an empty register at phase end means the rule was not applied, not that nothing was lost
    - `src/messages/glossary-additions/README.md` exists and states the one-file-per-plan convention, the four column names, and that 07-16 performs the merge
    - No speculative rows for untranslated surfaces
  </acceptance_criteria>
  <done>Each of the nine copy-writing plans has a conflict-free place of its own to record a line the design owner needs to rewrite and a marketing-only term it coined, and 07-16 has two aggregations to perform and reconcile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sibling app repo -> this repo | a script reads files from a different project and commits derived content here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information disclosure | `scripts/extract-glossary.mjs` reading the app repo | mitigate | Script reads exactly two named JSON files, emits only English/Spanish string pairs, and never walks the app tree or reads `.env`; committed output is reviewed as a diff before merge |
| T-07-02-02 | Tampering | build-time coupling to an external checkout | mitigate | Script is manual-invocation only; never referenced from `package.json` scripts used by build, from `next.config.ts`, or from CI. The committed glossary is a snapshot, not a live import |
| T-07-02-03 | Repudiation | glossary drifting from the app without anyone noticing | mitigate | Provenance line records source paths and extraction date; re-running the script reproduces the derived section, making drift a visible diff |
| T-07-02-04 | Spoofing | deriving a vocabulary from a file that is not the app's shipped catalogue | mitigate | Task 1's gate parses both catalogues, asserts the bare `es` locale code, rejects `es-ES`, and requires three known D-01 terms to resolve before any derivation runs; a failure halts the plan rather than degrading to hand-authored terms |
| T-07-02-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
1. Task 1's gate prints `GLOSSARY_SOURCE_OK` with the observed key counts.
2. `node scripts/extract-glossary.mjs` runs clean and its output matches the
   committed derived section of `src/messages/GLOSSARY.md`.
3. All eleven rows of 07-CONTEXT.md D-01 appear with matching Spanish values.
4. `grep -rn "el-portal/src/messages" package.json next.config.ts .github/` returns nothing.
5. `SPANISH-VOICE.md` review: `vosotros` allowed, `su` advisory, `usted` banned,
   inverted `!` banned, 16 anti-words present, `ACCENTED DISPLAY TYPE` section
   present and marked pending 07-05, `WORDPLAY ESCALATION` section present and
   marked LOCKED with its four triggers.
6. `TRANSLATION-FLAGS.md` exists with the five columns, an empty
   `## Aggregated rows` table, an empty `## Rewritten` section, and exactly one
   row under `## Example (never counted, never deleted)` reading `Un método`
   with its accent. `src/messages/glossary-additions/README.md` exists and
   states the one-file-per-plan convention.
</verification>

<success_criteria>
- The glossary source is proven before it is used, and a false premise halts the phase in Wave 1 rather than surfacing in Wave 4
- Product vocabulary is derived and reproducible, not authored
- The kept-English split is recorded as a first-class finding
- One binding voice document exists that CI and human reviewers both work from
- A line whose English effect cannot survive into Spanish has one named place to be recorded that no parallel plan competes for, so the design owner receives a rewrite queue instead of a silently flattened site
- Zero build-time or CI coupling to the sibling repo
</success_criteria>

<output>
Create `.planning/phases/07-spanish-localization/07-02-SUMMARY.md` when done.
Record: the observed en/es key counts from Task 1 and the locale code confirmed,
how many key-pairs the extraction yielded, any disagreement with 07-CONTEXT.md
D-01 and how the filter was corrected, the seed contents of the marketing-only
section, and confirmation that `TRANSLATION-FLAGS.md` is in place for Wave 2 to
append to.
</output>
