---
phase: 07-spanish-localization
plan: 08
type: execute
wave: 3
depends_on: ["07-03", "07-04", "07-05"]
files_modified:
  - src/app/[locale]/mcp/page.tsx
  - src/components/CopyButton.tsx
  - src/messages/en/mcp.json
  - src/messages/es/mcp.json
autonomous: true
requirements: [I18N-01, I18N-07]

must_haves:
  truths:
    - "/es/mcp renders in Spanish: tool descriptions, prose, headings and the table of contents"
    - "Every code block, JSON key, tool name and config snippet is byte-identical between locales"
    - "The copy-to-clipboard control is labelled in Spanish"
    - "KU-2's known English paragraph overflow does not get worse in Spanish, and no NEW offender is introduced"
    - "/mcp renders byte-identical English copy to before extraction"
  artifacts:
    - path: "src/messages/en/mcp.json"
      provides: "MCP page English prose, tool descriptions and headings"
      contains: "tools"
    - path: "src/messages/es/mcp.json"
      provides: "Spanish mirror of the mcp namespace"
  key_links:
    - from: "src/app/[locale]/mcp/page.tsx"
      to: "src/messages/en/mcp.json"
      via: "getTranslations('mcp') in a Server Component"
      pattern: "getTranslations"
    - from: "src/components/CopyButton.tsx"
      to: "src/messages/en/mcp.json"
      via: "translated label prop or useTranslations"
      pattern: "mcp"
---

<objective>
Translate `/mcp` (564 lines), the site's most technical surface, **without
translating a single character of code.**

Purpose: this page interleaves prose with executable configuration — an stdio
config block, an HTTP endpoint, and three example JSON payloads — plus a table of
twelve MCP tools whose *names* are API identifiers and whose *descriptions* are
prose. Translating an identifier produces documentation that does not work.
Drawing that line precisely is the whole difficulty of this plan, so it is stated
as a hard rule rather than left to judgement.

The page also carries KU-2 from Phase 5: twelve prose paragraphs already overflow
their box by up to 66px at 320px in English. Spanish prose is 15-25% longer.
07-UI-SPEC is explicit that a pre-existing defect getting *worse* under Spanish is
a new finding this phase owns, not something that inherits known-unfixed status
by association — so KU-2's numbers get re-measured here rather than assumed.

Output: an `mcp` namespace in both locales with code untouched.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-spanish-localization/07-CONTEXT.md
@.planning/phases/07-spanish-localization/07-UI-SPEC.md
@.planning/codebase/design/SPANISH-VOICE.md
@.planning/phases/05-mobile-responsive-retrofit/deferred-items.md
@src/messages/GLOSSARY.md
@CLAUDE.md

@src/app/[locale]/mcp/page.tsx
@src/components/CopyButton.tsx

<interfaces>
The page is a Server Component -> `getTranslations('mcp')`.
`CopyButton` is interactive; confirm its directive and use the matching API
(`useTranslations` if it is a Client Component).

NEVER TRANSLATE — these are code, not copy:
  STDIO_CONFIG, HTTP_CONFIG, EXAMPLE_SNAPSHOT, EXAMPLE_LOG_HABIT,
  EXAMPLE_MORNING   (template-literal code blocks)
  readTools[].name and writeTools[].name   (MCP tool identifiers)
  every JSON key inside an example payload
  anything rendered through `CodeBlock` or `InlineCode`

TRANSLATE:
  readTools[].description and writeTools[].description
  H2 / H3 headings, `Para` prose, `tocItems` labels
  the CodeBlock `label` prop (a caption, not code)
  CopyButton's visible label and its post-copy state text

Known-unfixed carried in from Phase 5 (containment.spec.ts):
  KU-2  /mcp  sweep A  selector p.text-sm.text-zinc-400.leading-5
        twelve prose paragraphs overflow their box by up to 66px at 320px and
        26px at 360px; none overflow at >= 390px.
  Plan 07-03 already added the mirrored `/es/mcp` suppression under the same ref.

Gate 2 behaviour (plan 07-03): translation status is INFERRED from the catalogue,
not configured anywhere. A namespace whose `es` file is exactly `{}` warns; the
moment it has any Spanish key it is fully enforced. There is no allowlist to
edit, and this plan must not touch `.github/workflows/` or `scripts/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract /mcp prose into the catalogue, leaving all code literal</name>
  <read_first>src/app/[locale]/mcp/page.tsx, src/components/CopyButton.tsx</read_first>
  <files>src/app/[locale]/mcp/page.tsx, src/components/CopyButton.tsx, src/messages/en/mcp.json</files>
  <action>
Move every user-facing prose string into `src/messages/en/mcp.json` under
semantic keys grouped by section — `hero.*`, `toc.*`, `sections.*`,
`tools.read[]`, `tools.write[]`, `copyButton.*`.

The tool tables are the delicate part. Each tool has a `name` that is an MCP
identifier and a `description` that is prose. Keep `name` in the component's
data structure as code, and move only `description` into the catalogue. Do not
restructure the tables into a single catalogue array of objects containing both,
because that puts an API identifier one careless edit away from being
"translated". Key the descriptions by tool name so the pairing stays obvious in
review.

Leave the five template-literal config and example blocks exactly where they are
in the module. They are code. They must be byte-identical between locales, and
the simplest guarantee of that is that they never enter the catalogue at all.

The `CodeBlock` `label` prop IS a caption and does get translated; the `code`
prop never does.

`CopyButton` currently defaults its label to the literal `"Copy"` and shows a
post-copy state. Both are user-facing. Confirm whether it is a Client Component
and use the matching next-intl API. Keep the `label` prop so callers can override
it, but source the default from the catalogue rather than from a string literal.
Phase 5 deliberately left `CopyButton`'s raw `button` element unconverted
(recorded as a decision in STATE.md) — do not convert it to the shared `Button`
component here either; that is out of scope and risks a `/mcp` styling
regression.

Replace prose literals with `getTranslations('mcp')`. The page stays a Server
Component. Swap internal `Link` imports to `@/i18n/navigation`.

Capture the rendered text of `/mcp` before and after and diff it; English must be
byte-identical.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npx playwright test containment.spec.ts a11y.spec.ts -g "/mcp"</automated>
  </verify>
  <acceptance_criteria>
    - Zero hardcoded user-facing prose remains in `mcp/page.tsx`
    - The five code template literals are unchanged and are not present in any message file
    - Tool `name` values stayed in code; only descriptions moved, keyed by tool name
    - `CodeBlock` label translated, `code` prop untouched
    - `CopyButton` default label sourced from the catalogue, raw button element unchanged
    - `getTranslations` used; page remains a Server Component
    - Rendered English text of `/mcp` diffs clean against the pre-extraction capture
  </acceptance_criteria>
  <done>/mcp prose is catalogue-driven and every code sample is still literal source.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Spanish MCP copy and re-measure KU-2</name>
  <read_first>src/messages/en/mcp.json, .planning/codebase/design/SPANISH-VOICE.md, src/messages/GLOSSARY.md, .planning/phases/05-mobile-responsive-retrofit/deferred-items.md</read_first>
  <files>src/messages/es/mcp.json, src/app/[locale]/mcp/page.tsx</files>
  <action>
Write the Spanish mirror into `src/messages/es/mcp.json`, key-identical to the
English file with matching array lengths and placeholder sets. The moment this
file stops being `{}`, plan 07-03's Gate 2 enforces the namespace fully — a
half-finished file fails the build, which is intended.

Apply `SPANISH-VOICE.md`: no written subject pronoun, `tu` conjugation for
instructions addressing the reader, infinitive for any action label, no `usted`
or formal `su`, no exclamation marks, none of the 16 anti-words. Technical
documentation is exactly where an unwary translator drifts into `usted` — this
page is not exempt, and the register gate will fail the build if it does.

Keep technical vocabulary technical. MCP, stdio, HTTP, JSON, endpoint names and
tool identifiers stay as they are. Product nouns resolve through
`src/messages/GLOSSARY.md`; if the page names `Habits`, `Goals` or `Trends`, use
the app's terms — remembering that `Trends` stays English.

Then re-measure KU-2 rather than assuming it carries over. Run the containment
sweep for `/es/mcp` at 320, 360, 390 and 430 and compare the reported
`overflowBy` numbers against Phase 5's recorded English baseline (up to 66px at
320px, 26px at 360px, none at 390px and above).

Three outcomes, three responses:
  - Spanish numbers comparable to English: KU-2's mirrored suppression stands.
    Record the measured numbers in the summary.
  - Spanish numbers materially worse than English: this phase owns it. Shorten
    the offending Spanish paragraphs. That is a copy fix and is always available.
  - A NEW offender appears in Spanish that has no English counterpart: fix it at
    source. It does not inherit KU status, and it must not be suppressed.

Do not touch the paragraph's `leading-5`, its `max-w-*`, or any clamp. Those are
Phase 5 tuned values shared with English; changing them changes English's
approved rendering, which RESP-08 forbids.
  </action>
  <verify>
    <automated>npm run i18n:gates && npm run build && npx playwright test containment.spec.ts a11y.spec.ts touch-targets.spec.ts -g "/es/mcp"</automated>
  </verify>
  <acceptance_criteria>
    - `mcp` en/es key-identical, equal array lengths, equal placeholder sets
    - `npm run i18n:gates` passes with `mcp` fully enforced by Gate 2 (no warning remains for this namespace)
    - Code blocks render identically on `/mcp` and `/es/mcp` — verified by diffing the rendered `pre` contents between the two routes
    - KU-2 overflow numbers measured on `/es/mcp` at all four widths and recorded
    - No new unsuppressed containment offender on `/es/mcp`
    - No `leading-*`, `max-w-*` or clamp changed
    - No file under `.github/workflows/` or `scripts/` was modified
    - `/mcp` and `/es/mcp` both prerendered
  </acceptance_criteria>
  <done>/es/mcp reads as Spanish technical documentation with English code, and KU-2 is measured rather than assumed.</done>
</task>


<task type="auto">
  <name>Task 3: Wordplay check — record what did not survive into Spanish</name>
  <read_first>.planning/codebase/design/SPANISH-VOICE.md (WORDPLAY ESCALATION), .planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md</read_first>
  <files>.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md</files>
  <action>
This task exists because I18N-07 requires it and because the loss it catches is
invisible by construction: a line carried by a pun, a rhythm, a double meaning or
a term of art gets rendered as a flat accurate sentence, the catalogue looks
complete, and nobody ever learns the line died. The catalogue cannot show you
that. Only this pass can.

Go back over the mcp namespace prose — code blocks are out of scope, they were never translated — the Spanish you just wrote, not the English you started
from — and check each string against SPANISH-VOICE.md's four wordplay triggers:

  1. the English turns on a pun, an idiom or a double meaning;
  2. the effect is carried by rhythm or sound that the Spanish loses;
  3. the term has no Spanish equivalent GLOSSARY.md resolves and no
     marketing-only coinage feels right;
  4. the Spanish that preserves the meaning breaks the length budget the
     display clamp allows, so a weaker phrasing shipped instead.

For every string where one fires, append a row to
`.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md`: key, English
verbatim, the Spanish actually shipped, one concrete sentence on what was lost,
and which trigger fired. The file is APPEND-ONLY — never edit or delete another
plan's rows, and never reorder the table.

Two rules that decide what a row means:

  - The Spanish shipped stays real copy. A row is a request for the design owner
    to re-write a line by hand, NOT a licence to leave the key empty, a
    placeholder, or the English string. The site has to read correctly whether or
    not that rewrite ever happens, and the parity gates fail on a blank anyway.
  - "Sounds worse" is not a row. Spanish here is a re-write in the brand voice,
    so departing from the English is expected and good. A row records a specific
    effect that died, named concretely enough that someone can decide whether to
    fix it.

Zero rows is a legitimate outcome and is a claim you are making. Do not
manufacture rows to look thorough — a padded register is worse than an empty one,
because it buries the lines that actually need attention.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md && grep -q "What was lost" .planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md && echo FLAGS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - Every Spanish string this plan wrote was checked against all four triggers, not sampled
    - Every row appended names a concrete lost effect, not a preference
    - No row leaves the shipped Spanish blank, placeholder, or English
    - No pre-existing row in the file was edited, reordered or deleted
    - The count of rows appended by this plan is stated in the summary as `TRANSLATION FLAGS: n`, including when n is 0
  </acceptance_criteria>
  <done>Any line whose English effect did not survive into Spanish is on the design owner's rewrite queue, and a zero count is an answer rather than a silence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalogue -> rendered documentation | translated values become integration instructions users copy and run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Tampering | code samples users copy and execute | mitigate | The five config and example blocks never enter the message catalogue, so no translation edit can alter an executable snippet; the acceptance check diffs rendered `pre` contents between locales |
| T-07-08-02 | Tampering | MCP tool identifiers | mitigate | `name` values stay in code; only `description` prose is translatable, so a mistranslation cannot rename an API surface |
| T-07-08-03 | Information disclosure | example payloads | accept | Payloads are pre-existing invented examples containing no credentials or real endpoints beyond the already-public app URL; this plan does not modify them |
| T-07-08-04 | Tampering | `CopyButton` clipboard content | mitigate | The button copies its `value` prop, which is sourced from the untranslated code constants; only its visible label becomes translatable |
| T-07-08-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
1. `npx tsc --noEmit`, `npm run lint`, `npm run i18n:gates` pass.
2. `npm run build` succeeds; `/mcp` and `/es/mcp` both prerendered.
3. Rendered `pre` contents are identical between `/mcp` and `/es/mcp`.
4. Containment, a11y and touch-target specs pass on both mcp routes.
5. Rendered English text of `/mcp` diffs clean against the pre-extraction capture.
</verification>

<success_criteria>
- /mcp prose fully catalogue-driven in both locales
- Zero code characters translated, proven by a rendered diff
- KU-2 re-measured on the Spanish route with numbers recorded
- No new containment offender, and none suppressed
</success_criteria>

<output>
Create `.planning/phases/07-spanish-localization/07-08-SUMMARY.md` when done.
Record: the measured `/es/mcp` overflow numbers against Phase 5's English
baseline, any paragraph shortened, and confirmation that rendered code blocks
diffed clean between locales.

End the summary with the line `TRANSLATION FLAGS: n`, where n is the number of
rows appended to TRANSLATION-FLAGS.md. Zero is a valid answer and must still be
written; plan 07-16 fails on a missing line, never on a low count.
</output>
