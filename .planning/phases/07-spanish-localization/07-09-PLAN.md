---
phase: 07-spanish-localization
plan: 09
type: execute
wave: 3
depends_on: ["07-03", "07-04", "07-05"]
files_modified:
  - src/components/pricing/PricingClient.tsx
  - src/messages/en/pricing.json
  - src/messages/es/pricing.json
  - .planning/phases/07-spanish-localization/TRANSLATION-FLAGS.07-09.md
  - src/messages/glossary-additions/07-09.md
autonomous: true
requirements: [I18N-01, I18N-07]

must_haves:
  truths:
    - "/es/pricing renders in Spanish: tier names, feature lists, the comparison table and the FAQ"
    - "Prices, currency symbols and billing periods are identical between locales"
    - "The plan badge fits inside the narrowest pricing card at 320px in Spanish"
    - "KU-4's known 77px comparison-table escape is not worse in Spanish, and no NEW offender is introduced"
    - "/pricing renders byte-identical English copy to before extraction"
    - "If the design owner rules mid-wave that pricing prose must stay English, the plan has a defined second path that still leaves the namespace Gate-2 clean"
  artifacts:
    - path: "src/messages/en/pricing.json"
      provides: "Tiers, features, comparison table and FAQ English copy"
      contains: "tiers"
    - path: "src/messages/es/pricing.json"
      provides: "Spanish mirror of the pricing namespace"
  key_links:
    - from: "src/components/pricing/PricingClient.tsx"
      to: "src/messages/en/pricing.json"
      via: "useTranslations('pricing') in the client subtree"
      pattern: "useTranslations"
---

<objective>
Translate `/pricing` (506 lines) — tiers, feature lists, comparison table and FAQ
— while leaving every price, currency symbol and billing period untouched.

Purpose: locale-specific pricing and currency are an explicit non-goal of this
phase (ROADMAP.md Non-goals). The Spanish page sells the same product at the same
price in the same currency; only the words around the numbers change. That
separation is what makes translating this page safe: a translator edits prose and
literally cannot reach a price, because prices stay in code behind ICU
placeholders.

This page carries the phase's single highest layout risk. The plan badge at
`pricing/page.tsx:424-434` is `whitespace-nowrap`, absolutely centred with
`left-1/2 -translate-x-1/2`, and has **no `max-width`**. It grows symmetrically
from the centre with no ellipsis fallback, so a Spanish badge string that is too
long does not truncate — it spills out of the card in both directions. 07-UI-SPEC
rates it High and states that truncation is not an acceptable fallback here
because a badge broken mid-word reads as a bug.

The page also carries KU-4: the comparison table already escapes its
`overflow-hidden` wrapper by 77px at 320px in English.

Output: a `pricing` namespace in both locales, with the badge measured rather
than eyeballed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-spanish-localization/07-CONTEXT.md
@.planning/phases/07-spanish-localization/07-UI-SPEC.md
@.planning/phases/07-spanish-localization/07-PATTERNS.md
@.planning/codebase/design/SPANISH-VOICE.md
@.planning/phases/05-mobile-responsive-retrofit/deferred-items.md
@src/messages/GLOSSARY.md
@CLAUDE.md

@src/components/pricing/PricingClient.tsx
@src/components/ui/badge.tsx

<interfaces>
Plan 07-01 split this route: `src/app/[locale]/pricing/page.tsx` is a thin
Server Component wrapper holding `setRequestLocale` and `generateMetadata`, and
the entire original body lives in `src/components/pricing/PricingClient.tsx`
with the `"use client"` directive (the pre-split file's `"use client"` at line 1
is confirmed in 07-PATTERNS.md). This plan owns the client component only; do not
edit the server wrapper.

Client subtree -> `useTranslations('pricing')`.

Data structures to extract (names from the pre-split file):
  tiers[]               -- name, price, period, blurb, features[], CTA label
  comparisonFeatures[]  -- name, plus per-tier cell values
  faqs[]                -- question, answer

NEVER TRANSLATE: price numerals, currency symbols, and any billing-period value
that is a number. Period *words* ("one-time", "per month") are prose and do
translate; the amounts do not.

SCOPE — PRICING PROSE IS TRANSLATED. NO BRANCH. I18N-01 lists `/pricing` as a
catalogue surface and this plan translates it in full.

An earlier draft carried a "PATH B" fallback for the case where the design owner
ruled mid-wave that pricing prose stays English. It is DELETED, and the deletion
is deliberate rather than an omission:

  - It was not executable as written. PATH B required one extra visible Spanish
    explanatory line in the pricing hero, but Task 2 owns only
    `src/messages/es/pricing.json`. Adding that key to the Spanish catalogue
    alone trips this phase's own bidirectional orphan-key gate (07-03 Gate 2);
    adding it to both catalogues without a render site produces a key nothing
    displays. Making it work would mean naming a component and a render site and
    adding `PricingClient.tsx` back into this task's scope — a second surface for
    a contingency nobody has invoked.
  - Nothing in the phase's source artifacts asks for it. D-01…D-14 contain no
    pricing-stays-English decision, and I18N-01 requires the opposite. The line
    is not load-bearing.
  - If the design owner does rule that pricing prose stays English, that is a
    change to the phase's scope, handled by re-planning this plan — not by an
    in-plan branch the executor silently selects.

What is NOT permitted, unchanged: a half-translated pricing page, or an `es`
file left at `{}` while the phase claims full coverage.

Layout-sensitive sites (07-UI-SPEC Layout-Sensitive Components Checklist):
  HIGH  plan badge, pricing/page.tsx:424-434 -- whitespace-nowrap, absolutely
        centred, NO max-width, no ellipsis fallback. Measure the rendered
        Spanish width against the narrowest card at 320px before shipping.
  MED   shadcn Badge primitive, ui/badge.tsx:8 -- whitespace-nowrap + w-fit.
        Audit EVERY call site for container headroom, not just the plan badge.
  HIGH  comparison table, pricing/page.tsx:243-300 -- overflow-hidden wrapper,
        pre-existing KU-4 escape of 77px at 320px, 37px at 360px, 7px at 390px.

PLANNER DECISION on KU-4's `data-reflow-exempt` reclassification, which
07-UI-SPEC deliberately left open: DO NOT reclassify in this phase. Marking the
table exempt would suppress the containment check rather than fix the layout,
and that check is the only thing that can detect Spanish copy making the escape
worse. Re-measure, do not exempt. If the Spanish numbers are worse, log it as a
design-owner finding in deferred-items.md rather than silencing the sweep.

Accented display headings: read SPANISH-VOICE.md's `ACCENTED DISPLAY TYPE`
section before writing any `.display` heading in Spanish.

Gate 2 behaviour (plan 07-03): translation status is INFERRED from the catalogue,
not configured anywhere. A namespace whose `es` file is exactly `{}` warns; the
moment it has any Spanish key it is fully enforced, and key identity runs in both
directions. There is no allowlist to edit, and this plan must not touch
`.github/workflows/` or `scripts/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the pricing page into the pricing namespace</name>
  <read_first>src/components/pricing/PricingClient.tsx, src/components/ui/badge.tsx, .planning/phases/07-spanish-localization/07-UI-SPEC.md (Layout-Sensitive Components Checklist)</read_first>
  <files>src/components/pricing/PricingClient.tsx, src/messages/en/pricing.json</files>
  <action>
Move every user-facing string into `src/messages/en/pricing.json` under semantic
keys grouped by section — `hero.*`, `tiers[]`, `comparison.*`, `faq[]`,
`badge.*`.

Keep `tiers`, `comparisonFeatures` and `faqs` as catalogue arrays so the parity
gate can compare lengths, and so a reordered tier is a visible diff rather than a
silent key shuffle.

Draw the price line carefully. A tier's amount and currency symbol are data and
stay in code; the surrounding words are copy and move to the catalogue. If a
price currently renders as one concatenated string, split it so the numeral is
interpolated into an ICU message with a placeholder rather than embedded in a
translatable string. A translator must not be able to edit a price by editing
prose, and the parity gate then checks the placeholder survives in both locales.

Comparison-table cell values that are booleans or an em-dash placeholder stay as
they are — they are symbols, not words. Cell values that are short phrases are
copy and move.

Replace literals with `useTranslations('pricing')`. The component stays a Client
Component; do not attempt `getTranslations` here. Swap internal `Link` imports to
`@/i18n/navigation`, leaving `APP_URL` external links on `next/link`.

Change no class name, no clamp, no `leading-*` value, no grid definition and
nothing about the badge's positioning. This task is a string move.

Capture the rendered text of `/pricing` before and after and diff it; English
must be byte-identical, prices included.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && node -e "const s=require('fs').readFileSync('src/components/pricing/PricingClient.tsx','utf8');if(!/useTranslations/.test(s)){console.error('PricingClient does not use useTranslations');process.exit(1)}if(!/use client/.test(s)){console.error('client directive lost');process.exit(1)}const en=require('./src/messages/en/pricing.json');if(!Array.isArray(en.tiers)){console.error('tiers is not a catalogue array');process.exit(1)}console.log('PRICING_EXTRACTION_OK')" && npm run build && npx playwright test containment.spec.ts a11y.spec.ts -g "/pricing"</automated>
  </verify>
  <acceptance_criteria>
    - Zero hardcoded user-facing strings remain in `PricingClient.tsx`
    - `tiers`, `comparison` and `faq` stored as catalogue arrays
    - Price numerals and currency symbols stayed in code and are interpolated via ICU placeholders where they appear inside a sentence
    - `useTranslations` used; component remains a Client Component
    - Internal Links import from `@/i18n/navigation`; Gate 3 passes
    - Rendered English text of `/pricing` diffs clean against the pre-extraction capture
    - No class, clamp, `leading-*`, grid or badge positioning changed
  </acceptance_criteria>
  <done>/pricing reads English copy from the catalogue with prices still in code.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Spanish pricing copy on the current path, measure the badge, and re-measure KU-4</name>
  <read_first>src/messages/en/pricing.json, .planning/codebase/design/SPANISH-VOICE.md, src/messages/GLOSSARY.md, .planning/phases/05-mobile-responsive-retrofit/deferred-items.md, .planning/phases/07-spanish-localization/07-UI-SPEC.md (Layout-Sensitive Components Checklist)</read_first>
  <files>src/messages/es/pricing.json</files>
  <action>
TRANSLATE. There is no branch to select and no decision to look up — the earlier
PATH A / PATH B split was deleted (see SCOPE in `<interfaces>`). Do not pause to
ask whether pricing prose should stay English; I18N-01 says it does not.

Write the Spanish mirror into `src/messages/es/pricing.json`,
key-identical to the English file with matching array lengths and placeholder
sets. The moment this file stops being `{}`, plan 07-03's Gate 2 enforces the
namespace fully — a half-finished file fails the build, which is intended, and
key identity runs in both directions.

Apply `SPANISH-VOICE.md`: no written subject pronoun, infinitive for tier CTAs,
`tu` conjugation for FAQ answers addressing the reader, no `usted` or formal
`su`, no exclamation marks, none of the 16 anti-words. FAQ answers are the
longest prose on this page and the easiest place to drift into a formal register.

Resolve product nouns through `src/messages/GLOSSARY.md`. Prices, currency and
billing amounts are untouched — locale-specific pricing is an explicit non-goal.

THE BADGE IS THE MEASUREMENT THAT MATTERS. Before accepting the
badge string, render `/es/pricing` at 320px and measure the badge's computed
width against the pricing card's computed width. The badge is
`whitespace-nowrap`, absolutely centred and has no `max-width`, so it grows out
of the card in both directions with no ellipsis to save it. If the measured width
is close to or exceeds the card width, shorten the Spanish badge copy. Truncation
is explicitly NOT an acceptable fallback here, and neither is adding a
`max-width` — that would change English's approved rendering too.

Audit every other `Badge` call site on the page the same way. The shadcn `Badge`
primitive is `whitespace-nowrap w-fit`, so it has the same grow-not-wrap
behaviour wherever it carries translated content, not only in the plan badge.

Then re-measure KU-4 rather than assuming it carries over. Run the containment
sweep for `/es/pricing` at 320, 360, 390 and 430 and compare the reported
`offscreenBy` numbers against Phase 5's English baseline (77px at 320, 37px at
360, 7px at 390, none at 430).

Three outcomes, three responses:
  - Comparable to English: the mirrored KU-4 suppression stands; record the
    numbers in the summary.
  - Materially worse in Spanish: this phase owns the delta. Shorten the offending
    Spanish column or row copy first, since that is the fix available without
    touching frozen layout. If it cannot be resolved by copy alone, log it in
    `deferred-items.md` as a design-owner finding — do NOT add
    `data-reflow-exempt`, and do not widen the suppression.
  - A NEW offender with no English counterpart: fix it at source; it does not
    inherit KU status.
  </action>
  <verify>
    <automated>npm run i18n:gates && node -e "const en=require('./src/messages/en/pricing.json'),es=require('./src/messages/es/pricing.json');const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>v&&typeof v==='object'?flat(v,p+k+'.'):[p+k]);const a=flat(en).sort().join('|'),b=flat(es).sort().join('|');if(a!==b){console.error('pricing en/es key sets differ');process.exit(1)}if(Object.keys(es).length===0){console.error('es/pricing.json left empty');process.exit(1)}const ident=flat(en).filter(k=>{const g=(o,k)=>k.split('.').reduce((a,x)=>a&&a[x],o);const a=g(en,k),b=g(es,k);return typeof a==='string'&&a===b&&a.trim().split(/\s+/).length>2});if(ident.length>3){console.error('too many pricing values identical to English — es/pricing.json looks like a copy, not a translation:',ident.slice(0,10));process.exit(1)}console.log('PRICING_PARITY_OK')" && npm run build && npx playwright test containment.spec.ts a11y.spec.ts touch-targets.spec.ts -g "/es/pricing"</automated>
  </verify>
  <acceptance_criteria>
    - `src/messages/es/pricing.json` contains real Spanish prose — not English values copied across, and not `{}`
    - `pricing` en/es key-identical, equal array lengths, equal placeholder sets, no orphaned `es` key
    - No new key was added to one catalogue without the other, and no key was added without a render site
    - `npm run i18n:gates` passes with `pricing` fully enforced by Gate 2 (no warning remains for this namespace)
    - The plan file contains no PATH A / PATH B branch and the executor selected no path — the summary states that pricing was translated per I18N-01
    - Spanish badge computed width measured at 320px against the narrowest card, and recorded
    - Every other `Badge` call site on the page audited for container headroom
    - KU-4 `offscreenBy` numbers measured on `/es/pricing` at all four widths and recorded
    - No `data-reflow-exempt` added anywhere; no suppression widened
    - Prices, currency symbols and amounts identical between locales
    - No file under `.github/workflows/` or `scripts/` was modified
    - `/pricing` and `/es/pricing` both prerendered
  </acceptance_criteria>
  <done>/es/pricing is translated in full, the badge is proven to fit, and KU-4 is measured rather than assumed.</done>
</task>


<task type="auto">
  <name>Task 3: Wordplay check — record what did not survive into Spanish</name>
  <read_first>.planning/codebase/design/SPANISH-VOICE.md (WORDPLAY ESCALATION), .planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md (read-only: header, five-column row template and the Example section)</read_first>
  <files>.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.07-09.md</files>
  <action>
This task exists because I18N-07 requires it and because the loss it catches is
invisible by construction: a line carried by a pun, a rhythm, a double meaning or
a term of art gets rendered as a flat accurate sentence, the catalogue looks
complete, and nobody ever learns the line died. The catalogue cannot show you
that. Only this pass can.

Go back over the pricing namespace — the Spanish you just wrote, not the English
you started from — and check each string against SPANISH-VOICE.md's four wordplay
triggers:

  1. the English turns on a pun, an idiom or a double meaning;
  2. the effect is carried by rhythm or sound that the Spanish loses;
  3. the term has no Spanish equivalent GLOSSARY.md resolves and no
     marketing-only coinage feels right;
  4. the Spanish that preserves the meaning breaks the length budget the
     display clamp allows, so a weaker phrasing shipped instead.

For every string where one fires, append a row to THIS PLAN'S OWN register file,
`.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.07-09.md`: key,
English verbatim, the Spanish actually shipped, one concrete sentence on what was
lost, and which trigger fired.

ONE FILE PER PLAN, and never the shared one. Waves 3 and 4 run several plans in
parallel, so a single shared markdown table is either a git conflict or a
silently lost append across isolated worktrees — "append-only" prevents semantic
deletion and does nothing about either of those. Create
`TRANSLATION-FLAGS.07-09.md` even when this plan produces zero rows, copying the
five-column header verbatim from `TRANSLATION-FLAGS.md`, and write only rows this
plan produced. Do NOT write to `TRANSLATION-FLAGS.md` itself; plan 07-16
aggregates every per-plan file into it after the last translation wave lands.

Two rules that decide what a row means:

  - The Spanish shipped stays real copy. A row is a request for the design owner
    to re-write a line by hand, NOT a licence to leave the key empty, a
    placeholder, or the English string. The site has to read correctly whether or
    not that rewrite ever happens, and the parity gates fail on a blank anyway.
  - "Sounds worse" is not a row. Spanish here is a re-write in the brand voice,
    so departing from the English is expected and good. A row records a specific
    effect that died, named concretely enough that someone can decide whether to
    fix it.

Zero rows is a legitimate outcome and is a claim you are making. Do not
manufacture rows to look thorough — a padded register is worse than an empty one,
because it buries the lines that actually need attention.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const dir='.planning/phases/07-spanish-localization/';const own=dir+'TRANSLATION-FLAGS.07-09.md';if(!fs.existsSync(dir+'TRANSLATION-FLAGS.md')){console.error('shared register missing - plan 07-02 did not run');process.exit(1)}if(!fs.existsSync(own)){console.error('missing per-plan register '+own+' - create it even when zero rows apply');process.exit(1)}const s=fs.readFileSync(own,'utf8');if(!s.includes('| Key | English | Spanish shipped | What was lost | Trigger |')){console.error('per-plan register missing the five-column header copied from TRANSLATION-FLAGS.md');process.exit(1)}const rows=s.split('\n').map(l=>l.trim()).filter(l=>l.startsWith('|')&&!l.startsWith('| Key |')&&!/^\|\s*-/.test(l));console.log('FLAGS_OWN_FILE_OK rows='+rows.length)"</automated>
  </verify>
  <acceptance_criteria>
    - Every Spanish string this plan wrote was checked against all four triggers, not sampled
    - Every row appended names a concrete lost effect, not a preference
    - No row leaves the shipped Spanish blank, placeholder, or English
    - `.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.07-09.md` exists, carries the five-column header verbatim, and contains only rows this plan produced — zero rows is valid and the file is still created
    - The shared `TRANSLATION-FLAGS.md` is byte-unchanged by this plan (`git diff --stat -- .planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md` is empty)
    - Any marketing-only term this plan coined is recorded in `src/messages/glossary-additions/07-09.md`; `src/messages/GLOSSARY.md` is byte-unchanged by this plan
    - The count of rows appended by this plan is stated in the summary as `TRANSLATION FLAGS: n`, including when n is 0
  </acceptance_criteria>
  <done>Any line whose English effect did not survive into Spanish is on the design owner's rewrite queue, and a zero count is an answer rather than a silence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalogue -> rendered commercial claims | translated values state what a customer gets for a price |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Tampering | price values | mitigate | Numerals and currency symbols stay in code and are interpolated through ICU placeholders; a catalogue edit cannot change a price, and the parity gate fails if a placeholder is dropped in either locale |
| T-07-09-02 | Repudiation | divergent commercial claims between locales | mitigate | Parity gate enforces key-identical tier and comparison arrays, so a feature cannot be promised in one locale and absent in the other |
| T-07-09-03 | Tampering | `pricing` catalogue values | mitigate | Rendered as React text nodes via `useTranslations`; no `dangerouslySetInnerHTML` introduced |
| T-07-09-04 | Repudiation | suppressing KU-4 instead of measuring it | mitigate | Planner decision recorded in `<interfaces>`: no `data-reflow-exempt` reclassification this phase; any worsening is logged as a design-owner finding with measured numbers |
| T-07-09-05 | Repudiation | an untranslated or half-translated pricing page shipping as complete | mitigate | There is one path and it translates: the empty, half-translated and English-copied states are each named as forbidden and each caught mechanically — Gate 2 for parity, this plan's own gate for emptiness, and a same-value-count check for a catalogue copied across rather than translated |
| T-07-09-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
1. `npx tsc --noEmit`, `npm run lint`, `npm run i18n:gates` pass.
2. `npm run build` succeeds; `/pricing` and `/es/pricing` both prerendered.
3. Containment, a11y and touch-target specs pass on both pricing routes.
4. Badge width at 320px measured and recorded for both locales.
5. `es/pricing.json` is complete, key-identical, and contains real Spanish prose rather than English values copied across.
6. Rendered English text of `/pricing` diffs clean against the pre-extraction
   capture, prices included.
</verification>

<success_criteria>
- /pricing fully catalogue-driven in both locales with prices in code
- The translate-or-not question has a defined answer on both branches, and neither branch blocks the wave
- Spanish badge measured to fit the narrowest card at 320px
- KU-4 re-measured on the Spanish route with numbers recorded and no new suppression
- No locale-specific pricing, currency or type scale introduced
</success_criteria>

<output>
Create `.planning/phases/07-spanish-localization/07-09-SUMMARY.md` when done.
Record: which path (A or B) was taken and on what evidence, measured badge and
card widths at 320px in both locales, the `/es/pricing` KU-4 numbers against
Phase 5's baseline, and any copy shortened as a result.

End the summary with the line `TRANSLATION FLAGS: n`, where n is the number of
rows appended to TRANSLATION-FLAGS.07-09.md. Zero is a valid answer and must still be
written; plan 07-16 fails on a missing line, never on a low count.
</output>
