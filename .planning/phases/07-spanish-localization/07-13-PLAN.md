---
phase: 07-spanish-localization
plan: 13
type: execute
wave: 4
depends_on: ["07-11"]
files_modified:
  - src/messages/es/features.json
autonomous: true
requirements: [I18N-01, I18N-07]

must_haves:
  truths:
    - "/es/features renders entirely in Spanish across all five deep-dive sections"
    - "Product nouns match the shipped app exactly, including the terms that stay English"
    - "The tu register holds across roughly ninety strings with no usted and no written subject pronoun"
    - "No Spanish string on /es/features is wider than its own box at any tested width"
    - "The English page is untouched — this plan modifies exactly one file"
  artifacts:
    - path: "src/messages/es/features.json"
      provides: "Complete Spanish copy for the largest surface on the site"
      contains: "scales"
  key_links:
    - from: "src/messages/es/features.json"
      to: "src/messages/en/features.json"
      via: "key-identical mirror enforced by the parity gate"
      pattern: "features"
---

<objective>
Write the Spanish copy for `/features` — roughly ninety strings across five
deep-dive sections — as a single-file translation.

Purpose: plan 07-11 extracted this page into a structured English catalogue
specifically so this plan's diff is one file of prose with no interleaved JSX
churn. That is what makes ninety strings reviewable.

The constraint that shapes the work: this plan may modify exactly one file. If a
Spanish string does not fit its box, the fix is shorter copy — which is a change
to this file. Any problem that would require a component edit is out of scope and
gets logged for the final verification plan rather than solved by widening the
blast radius.

Output: `src/messages/es/features.json`, complete and passing the gates.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-spanish-localization/07-CONTEXT.md
@.planning/phases/07-spanish-localization/07-UI-SPEC.md
@.planning/codebase/design/SPANISH-VOICE.md
@.planning/codebase/design/BRAND.md
@src/messages/GLOSSARY.md
@.planning/phases/07-spanish-localization/07-11-SUMMARY.md
@CLAUDE.md

@src/messages/en/features.json
@src/messages/es/manifesto.json

<interfaces>
Catalogue structure was fixed by plan 07-11: one top-level key per page section
(`hero`, `temporalHierarchy`, `dailyScore`, `pulseLoop`, `habitsAndGoals`,
`trendsInsights`), with nested lists preserved as arrays. Read
`07-11-SUMMARY.md` for the exact key structure before starting.

`src/messages/es/manifesto.json` is the approved register reference — plan
07-05's checkpoint signed it off as the standard the rest of the site is written
to. When a sentence is ambiguous, match that file's voice.

Glossary rows that appear on this surface:
  Versions -> Versiones   Cycle -> Ciclo   Day -> Día
  Habits -> Hábitos   Goals -> Objetivos   Score -> Puntuación
  Archives -> Archivos
  Trends -> Trends   Dashboard -> Dashboard   Cinema -> Cinema   Pulse -> Pulse
The four on the last line stay ENGLISH despite having obvious Spanish
equivalents. This is the split 07-CONTEXT.md D-01 warns is not guessable.

Gate 2 behaviour (plan 07-03): translation status is INFERRED from the catalogue.
`src/messages/es/features.json` currently sits at `{}` and Gate 2 warns. The
moment this plan writes the first Spanish key, the namespace flips to FULL
enforcement — so a half-finished file fails the build. Finish the file within the
plan. There is no allowlist to edit and this plan must not touch
`.github/workflows/` or `scripts/`.

Known-unfixed on this route:
  KU-1  /features  sweep A  span.font-mono.text-[11px].uppercase
        the English "Connectedness" label overflows its box by 7px at every
        viewport including desktop-1440. If the Spanish label is shorter, the
        Spanish route may simply stop reporting it — that is fine and expected,
        and the mirrored suppression staying unused is not a failure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate the page chrome, SCALES, and the first two deep-dive sections</name>
  <read_first>src/messages/en/features.json, .planning/phases/07-spanish-localization/07-11-SUMMARY.md, .planning/codebase/design/SPANISH-VOICE.md, src/messages/GLOSSARY.md, src/messages/es/manifesto.json</read_first>
  <files>src/messages/es/features.json</files>
  <action>
Write the Spanish values for `hero`, the eyebrow labels, `scales` and the
`temporalHierarchy` and `dailyScore` sections into
`src/messages/es/features.json`, mirroring the English file's key structure and
array lengths exactly.

Apply `SPANISH-VOICE.md` throughout: never write a subject pronoun; infinitive
for CTAs, buttons and links; nominal headings where that reads naturally and `tu`
where it does not; `tu` conjugation for prose addressing the reader; `tu X`
rather than the definite-article dodge; no `usted`, no formal `su`, no
exclamation marks, none of the 16 Spanish anti-words. Peninsular Spanish is
intended — write naturally rather than hedging toward a neutral register.

Look every product noun up in `src/messages/GLOSSARY.md`. Do not infer from
pattern: `Habits` becomes `Hábitos` but `Trends` stays `Trends`, and there is no
rule connecting the two. A term the app has no string for gets appended to the
glossary's marketing-only section (D-02b) rather than coined inline.

Match `src/messages/es/manifesto.json`'s voice when a sentence could go several
ways. That file's register was explicitly approved at plan 07-05's checkpoint and
is the standard the rest of the site is measured against.

Eyebrow labels are short uppercase strings in a monospace face with tight boxes.
Prefer a compact Spanish term over a literal one — this is the category KU-1
already tracks an English offender in.

Do not edit `src/messages/en/features.json`. If the English side contains a typo
or an awkward phrase, note it in the summary; fixing it here would put an English
copy change inside a Spanish translation diff.

Because Gate 2 escalates to full enforcement as soon as this file has any key, do
not run the gate expecting green until Task 2 completes the file. That is
expected mid-plan, not a defect.
  </action>
  <verify>
    <automated>node -e "const en=require('./src/messages/en/features.json'),es=require('./src/messages/es/features.json');const f=(o,p='')=>Object.entries(o).flatMap(([k,v])=>v&&typeof v==='object'?f(v,p+k+'.'):[p+k]);const a=f(en),b=new Set(f(es));const missing=a.filter(k=>!b.has(k));console.log('translated',a.length-missing.length,'of',a.length);if(missing.length>a.length/2){console.error('less than half translated');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `hero`, eyebrows, `scales`, `temporalHierarchy` and `dailyScore` fully translated with matching array lengths
    - Product nouns resolved through GLOSSARY.md; the four kept-English terms untouched
    - No `usted`, no formal `su`, no written subject pronoun, no exclamation marks, no anti-words
    - Register consistent with `src/messages/es/manifesto.json`
    - `src/messages/en/features.json` unmodified
  </acceptance_criteria>
  <done>The top half of /es/features reads as natural peninsular Spanish in the approved register.</done>
</task>

<task type="auto">
  <name>Task 2: Translate the three remaining sections and prove the Spanish page contains itself</name>
  <read_first>src/messages/en/features.json, src/messages/es/features.json, .planning/phases/07-spanish-localization/07-UI-SPEC.md (Text-expansion contract)</read_first>
  <files>src/messages/es/features.json</files>
  <action>
Translate `pulseLoop`, `habitsAndGoals`, `trendsInsights` and the closing CTA,
completing the file. Same rules as Task 1, same glossary, same register
reference.

Then measure. Run the containment sweep for `/es/features` at 320, 360, 390 and
430, plus the desktop widths — KU-1 proved this page carries a width-independent
overflow class, so a Spanish label can fail at 1440 just as easily as at 320.

The text-expansion contract governs what counts as a failure. A heading wrapping
to MORE lines in Spanish is acceptable and expected; `text-balance` and `max-w-*`
exist to reflow. Text wider than its own box is not acceptable, and neither is
content escaping a clipping ancestor.

THE ONLY FIX AVAILABLE TO THIS PLAN IS SHORTER COPY. This plan owns exactly one
file. Shrinking a clamp to fit a long Spanish string is explicitly forbidden by
the text-expansion contract, a locale-specific type scale is forbidden, and a
component edit is outside this plan's file ownership. If a Spanish string
overflows, rewrite it shorter — the register's infinitive constructions and
nominal headings are naturally compact, so this is nearly always available.

If a genuine offender cannot be resolved by copy alone, do NOT suppress it and do
NOT edit a component. Record it in the summary as a finding for the final
verification plan, with the selector, the measured overflow and the viewports.

Finish by confirming `npm run i18n:gates` is green: with the file complete, Gate
2 enforces `features` fully and should report neither a warning nor a failure for
that namespace.
  </action>
  <verify>
    <automated>npm run i18n:gates && npm run build && npx playwright test containment.spec.ts a11y.spec.ts -g "/es/features"</automated>
  </verify>
  <acceptance_criteria>
    - `features` en/es key-identical with equal array lengths and equal placeholder sets
    - `npm run i18n:gates` passes with `features` fully enforced and no remaining warning for that namespace
    - Containment passes on `/es/features` at 320, 360, 390, 430 and the desktop widths, with no new suppression
    - Any unresolvable offender is recorded as a finding, not suppressed and not fixed by a component edit
    - `src/messages/es/features.json` is the only file this plan modified
    - `/features` and `/es/features` both still prerendered
  </acceptance_criteria>
  <done>/es/features is fully translated and proven to contain itself at every tested width.</done>
</task>


<task type="auto">
  <name>Task 3: Wordplay check — record what did not survive into Spanish</name>
  <read_first>.planning/codebase/design/SPANISH-VOICE.md (WORDPLAY ESCALATION), .planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md</read_first>
  <files>.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md</files>
  <action>
This task exists because I18N-07 requires it and because the loss it catches is
invisible by construction: a line carried by a pun, a rhythm, a double meaning or
a term of art gets rendered as a flat accurate sentence, the catalogue looks
complete, and nobody ever learns the line died. The catalogue cannot show you
that. Only this pass can.

Go back over the features namespace — the Spanish you just wrote, not the English you started
from — and check each string against SPANISH-VOICE.md's four wordplay triggers:

  1. the English turns on a pun, an idiom or a double meaning;
  2. the effect is carried by rhythm or sound that the Spanish loses;
  3. the term has no Spanish equivalent GLOSSARY.md resolves and no
     marketing-only coinage feels right;
  4. the Spanish that preserves the meaning breaks the length budget the
     display clamp allows, so a weaker phrasing shipped instead.

For every string where one fires, append a row to
`.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md`: key, English
verbatim, the Spanish actually shipped, one concrete sentence on what was lost,
and which trigger fired. The file is APPEND-ONLY — never edit or delete another
plan's rows, and never reorder the table.

Two rules that decide what a row means:

  - The Spanish shipped stays real copy. A row is a request for the design owner
    to re-write a line by hand, NOT a licence to leave the key empty, a
    placeholder, or the English string. The site has to read correctly whether or
    not that rewrite ever happens, and the parity gates fail on a blank anyway.
  - "Sounds worse" is not a row. Spanish here is a re-write in the brand voice,
    so departing from the English is expected and good. A row records a specific
    effect that died, named concretely enough that someone can decide whether to
    fix it.

Zero rows is a legitimate outcome and is a claim you are making. Do not
manufacture rows to look thorough — a padded register is worse than an empty one,
because it buries the lines that actually need attention.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md && grep -q "What was lost" .planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md && echo FLAGS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - Every Spanish string this plan wrote was checked against all four triggers, not sampled
    - Every row appended names a concrete lost effect, not a preference
    - No row leaves the shipped Spanish blank, placeholder, or English
    - No pre-existing row in the file was edited, reordered or deleted
    - The count of rows appended by this plan is stated in the summary as `TRANSLATION FLAGS: n`, including when n is 0
  </acceptance_criteria>
  <done>Any line whose English effect did not survive into Spanish is on the design owner's rewrite queue, and a zero count is an answer rather than a silence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalogue -> rendered feature claims | translated values state what the product does to a Spanish-reading buyer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13-01 | Repudiation | a Spanish feature claim diverging from the English one | mitigate | Parity gate enforces key-identical structure and array lengths, so a claim cannot be added, dropped or reordered in one locale only |
| T-07-13-02 | Tampering | `features` catalogue values | mitigate | Rendered as React text nodes by the components plan 07-11 already wired; this plan adds no markup and touches no component |
| T-07-13-03 | Repudiation | inventing product vocabulary that contradicts the shipped app | mitigate | Terms resolve through the derived GLOSSARY.md; unlisted terms are appended to its marketing-only section rather than coined inline |
| T-07-13-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
1. `npm run i18n:gates` passes with `features` enforced.
2. `npm run build` succeeds; `/features` and `/es/features` both prerendered.
3. Containment and a11y pass on `/es/features` across mobile and desktop widths.
4. `git diff --name-only` shows only `src/messages/es/features.json`.
</verification>

<success_criteria>
- Roughly ninety Spanish strings, register-consistent with the approved manifesto
- Product vocabulary matches the shipped app including the kept-English terms
- Spanish page contains itself at every tested width, fixed by copy only
- Exactly one source file modified
</success_criteria>

<output>
Create `.planning/phases/07-spanish-localization/07-13-SUMMARY.md` when done.
Record: strings shortened for containment and why, terms appended to the
glossary's marketing-only section, any English typo noticed but deliberately not
fixed, and any offender left as a finding for the final verification plan.

End the summary with the line `TRANSLATION FLAGS: n`, where n is the number of
rows appended to TRANSLATION-FLAGS.md. Zero is a valid answer and must still be
written; plan 07-16 fails on a missing line, never on a low count.
</output>
