---
phase: 07-spanish-localization
plan: 07
type: execute
wave: 3
depends_on: ["07-03", "07-04", "07-05"]
files_modified:
  - src/components/SystemBlueprintSection.tsx
  - src/components/MethodologyPreviewSection.tsx
  - src/data/systemBlueprintData.ts
  - src/messages/en/blueprint.json
  - src/messages/es/blueprint.json
  - .planning/phases/07-spanish-localization/TRANSLATION-FLAGS.07-07.md
  - src/messages/glossary-additions/07-07.md
autonomous: true
requirements: [I18N-01, I18N-07]

must_haves:
  truths:
    - "The lower half of /es renders in Spanish: the system blueprint dashboard mockup and the three methodology principles"
    - "Trends stays English even though it sits in a title= prop — the one string in this pair that is not translated"
    - "Sample goal titles inside the blueprint mockup are short enough not to truncate mid-word in Spanish"
    - "The three methodology principle cards read as natural tu-register Spanish"
    - "/ renders byte-identical English copy to before extraction"
  artifacts:
    - path: "src/messages/en/blueprint.json"
      provides: "SystemBlueprintSection and MethodologyPreviewSection English copy"
      contains: "principles"
    - path: "src/messages/es/blueprint.json"
      provides: "Spanish mirror of the blueprint namespace"
  key_links:
    - from: "src/components/SystemBlueprintSection.tsx"
      to: "src/messages/en/blueprint.json"
      via: "getTranslations('blueprint') in a Server Component"
      pattern: "getTranslations"
    - from: "src/components/MethodologyPreviewSection.tsx"
      to: "src/i18n/navigation.ts"
      via: "locale-aware Link on the methodology CTA"
      pattern: "@/i18n/navigation"
---

<objective>
Translate the lower half of the home page: `SystemBlueprintSection` (the largest
single home component at 511 lines, including the dashboard mockup) and
`MethodologyPreviewSection` with its three principle cards.

Purpose: this is the second half of the home page, split from plan 07-06 so
neither plan carries an unreviewable diff. It is separated by namespace
(`blueprint` versus `home`) precisely so the two can run in parallel without
touching the same catalogue file.

The specific risk here is `SystemBlueprintSection.tsx:186`, where sample goal
titles render with Tailwind's `truncate`. Truncation degrades gracefully and is
not a containment failure, but a Spanish sample goal that ellipsises mid-word
reads as a broken mockup rather than as illustrative data. That is a copy-length
decision, not a layout fix.

The second risk is quieter: `Trends` sits in a `title=` prop at line 50 and looks
exactly like every other translatable prop on the page, but the locked glossary
keeps it English. A mechanical "translate every prop" pass gets it wrong.

Output: a `blueprint` namespace in both locales, with English rendering
unchanged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-spanish-localization/07-CONTEXT.md
@.planning/phases/07-spanish-localization/07-UI-SPEC.md
@.planning/phases/07-spanish-localization/07-PATTERNS.md
@.planning/codebase/design/SPANISH-VOICE.md
@src/messages/GLOSSARY.md
@CLAUDE.md

@src/components/SystemBlueprintSection.tsx
@src/components/MethodologyPreviewSection.tsx
@src/data/systemBlueprintData.ts

<interfaces>
Both components are Server Components -> use `getTranslations('blueprint')` from
`next-intl/server`. Do not add `"use client"` to either; doing so would move them
into the client bundle for no gain.

VERIFIED STRUCTURE (07-PATTERNS.md, direct read 2026-08-02). Where a line number
has moved, trust the symbol and the quoted text:

  src/data/systemBlueprintData.ts (5 lines, confirmed) holds exactly two
    user-facing strings and nothing else:
      systemBlueprintData.header.badge  "BEHAVIORAL SYSTEM & PATTERN RECOGNITION"
      systemBlueprintData.header.title  "Stop guessing. Start executing with deterministic precision."
    Both move into the catalogue, which leaves the module an empty shell.

  src/components/SystemBlueprintSection.tsx (511 lines) — props are real here,
    but ONLY at the top of the file. There are exactly two prop-based string
    pairs, both on `CardHeading`:
      lines 28-32  title="The Lab"  description="Where you design, execute, and iterate on your self-development system."
      lines 48-52  title="Trends"   description="A correlation engine and AI model, evaluate your daily logs and biometric data to find patterns that were hidden."
    `Trends` (line 50) is a KEPT-ENGLISH glossary term per D-01 and must NOT be
    translated despite sitting in a `title=` prop. It is the single exception on
    this surface — its `description` DOES translate.
    Everything below those two headings is inline-string territory inside the
    mockups, not props: `LabMockup` starts at line 88, its sample goal array is
    at lines 175-180, and the `truncate` className on those titles is at
    line 186. `CardHeadingProps` is declared at line 493 and the component at
    line 499 — do not restructure either.

  src/components/MethodologyPreviewSection.tsx (121 lines) is the one file in
    this pair with a genuine, verified content array — 07-PATTERNS.md found zero
    discrepancies in it:
      PRINCIPLES, lines 19-35, 3 items, each { num, title, body }
      rendered by `.map()` at line 80
    Two further strings sit OUTSIDE the array and a data-driven pass will miss
    them: the eyebrow `Features` at line 59, and the CTA link text
    `Read the full methodology` at line 113 wrapping the file's only `/features`
    href.

There is no `description` prop contract anywhere else in this pair. Where the
copy is inline, lift the literal — grep for JSX text nodes and for quoted
literals passed as `title=` / `description=` / `label=` props and reconcile the
hits against the list above.

MOCKUP SAMPLE DATA — a scope question to settle, not to assume. The sample goal
titles at lines 175-180 ("Sub-22 5k race in 30 days", "Marketing site v2",
"Hit 100 paying customers", "Read 12 books this version") are invented
illustrative content inside a screenshot-style UI, not site prose. This plan
DOES translate them: a Spanish visitor seeing an English mockup reads the page as
untranslated, and 07-UI-SPEC classifies mockup copy as reader-facing. Record that
call in the summary so a later reader knows it was a decision rather than an
oversight.

Layout-sensitive site (07-UI-SPEC Layout-Sensitive Components Checklist):
  SystemBlueprintSection.tsx:186 -- sample goal titles use `truncate` (ellipsis).
  Low risk, degrades gracefully. Prefer Spanish sample titles short enough to
  avoid mid-word ellipsis; a truncated sample goal is not a harness failure.

OUT OF SCOPE — `src/components/DashboardPreview.tsx` is dead code (zero imports,
confirmed in 07-PATTERNS.md). It looks like a sibling of the blueprint mockup and
is not one. Do not extract from it.

Glossary rows likely on these surfaces:
  Goals -> Objetivos   Habits -> Hábitos   Score -> Puntuación
  Dashboard -> Dashboard (kept English)   Trends -> Trends (kept English)

Accented display headings: read SPANISH-VOICE.md's `ACCENTED DISPLAY TYPE`
section before writing any `.display` heading in Spanish; plan 07-05 filled it
with a measured verdict.

Gate 2 behaviour (plan 07-03): translation status is INFERRED from the catalogue,
not configured anywhere. A namespace whose `es` file is exactly `{}` warns; the
moment it has any Spanish key it is fully enforced, and key identity runs in both
directions. There is no allowlist to edit, and this plan must not touch
`.github/workflows/` or `scripts/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract SystemBlueprintSection and MethodologyPreviewSection into the blueprint namespace</name>
  <read_first>src/components/SystemBlueprintSection.tsx, src/components/MethodologyPreviewSection.tsx, src/data/systemBlueprintData.ts, .planning/phases/07-spanish-localization/07-PATTERNS.md (SystemBlueprintSection and MethodologyPreviewSection sections)</read_first>
  <files>src/components/SystemBlueprintSection.tsx, src/components/MethodologyPreviewSection.tsx, src/data/systemBlueprintData.ts, src/messages/en/blueprint.json</files>
  <action>
Work from the VERIFIED STRUCTURE list in `<interfaces>` as the checklist, and
from a grep over JSX text nodes and `title=` / `description=` / `label=` string
literals as the audit that the checklist was complete. Two prop pairs at the top
of `SystemBlueprintSection`, everything else inline — do not go looking for a
content model below line 60; there isn't one.

Move every user-facing string in both components, plus the two strings in
`src/data/systemBlueprintData.ts`, into `src/messages/en/blueprint.json` under
semantic keys grouped by component — `blueprintHeader.*`, `mockup.*`,
`methodology.*`.

`MethodologyPreviewSection`'s `PRINCIPLES` array (three entries, each with `num`,
`title` and `body`) becomes a catalogue array. Keep `num` as presentational data
in the component if it is a pure sequence marker; only the human-readable
`title` and `body` need translating, and moving a hardcoded `"01"` into a message
catalogue adds a translatable string that can never legitimately differ. Do not
forget the eyebrow (line 59) and the CTA link text (line 113) — they sit outside
the array and a data-driven pass drops them.

The dashboard-mockup sample content in `SystemBlueprintSection` — sample goal
titles, habit names, metric labels — is user-facing and translatable, per the
scope call recorded in `<interfaces>`.

After moving both strings out of `src/data/systemBlueprintData.ts`, delete the
module — the verified file is 5 lines and holds nothing but those two strings, so
nothing structural survives. A data file exporting an empty shell is worse than
no data file. Update the importer accordingly. If, contrary to the verified read,
non-string data is present, keep the module and strip only the strings, and say
so in the summary.

Replace literals with `getTranslations('blueprint')`. Both components stay
Server Components. Swap `MethodologyPreviewSection`'s `/features` Link import to
`@/i18n/navigation`.

Change no class name, no clamp, no `leading-*` value, no `truncate`, no grid
definition. This task is a string move.

Capture the rendered text of `/` before and after and diff it — English must be
byte-identical, typographic apostrophes included.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && node -e "const fs=require('fs');const en=require('./src/messages/en/blueprint.json');const blob=JSON.stringify(en);for(const t of ['The Lab','BEHAVIORAL SYSTEM','Read the full methodology','Identity drives behavior']){if(!blob.includes(t)){console.error('expected string not lifted into the catalogue:',t);process.exit(1)}}const b=fs.readFileSync('src/components/SystemBlueprintSection.tsx','utf8');if(!/getTranslations/.test(b)){console.error('SystemBlueprintSection does not use getTranslations');process.exit(1)}if(/use client/.test(b)){console.error('SystemBlueprintSection became a Client Component');process.exit(1)}const m=fs.readFileSync('src/components/MethodologyPreviewSection.tsx','utf8');if(/use client/.test(m)){console.error('MethodologyPreviewSection became a Client Component');process.exit(1)}console.log('BLUEPRINT_EXTRACTION_OK')" && npm run build && npx playwright test containment.spec.ts a11y.spec.ts -g "overflow: /$"</automated>
  </verify>
  <acceptance_criteria>
    - Every site in the VERIFIED STRUCTURE list is accounted for in `src/messages/en/blueprint.json`, including the eyebrow at line 59 and the CTA text at line 113
    - A grep for JSX text nodes and `title=`/`description=`/`label=` string literals across both components returns no remaining English copy
    - `PRINCIPLES` is a catalogue array of 3; presentational sequence markers stayed in code
    - `systemBlueprintData.ts` is deleted and its importer updated, or the summary explains what non-string data forced it to survive
    - `getTranslations` used; both components remain Server Components
    - `MethodologyPreviewSection`'s `/features` Link imports from `@/i18n/navigation`; Gate 3 passes
    - `src/components/DashboardPreview.tsx` is unmodified
    - Rendered English text of `/` diffs clean against the pre-extraction capture
    - No class, clamp, `leading-*`, `truncate` or grid value changed
  </acceptance_criteria>
  <done>Both lower-home sections read English copy from the catalogue with no visual change.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Spanish blueprint copy and check the truncating mockup</name>
  <read_first>src/messages/en/blueprint.json, .planning/codebase/design/SPANISH-VOICE.md (including the ACCENTED DISPLAY TYPE section 07-05 filled), src/messages/GLOSSARY.md, .planning/phases/07-spanish-localization/07-UI-SPEC.md (Layout-Sensitive Components Checklist)</read_first>
  <files>src/messages/es/blueprint.json, src/components/SystemBlueprintSection.tsx, src/components/MethodologyPreviewSection.tsx</files>
  <action>
Write the Spanish mirror into `src/messages/es/blueprint.json`, key-identical to
the English file with matching array lengths and placeholder sets. The moment
this file stops being `{}`, plan 07-03's Gate 2 enforces the namespace fully — a
half-finished file fails the build, which is intended. Key identity runs in both
directions, so an orphaned `es` key fails too.

`Trends` STAYS `Trends` in the Spanish catalogue. It is a `title=` prop like
every other translatable string on this surface, which is exactly why it gets
called out here: the glossary keeps it English, its sibling `description` does
translate, and translating the title while translating the description is the
mistake that reads as correct in review.

Apply `SPANISH-VOICE.md` throughout: no written subject pronoun, `tu`
conjugation for prose addressing the reader, infinitive for the section CTA, no
`usted` or formal `su`, no exclamation marks, none of the 16 anti-words. The
three methodology principle bodies are second-person prose and are the closest
thing on the home page to `/manifesto`'s register — hold them to the same
standard the manifesto set in plan 07-05.

Resolve product nouns through `src/messages/GLOSSARY.md`. `Goals`, `Habits` and
`Score` translate; `Dashboard` and `Trends` do not, despite having obvious
Spanish equivalents. Look up rather than infer.

Then inspect the dashboard mockup at mobile widths. Sample goal titles render
with `truncate` (className at line 186), so a too-long Spanish title ellipsises
rather than overflowing — the containment harness will stay green while the
mockup looks broken. Read the rendered mockup at 320 and 390 and shorten any
Spanish sample title that ellipsises mid-word. This is a copy edit; do not widen
the column, remove `truncate`, or add a locale-specific style.

Run the containment sweep for `/es` at all four mobile widths for everything
that is not truncating. Longer Spanish reflowing to more lines is fine; text
wider than its own box is not, and the fix is shorter copy rather than a smaller
clamp.
  </action>
  <verify>
    <automated>npm run i18n:gates && node -e "const es=require('./src/messages/es/blueprint.json');const blob=JSON.stringify(es);if(!blob.includes('Trends')){console.error('Trends was translated — the glossary keeps it English');process.exit(1)}if(/Tendencias/.test(blob)){console.error('Tendencias found — Trends must stay English per D-01');process.exit(1)}console.log('KEPT_ENGLISH_OK')" && npm run build && npx playwright test containment.spec.ts a11y.spec.ts -g "/es$"</automated>
  </verify>
  <acceptance_criteria>
    - `blueprint` en/es key-identical, equal array lengths, equal placeholder sets, no orphaned `es` key
    - `npm run i18n:gates` passes with `blueprint` fully enforced by Gate 2 (no warning remains for this namespace)
    - The Spanish catalogue contains `Trends` verbatim and contains no `Tendencias`; the sibling `description` IS translated
    - Containment passes on `/es` at 320, 360, 390 and 430 with no new suppression
    - No Spanish sample goal title ellipsises mid-word at 320 or 390
    - `Dashboard` remains English; `Objetivos`, `Hábitos`, `Puntuación` match the glossary
    - Any `.display` heading complies with SPANISH-VOICE.md's `ACCENTED DISPLAY TYPE` verdict
    - No clamp, `leading-*`, column width or `truncate` changed
    - No file under `.github/workflows/` or `scripts/` was modified
  </acceptance_criteria>
  <done>The Spanish lower-home sections read correctly, the kept-English term survived a page full of translated props, and the dashboard mockup still reads as a mockup rather than as a truncation accident.</done>
</task>


<task type="auto">
  <name>Task 3: Wordplay check — record what did not survive into Spanish</name>
  <read_first>.planning/codebase/design/SPANISH-VOICE.md (WORDPLAY ESCALATION), .planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md (read-only: header, five-column row template and the Example section)</read_first>
  <files>.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.07-07.md</files>
  <action>
This task exists because I18N-07 requires it and because the loss it catches is
invisible by construction: a line carried by a pun, a rhythm, a double meaning or
a term of art gets rendered as a flat accurate sentence, the catalogue looks
complete, and nobody ever learns the line died. The catalogue cannot show you
that. Only this pass can.

Go back over the blueprint namespace strings this plan translated (SystemBlueprintSection, MethodologyPreviewSection) — the Spanish you just wrote, not the English you started
from — and check each string against SPANISH-VOICE.md's four wordplay triggers:

  1. the English turns on a pun, an idiom or a double meaning;
  2. the effect is carried by rhythm or sound that the Spanish loses;
  3. the term has no Spanish equivalent GLOSSARY.md resolves and no
     marketing-only coinage feels right;
  4. the Spanish that preserves the meaning breaks the length budget the
     display clamp allows, so a weaker phrasing shipped instead.

For every string where one fires, append a row to THIS PLAN'S OWN register file,
`.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.07-07.md`: key,
English verbatim, the Spanish actually shipped, one concrete sentence on what was
lost, and which trigger fired.

ONE FILE PER PLAN, and never the shared one. Waves 3 and 4 run several plans in
parallel, so a single shared markdown table is either a git conflict or a
silently lost append across isolated worktrees — "append-only" prevents semantic
deletion and does nothing about either of those. Create
`TRANSLATION-FLAGS.07-07.md` even when this plan produces zero rows, copying the
five-column header verbatim from `TRANSLATION-FLAGS.md`, and write only rows this
plan produced. Do NOT write to `TRANSLATION-FLAGS.md` itself; plan 07-16
aggregates every per-plan file into it after the last translation wave lands.

Two rules that decide what a row means:

  - The Spanish shipped stays real copy. A row is a request for the design owner
    to re-write a line by hand, NOT a licence to leave the key empty, a
    placeholder, or the English string. The site has to read correctly whether or
    not that rewrite ever happens, and the parity gates fail on a blank anyway.
  - "Sounds worse" is not a row. Spanish here is a re-write in the brand voice,
    so departing from the English is expected and good. A row records a specific
    effect that died, named concretely enough that someone can decide whether to
    fix it.

Zero rows is a legitimate outcome and is a claim you are making. Do not
manufacture rows to look thorough — a padded register is worse than an empty one,
because it buries the lines that actually need attention.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const dir='.planning/phases/07-spanish-localization/';const own=dir+'TRANSLATION-FLAGS.07-07.md';if(!fs.existsSync(dir+'TRANSLATION-FLAGS.md')){console.error('shared register missing - plan 07-02 did not run');process.exit(1)}if(!fs.existsSync(own)){console.error('missing per-plan register '+own+' - create it even when zero rows apply');process.exit(1)}const s=fs.readFileSync(own,'utf8');if(!s.includes('| Key | English | Spanish shipped | What was lost | Trigger |')){console.error('per-plan register missing the five-column header copied from TRANSLATION-FLAGS.md');process.exit(1)}const rows=s.split('\n').map(l=>l.trim()).filter(l=>l.startsWith('|')&&!l.startsWith('| Key |')&&!/^\|\s*-/.test(l));console.log('FLAGS_OWN_FILE_OK rows='+rows.length)"</automated>
  </verify>
  <acceptance_criteria>
    - Every Spanish string this plan wrote was checked against all four triggers, not sampled
    - Every row appended names a concrete lost effect, not a preference
    - No row leaves the shipped Spanish blank, placeholder, or English
    - `.planning/phases/07-spanish-localization/TRANSLATION-FLAGS.07-07.md` exists, carries the five-column header verbatim, and contains only rows this plan produced — zero rows is valid and the file is still created
    - The shared `TRANSLATION-FLAGS.md` is byte-unchanged by this plan (`git diff --stat -- .planning/phases/07-spanish-localization/TRANSLATION-FLAGS.md` is empty)
    - Any marketing-only term this plan coined is recorded in `src/messages/glossary-additions/07-07.md`; `src/messages/GLOSSARY.md` is byte-unchanged by this plan
    - The count of rows appended by this plan is stated in the summary as `TRANSLATION FLAGS: n`, including when n is 0
  </acceptance_criteria>
  <done>Any line whose English effect did not survive into Spanish is on the design owner's rewrite queue, and a zero count is an answer rather than a silence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalogue -> rendered home page | translated values become home-page content below the fold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Tampering | `blueprint` catalogue values | mitigate | Rendered as React text nodes via `getTranslations`; no `dangerouslySetInnerHTML` introduced |
| T-07-07-02 | Information disclosure | dashboard mockup sample data | mitigate | Sample goals, habits and metrics are invented illustrative content; the translation must not introduce anything resembling real user data, and no value is sourced from a live system |
| T-07-07-03 | Repudiation | deleting `systemBlueprintData.ts` | accept | Module deletion is recorded in the summary and recoverable from git history; the verified file exports no runtime behaviour beyond two strings |
| T-07-07-04 | Repudiation | a kept-English product noun silently translated | mitigate | Task 2's gate asserts `Trends` present and `Tendencias` absent in the Spanish catalogue, so the one glossary exception on this surface cannot be lost to a mechanical pass |
| T-07-07-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
1. `npx tsc --noEmit`, `npm run lint`, `npm run i18n:gates` pass.
2. `npm run build` succeeds; `/` and `/es` both prerendered.
3. Containment and a11y pass on `/` and `/es` at all four mobile widths.
4. Rendered English text of `/` diffs clean against the pre-extraction capture.
5. The kept-English gate reports `KEPT_ENGLISH_OK`.
6. Visual read of the Spanish dashboard mockup at 320 and 390 shows no mid-word
   ellipsis.
</verification>

<success_criteria>
- Both lower-home sections fully catalogue-driven in both locales
- English rendering unchanged
- Spanish contains itself at every mobile width with no clamp edited
- `Trends` survives the translation pass as English
- Mockup sample content reads naturally in Spanish without truncation artefacts
</success_criteria>

<output>
Create `.planning/phases/07-spanish-localization/07-07-SUMMARY.md` when done.
Record: whether `systemBlueprintData.ts` was deleted or kept and why, the
recorded scope call on translating mockup sample data, any sample title shortened
to avoid mid-word ellipsis, and any term appended to the glossary's
marketing-only section.

End the summary with the line `TRANSLATION FLAGS: n`, where n is the number of
rows appended to TRANSLATION-FLAGS.07-07.md. Zero is a valid answer and must still be
written; plan 07-16 fails on a missing line, never on a low count.
</output>
