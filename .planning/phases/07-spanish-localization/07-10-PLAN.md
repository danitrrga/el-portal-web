---
phase: 07-spanish-localization
plan: 10
type: execute
wave: 3
depends_on: ["07-03", "07-04", "07-05"]
files_modified:
  - src/app/[locale]/privacy/page.tsx
  - src/app/[locale]/terms/page.tsx
  - src/messages/en/legal.json
  - src/messages/es/legal.json
autonomous: true
requirements: [I18N-01, I18N-07]

must_haves:
  truths:
    - "/es/privacy and /es/terms render fully in Spanish with content parity to the English pages"
    - "Both pages carry a visible notice that the English version governs in case of discrepancy"
    - "The notice appears in BOTH locales, not only the Spanish one"
    - "The tu register holds in legal copy too — no usted anywhere"
    - "KU-3's known English paragraph overflow is not worse in Spanish, and no NEW offender is introduced"
    - "/privacy and /terms render byte-identical English legal copy to before extraction"
  artifacts:
    - path: "src/messages/en/legal.json"
      provides: "Privacy and Terms English copy plus the authority notice"
      contains: "authority"
    - path: "src/messages/es/legal.json"
      provides: "Spanish mirror of the legal namespace"
  key_links:
    - from: "src/app/[locale]/privacy/page.tsx"
      to: "src/messages/en/legal.json"
      via: "getTranslations('legal') in a Server Component"
      pattern: "getTranslations"
    - from: "src/app/[locale]/terms/page.tsx"
      to: "src/messages/en/legal.json"
      via: "shared legal namespace, one key group per page"
      pattern: "getTranslations"
---

<objective>
Translate `/privacy` and `/terms` (465 lines combined) and add the
English-governs notice both pages need.

Purpose: D-07 is locked — translate both, and state visibly that English is
authoritative in case of discrepancy. That is standard practice, it keeps content
parity, and it removes the meaning risk that made legal translation an open
question in research §6. Spanish legal copy is for comprehension; English
governs.

D-08 flags both pages for human read-through before merge. With
`human_verify_mode: end-of-phase` that read-through is collected as a
`<human-check>` rather than as a blocking checkpoint, and it is listed here so
the end-of-phase verification cannot lose it.

The register applies here too. `07-RESEARCH.md` §7 rule 6 is explicit: never
`usted`, "not as a fallback, not for legal copy. One register across the whole
site." Legal Spanish drifts formal by default, so this is the surface where the
CI register gate earns its keep.

Output: a `legal` namespace in both locales, with the authority notice rendered
on both pages in both languages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-spanish-localization/07-CONTEXT.md
@.planning/phases/07-spanish-localization/07-RESEARCH.md
@.planning/phases/07-spanish-localization/07-UI-SPEC.md
@.planning/codebase/design/SPANISH-VOICE.md
@.planning/phases/05-mobile-responsive-retrofit/deferred-items.md
@CLAUDE.md

@src/app/[locale]/privacy/page.tsx
@src/app/[locale]/terms/page.tsx
@src/components/ReadingLayout.tsx

<interfaces>
Both pages are Server Components -> `getTranslations('legal')`.
Their `export const metadata` literals were moved into `src/lib/seo.ts` by plan
07-01; do not reintroduce them here.

Both use `ReadingLayout`, which carries no user-facing strings — do not modify
it. Phase 5 fixed a 420px glow overflow there with `overflow-x-clip` (never
`overflow-x-hidden`, a phase-level lock); leave that alone.

Authority notice placement (07-UI-SPEC Copywriting Contract, locked):
  directly under the H1, before the first body paragraph
  styled as existing meta text: `text-sm`, `--color-ep-fg-muted`
  NOT a banner, NOT a coloured callout box, no new visual chrome
  it is a sentence, not an alert

Known-unfixed carried in from Phase 5 (containment.spec.ts):
  KU-3  /privacy  sweep A  selector p.text-
        five legal-copy paragraphs overflow their box by up to 73px at 320px,
        33px at 360px and 3px at 390px.
  Plan 07-03 already added the mirrored `/es/privacy` suppression under the same
  ref. 07-UI-SPEC notes Spanish legal prose is very likely longer, and that if
  the numbers grow materially it is signal the underlying paragraph width or
  line-height needs the fix Phase 5 deferred.

Gate 2 behaviour (plan 07-03): translation status is INFERRED from the catalogue,
not configured anywhere. A namespace whose `es` file is exactly `{}` warns; the
moment it has any Spanish key it is fully enforced. There is no allowlist to
edit, and this plan must not touch `.github/workflows/` or `scripts/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract both legal pages into the legal namespace and add the authority notice</name>
  <read_first>src/app/[locale]/privacy/page.tsx, src/app/[locale]/terms/page.tsx, .planning/phases/07-spanish-localization/07-UI-SPEC.md (Legal pages authority notice)</read_first>
  <files>src/app/[locale]/privacy/page.tsx, src/app/[locale]/terms/page.tsx, src/messages/en/legal.json</files>
  <action>
Move every user-facing string on both pages into `src/messages/en/legal.json`,
namespaced by page — `privacy.*` and `terms.*` — plus a shared `authority.*`
key for the governing-language notice.

Preserve the legal copy verbatim. Phase 3's constraint that original legal text
is preserved word for word still binds: this is an extraction, not an edit, and
a "clearer" rewrite of a clause during extraction is a defect. Move the strings
character for character, typographic apostrophes and all.

Privacy's structured sections (`Bucket`, `ProviderRow`, `RightRow` and their
props: tag, title, lede, stored, neverStored, meta, provider name, region,
purpose) are user-facing and move to the catalogue as arrays, keeping the
components and their class names untouched. Provider names are proper nouns and
stay as they are; their `purpose` and `region` descriptions are prose and move.

Add the authority notice to BOTH pages, sourced from `authority.notice`. It
renders in both locales, not just Spanish — an English reader should see the same
statement, and a notice that appears only on the translated page reads as an
apology rather than as a term. Place it directly under the H1 and before the
first body paragraph, styled with `text-sm` and `--color-ep-fg-muted` to match
existing meta text. Do not build a banner, a callout box, a coloured background,
a border, or an icon. It is a sentence.

Replace literals with `getTranslations('legal')`. Both pages stay Server
Components. Swap internal `Link` imports to `@/i18n/navigation`. Do not modify
`ReadingLayout.tsx`.

Capture the rendered text of `/privacy` and `/terms` before and after and diff
them. Apart from the newly added notice, English must be byte-identical.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npx playwright test containment.spec.ts a11y.spec.ts -g "/privacy|/terms"</automated>
  </verify>
  <acceptance_criteria>
    - Zero hardcoded user-facing strings remain in either legal page
    - Legal copy moved character for character; no clause reworded
    - Privacy's Bucket / ProviderRow / RightRow content stored as catalogue arrays
    - The authority notice renders on both pages in both locales, under the H1, as `text-sm` muted meta text with no banner chrome
    - `getTranslations` used; both pages remain Server Components
    - `ReadingLayout.tsx` unmodified
    - Rendered English text diffs clean apart from the added notice
  </acceptance_criteria>
  <done>Both legal pages are catalogue-driven in English and carry the governing-language notice.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Spanish legal copy and re-measure KU-3</name>
  <read_first>src/messages/en/legal.json, .planning/codebase/design/SPANISH-VOICE.md, src/messages/GLOSSARY.md, .planning/phases/05-mobile-responsive-retrofit/deferred-items.md</read_first>
  <files>src/messages/es/legal.json</files>
  <action>
Write the Spanish mirror into `src/messages/es/legal.json`, key-identical to the
English file with matching array lengths and placeholder sets. The moment this
file stops being `{}`, plan 07-03's Gate 2 enforces the namespace fully — a
half-finished file fails the build, which is intended.

Content parity is the requirement: every clause present in English must be
present in Spanish. A shortened Spanish privacy policy is a legal problem, not a
copy decision.

Apply `SPANISH-VOICE.md` without a legal exemption. Rule 6 is explicit — never
`usted`, never formal `su`, not as a fallback and not for legal copy, because one
register across the whole site beats a correct-sounding inconsistency. Legal
Spanish drifts formal by default, so write deliberately: `tu` conjugation when
addressing the reader, no written subject pronoun, infinitive for any action
label, no exclamation marks.

Translate the authority notice into Spanish as a plain, unambiguous sentence
stating that the English version governs in case of discrepancy. This is the one
string on the site where reading clearly matters more than reading elegantly.

Do not translate proper nouns: provider names, product names, and the legal
entity name stay as they are. Hosting regions stay factually identical — Phase 3
reconciled those claims against the real app, and a translation must not
introduce a new claim. If a claim reads ambiguously in Spanish, flag it in the
summary rather than resolving it by rewording.

Then re-measure KU-3 rather than assuming it carries over. Run the containment
sweep for `/es/privacy` and `/es/terms` at 320, 360, 390 and 430 and compare the
reported `overflowBy` numbers against Phase 5's English baseline (up to 73px at
320, 33px at 360, 3px at 390).

Three outcomes, three responses:
  - Comparable to English: the mirrored KU-3 suppression stands; record the
    numbers.
  - Materially worse in Spanish: 07-UI-SPEC says that is signal the underlying
    paragraph width or line-height needs the fix Phase 5 deferred. That fix would
    change English's approved rendering, so it is a design-owner decision — log
    it in `deferred-items.md` with the measured numbers and do not apply it here.
    Do not widen the suppression to cover the larger overflow either.
  - A NEW offender with no English counterpart: fix it at source; it does not
    inherit KU status.
  </action>
  <verify>
    <automated>npm run i18n:gates && npm run build && npx playwright test containment.spec.ts a11y.spec.ts -g "/es/privacy|/es/terms"</automated>
    <human-check>
      D-08 read-through, collected at end of phase.
      Read `/es/privacy` and `/es/terms` in full on the running build. Confirm:
      (1) every clause present in English is present in Spanish;
      (2) no `usted` and no formal `su` anywhere;
      (3) the governing-language notice reads unambiguously in Spanish and
          appears on both pages in both locales;
      (4) no hosting-region, provider or data-handling claim differs in meaning
          from the English text.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `legal` en/es key-identical, equal array lengths, equal placeholder sets — content parity, no omitted clause
    - `npm run i18n:gates` passes with `legal` fully enforced by Gate 2 (no warning remains for this namespace)
    - No `usted` or formal `su` in the Spanish legal copy
    - Authority notice translated and rendering on both Spanish pages
    - Proper nouns, provider names and hosting regions unchanged in meaning
    - KU-3 numbers measured on `/es/privacy` at all four widths and recorded
    - No suppression widened; any worsening logged in `deferred-items.md`
    - No file under `.github/workflows/` or `scripts/` was modified
    - All four legal routes prerendered
  </acceptance_criteria>
  <done>Both legal pages read in Spanish with full content parity, English governs visibly, and KU-3 is measured rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalogue -> rendered legal terms | translated values state binding privacy and terms commitments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10-01 | Repudiation | divergent legal meaning between locales | mitigate | The English-governs notice renders on both pages in both locales, and the parity gate enforces key-identical clause arrays so no clause can exist in one locale only |
| T-07-10-02 | Information disclosure | data-handling and hosting-region claims | mitigate | Provider names, regions and claims are carried across unchanged; the plan forbids resolving an ambiguous claim by rewording and requires flagging it instead. Phase 3 already reconciled these against the real app |
| T-07-10-03 | Tampering | `legal` catalogue values | mitigate | Rendered as React text nodes via `getTranslations`; no `dangerouslySetInnerHTML` introduced |
| T-07-10-04 | Repudiation | silent clause loss during extraction | mitigate | Rendered-text diff against a pre-extraction capture, plus array-length parity in the gate |
| T-07-10-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
1. `npx tsc --noEmit`, `npm run lint`, `npm run i18n:gates` pass.
2. `npm run build` succeeds; all four legal routes prerendered.
3. Containment and a11y pass on `/privacy`, `/terms`, `/es/privacy`, `/es/terms`.
4. Rendered English text diffs clean apart from the added authority notice.
5. Human read-through of both Spanish legal pages (D-08), collected at end of
   phase.
</verification>

<success_criteria>
- Both legal pages fully catalogue-driven in both locales with full content parity
- English-governs notice visible on both pages in both languages
- One register across the site, legal copy included — no `usted`
- KU-3 re-measured with numbers recorded and no suppression widened
</success_criteria>

<output>
Create `.planning/phases/07-spanish-localization/07-10-SUMMARY.md` when done.
Record: the `/es/privacy` KU-3 numbers against Phase 5's baseline, any claim
flagged as ambiguous in Spanish, and the exact wording of the authority notice in
both locales.
</output>
