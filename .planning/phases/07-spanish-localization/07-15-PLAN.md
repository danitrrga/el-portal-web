---
phase: 07-spanish-localization
plan: 15
type: execute
wave: 5
depends_on: ["07-06", "07-07", "07-08", "07-09", "07-10", "07-11", "07-12", "07-13", "07-14"]
files_modified:
  - src/lib/seo.ts
  - src/messages/en/metadata.json
  - src/messages/es/metadata.json
  - src/app/sitemap.ts
  - src/app/[locale]/layout.tsx
  - .github/workflows/responsive-audit.yml
autonomous: true
requirements: [I18N-05]

must_haves:
  truths:
    - "The site origin every canonical URL is built from is CONFIRMED from the environment before a single URL is emitted, not assumed from a plan"
    - "A build with no NEXT_PUBLIC_SITE_URL fails loudly with a named error instead of silently emitting canonicals, hreflang alternates and a sitemap against an empty origin"
    - "CI supplies the origin through the repository's own variable mechanism, so a clean checkout builds correctly without a .env.local that is not in the repository"
    - "Spanish metadata is written against FINAL page copy — every copy plan including features (07-13) and changelog (07-14) has landed first"
    - "Every one of the 16 pages emits a per-locale title and meta description — no Spanish page inherits English metadata"
    - "Every page emits hreflang alternates for en and es plus x-default"
    - "sitemap.xml lists all 16 URLs with their locale alternates"
    - "Open Graph and Twitter tags are per-locale, with og:locale set correctly"
    - "All 16 routes still prerender after metadata generation is added"
  artifacts:
    - path: "src/lib/seo.ts"
      provides: "Per-locale metadata with hreflang alternates for every route"
      exports: ["RouteKey", "ROUTE_PATHS", "buildPageMetadata"]
    - path: "src/app/sitemap.ts"
      provides: "Both-locale sitemap with alternates"
      exports: ["default"]
    - path: ".github/workflows/responsive-audit.yml"
      provides: "NEXT_PUBLIC_SITE_URL supplied to the jobs that build, so CI is not the one environment where the origin is undefined"
      contains: "NEXT_PUBLIC_SITE_URL"
    - path: "src/messages/en/metadata.json"
      provides: "Per-route English titles, descriptions and OG copy"
      contains: "features"
    - path: "src/messages/es/metadata.json"
      provides: "Spanish mirror of the metadata namespace"
  key_links:
    - from: "src/app/[locale]/*/page.tsx"
      to: "src/lib/seo.ts"
      via: "generateMetadata wired by plan 07-01, unchanged here"
      pattern: "buildPageMetadata"
    - from: "src/lib/seo.ts"
      to: "src/messages/en/metadata.json"
      via: "getTranslations('metadata') keyed by RouteKey"
      pattern: "metadata"
    - from: "src/app/sitemap.ts"
      to: "src/lib/seo.ts"
      via: "shared ROUTE_PATHS so the sitemap cannot drift from the route table"
      pattern: "ROUTE_PATHS"
---

<objective>
Emit the SEO signals that make both language versions findable: per-locale
titles and descriptions on all 16 pages, `hreflang` alternates including
`x-default`, per-locale Open Graph and Twitter tags, and a sitemap covering both
locales.

Purpose: this is the requirement the whole detection decision was made to
protect. `07-RESEARCH.md` §1 rejected blanket redirection because Googlebot
crawls from the US and sends no `Accept-Language`, which can leave one locale
permanently unindexed. Explicit signals — distinct URLs, `hreflang`
annotations, and a sitemap listing both — are what replaces that implicit
detection. Without them, `/es/*` exists and is invisible.

Six of the eight routes currently export no metadata at all and inherit a single
site-wide title. That was tolerable for one locale; with two it means a Spanish
page rendering Spanish content under an English title, which is exactly what
success criterion 5 forbids.

This plan is sequenced after every copy plan so descriptions are written against
the page copy that actually shipped, not against a draft.

Every artifact this plan emits — canonicals, alternates, OG urls, the sitemap —
is an absolute URL built from one environment variable. If that variable is
wrong, all four are wrong sitewide and Google is the one that notices. Task 1
therefore starts by confirming it rather than trusting the value written into
this plan.

Output: catalogue-driven metadata for all 16 pages, a both-locale sitemap, and
no route dropped from the static build.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-spanish-localization/07-CONTEXT.md
@.planning/phases/07-spanish-localization/07-RESEARCH.md
@.planning/codebase/design/SPANISH-VOICE.md
@src/messages/GLOSSARY.md
@CLAUDE.md

@src/lib/seo.ts
@src/app/[locale]/layout.tsx
@src/i18n/routing.ts

<interfaces>
Contract fixed by plan 07-01 — this plan fills the body, it does not change the
signature, and it does NOT edit any page file:
  export type RouteKey = 'home' | 'features' | 'manifesto' | 'changelog' | 'mcp' | 'pricing' | 'privacy' | 'terms'
  export const ROUTE_PATHS: Record<RouteKey, string>
  export function buildPageMetadata(locale: string, route: RouteKey): Promise<Metadata>

Every page already exports `generateMetadata` delegating to `buildPageMetadata`.
Do not touch `src/app/[locale]/**/page.tsx` — those files are owned by the copy
plans and their metadata wiring is already correct.

WHY THIS PLAN MOVED TO WAVE 5. It previously depended on 07-06 through 07-12 and
ran in Wave 4 alongside 07-13 (Spanish `/features` copy) and 07-14 (Spanish
`/changelog` copy). Task 1 authors descriptions against final page copy, so two
of the eight routes' copy would still have been moving while their metadata was
being written. 07-13 and 07-14 are now explicit dependencies and this plan runs
after them, which pushes 07-16 to Wave 6. That is the intended trade: one extra
wave against Spanish search snippets written from copy that later changed.

SITE ORIGIN — CONFIRM, DO NOT ASSUME. `.env.local` is mode 600, so the cross-AI
review could not read it and flagged the value as unverifiable. As of 2026-08-02
it reads `NEXT_PUBLIC_SITE_URL = https://el-portal.app`, distinct from
`NEXT_PUBLIC_APP_URL = https://app.el-portal.app`, which is the application and
NOT this site. Task 1's first step re-confirms this at execution time. Use the
site URL for `metadataBase`, canonicals, alternates and the sitemap; never the
app URL. A canonical pointing at the app origin would ask Google to de-index the
marketing site in favour of a page that does not serve marketing content.

URL shape under `localePrefix: 'as-needed'`:
  en  ->  https://el-portal.app/pricing        (no prefix)
  es  ->  https://el-portal.app/es/pricing
  home is `/` and `/es`, not `/` and `/es/`

X-DEFAULT TARGET — state it, do not leave it implied. With `localeDetection:
false` and manual negotiation at `/`, `x-default` points at the ENGLISH,
unprefixed URL for every route, and `/`'s alternate set lists `en`, `es` and
`x-default` with `x-default` on `/`. That is what stops `/es` being read as the
canonical home page for a crawler whose language matches neither.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the site origin, then write per-locale metadata copy and implement buildPageMetadata with hreflang</name>
  <read_first>src/lib/seo.ts, src/app/[locale]/layout.tsx, src/i18n/routing.ts, src/messages/en/home.json, src/messages/en/features.json, .planning/codebase/design/SPANISH-VOICE.md, .github/workflows/responsive-audit.yml</read_first>
  <files>src/messages/en/metadata.json, src/messages/es/metadata.json, src/lib/seo.ts, src/app/[locale]/layout.tsx, .github/workflows/responsive-audit.yml</files>
  <action>
FIRST, CONFIRM THE ORIGIN. Before writing any URL-emitting code, read
`NEXT_PUBLIC_SITE_URL` from the environment (`grep '^NEXT_PUBLIC_SITE_URL'
.env.local`, or the deployment's environment if that file is unavailable) and
assert four things:
  - it is set and non-empty;
  - it parses as an absolute `https:` URL;
  - it is NOT equal to `NEXT_PUBLIC_APP_URL` — two variables that resolve to the
    same origin means one of them is wrong and the canonicals would point at the
    application;
  - it has no trailing slash, or the URL helper below normalises one away.
Echo the confirmed origin (it is a public value, not a secret) and record it in
the summary.

If the variable is absent or is not an https origin, STOP and report the plan as
BLOCKED naming the observed value. Do not fall back to a hardcoded default, do
not guess from `package.json`, and do not emit relative canonicals. Wrong
absolute URLs in `alternates.canonical` and `sitemap.xml` are worse than missing
ones: they instruct a crawler confidently and wrongly, sitewide, and the damage
is invisible in every local check this phase runs.

SECOND, GIVE THE ORIGIN AN ENVIRONMENT CONTRACT — this plan owns it, and without
it the refusal-to-fall-back above turns CI into the broken case. `.env.local` is
not in the repository, and `.github/workflows/responsive-audit.yml` currently
defines no `NEXT_PUBLIC_SITE_URL`. A clean CI checkout therefore builds with an
undefined origin. Three pieces, all required:

  (a) REPOSITORY VARIABLE. Publish the confirmed origin as a GitHub Actions
      repository variable using the CLI — it is a public value, so a variable is
      correct and a secret is not:
        gh variable set NEXT_PUBLIC_SITE_URL --body "<confirmed origin>"
      Then confirm it with `gh variable list` and record the output. If `gh` is
      not authenticated, do not skip this and do not substitute a hardcoded
      value in the workflow — stop, report the auth failure, and resume after
      the user authenticates.

  (b) WORKFLOW WIRING. Add a workflow-level `env:` block to
      `.github/workflows/responsive-audit.yml` setting
      `NEXT_PUBLIC_SITE_URL: ${{ vars.NEXT_PUBLIC_SITE_URL }}`. Workflow level,
      not step level: `playwright.config.ts`'s `webServer` runs
      `npm run build && npm run start` from inside the `responsive` job, so the
      value has to be present for a process this workflow does not name
      directly. Change nothing else in that file — its Playwright browser
      caching, its `--with-deps` install and its concurrency block are Phase 5
      machinery and are not this plan's business.

  (c) BUILD-TIME ASSERTION. In `src/lib/seo.ts`, resolve the origin once through
      a module-level helper that THROWS a named error when
      `process.env.NEXT_PUBLIC_SITE_URL` is missing, empty, or not an absolute
      `https:` URL. Use a distinctive, greppable message —
      `E_MISSING_SITE_ORIGIN: NEXT_PUBLIC_SITE_URL is required to emit canonical
      URLs, hreflang alternates and sitemap.xml` — so the failure names itself in
      a CI log. Throwing is the point: `generateMetadata` and `sitemap.ts` both
      run during `next build`, so an absent origin fails the build instead of
      shipping sixteen pages of canonicals against `undefined/pricing`. Do not
      soften this to a `console.warn`, and do not add a development-only bypass;
      a silent empty origin in dev is how the value goes missing in production.

Prove (c) works rather than assuming it: run one build with the variable
unset (`env -u NEXT_PUBLIC_SITE_URL npm run build`) and confirm it fails with
`E_MISSING_SITE_ORIGIN` in the output, then run the normal build and confirm it
succeeds. Record both outcomes.

Then author `src/messages/en/metadata.json` with one key group per `RouteKey`,
each holding `title`, `description` and any OG-specific copy that should differ
from the page title. Preserve the two metadata objects plan 07-01 moved out of
the privacy and terms pages verbatim as those routes' English values — they are
existing shipped metadata and this is not the place to rewrite them. Author the
six missing routes against the copy those pages actually ship, now that every
copy plan has landed.

Write `src/messages/es/metadata.json` as the key-identical Spanish mirror.
Descriptions are marketing copy and are bound by `SPANISH-VOICE.md` exactly like
any other string: no written subject pronoun, no `usted`, no exclamation marks,
none of the 16 anti-words, product nouns resolved through `GLOSSARY.md`. Search
snippets are where a register slip is most publicly visible.

Keep titles inside the length search engines display and descriptions inside
theirs. Spanish runs 15-25% longer, so a description translated literally from a
tight English one will truncate in results. Write the Spanish description to fit
rather than translating and hoping.

Implement `buildPageMetadata(locale, route)` in `src/lib/seo.ts`:
  - read the route's values with `getTranslations({ locale, namespace: 'metadata' })`,
    passing the locale explicitly rather than relying on request scope, since
    this runs inside `generateMetadata`;
  - set `title` and `description` from the catalogue;
  - set `alternates.canonical` to this locale's own URL for the route;
  - set `alternates.languages` with an entry for `en`, an entry for `es`, and an
    `x-default` entry pointing at the ENGLISH, unprefixed URL for that route —
    including on `/` itself, where the set is `en: /`, `es: /es`,
    `x-default: /`. `x-default` is what tells a crawler which version to show a
    user whose language matches neither, and getting it wrong on `/` is what
    would make `/es` the canonical home page;
  - set `openGraph` with `locale` (`en_US` and `es_ES`), `alternateLocale`, the
    canonical `url`, `title`, `description` and `siteName`;
  - set `twitter` card metadata mirroring the OG values.

Build every URL from the confirmed `NEXT_PUBLIC_SITE_URL` plus
`ROUTE_PATHS[route]`, with the `/es` prefix applied only for the Spanish locale —
that single helper is what keeps `as-needed` prefixing correct in the canonical,
the alternates, the OG url and the sitemap simultaneously. Write it once and use
it everywhere; four hand-built URL strings will disagree within a month.

In `src/app/[locale]/layout.tsx`, add `metadataBase` from the same confirmed
origin so relative OG image paths resolve, and make the root title/description
per-locale as well. Change nothing else in that file — the fonts (including any
`latin-ext` subset plan 07-05 added), the `viewport` export, the body classes and
the provider wiring are Phase 5, 07-01 and 07-05 deliverables.

Do not edit any page file. `generateMetadata` is already wired in all nine of
them.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const raw=fs.existsSync('.env.local')?fs.readFileSync('.env.local','utf8'):'';const pick=n=>{const m=raw.split('\n').find(l=>l.trim().startsWith(n));return m?m.split('=').slice(1).join('=').trim():process.env[n]};const site=pick('NEXT_PUBLIC_SITE_URL'),app=pick('NEXT_PUBLIC_APP_URL');if(!site){console.error('BLOCKED: NEXT_PUBLIC_SITE_URL is not set');process.exit(1)}let u;try{u=new URL(site)}catch(e){console.error('BLOCKED: NEXT_PUBLIC_SITE_URL is not an absolute URL:',site);process.exit(1)}if(u.protocol!=='https:'){console.error('BLOCKED: site origin is not https:',site);process.exit(1)}if(app&&new URL(app).origin===u.origin){console.error('BLOCKED: site and app origins are identical — canonicals would point at the application');process.exit(1)}console.log('SITE_ORIGIN_CONFIRMED',u.origin)" && node -e "const fs=require('fs');const y=fs.readFileSync('.github/workflows/responsive-audit.yml','utf8');if(!/NEXT_PUBLIC_SITE_URL:\s*\$\{\{\s*vars\.NEXT_PUBLIC_SITE_URL\s*\}\}/.test(y)){console.error('workflow does not supply NEXT_PUBLIC_SITE_URL from a repository variable — CI would build against an undefined origin');process.exit(1)}const seo=fs.readFileSync('src/lib/seo.ts','utf8');if(!seo.includes('E_MISSING_SITE_ORIGIN')){console.error('src/lib/seo.ts has no named build-time assertion for a missing site origin');process.exit(1)}if(/NEXT_PUBLIC_SITE_URL\s*\|\|\s*[\x27\x22]/.test(seo)){console.error('src/lib/seo.ts contains a hardcoded origin fallback');process.exit(1)}console.log('SITE_ORIGIN_CONTRACT_OK')" && npx tsc --noEmit && npm run lint && npm run i18n:gates && npm run build && node -e "const{readFileSync}=require('fs');const h=readFileSync('.next/server/app/es/pricing.html','utf8');for(const need of ['hreflang=\"en\"','hreflang=\"es\"','hreflang=\"x-default\"','og:locale']){if(!h.includes(need)){console.error('missing',need);process.exit(1)}}console.log('HREFLANG OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `NEXT_PUBLIC_SITE_URL` was read at execution time, is a non-empty absolute https origin, differs from `NEXT_PUBLIC_APP_URL`, and the confirmed value is echoed and recorded in the summary
    - `gh variable set NEXT_PUBLIC_SITE_URL` was run and `gh variable list` shows it; the output is recorded in the summary
    - `.github/workflows/responsive-audit.yml` carries a workflow-level `env:` block with `NEXT_PUBLIC_SITE_URL: ${{ vars.NEXT_PUBLIC_SITE_URL }}`, and nothing else in that file changed
    - `src/lib/seo.ts` throws `E_MISSING_SITE_ORIGIN` when the variable is missing, empty or not an absolute https URL — proven by running `env -u NEXT_PUBLIC_SITE_URL npm run build` and observing that exact string in the failure output
    - There is no hardcoded origin fallback and no development-only bypass anywhere in `src/lib/seo.ts` or `src/app/sitemap.ts`
    - If it had been absent or non-https, the plan would have reported BLOCKED — no hardcoded fallback origin exists anywhere in `src/lib/seo.ts`
    - `metadata` en/es key-identical with one group per `RouteKey`; Gate 2 enforces it green
    - Privacy and Terms English metadata preserved verbatim from what plan 07-01 moved
    - Every one of the 16 rendered pages carries its own `title` and `description`; none inherits the root default
    - Every page emits `hreflang` for `en`, `es` and `x-default`, with `x-default` pointing at the English unprefixed URL — including on `/`, where the alternates are `en: /`, `es: /es`, `x-default: /`
    - `openGraph.locale` is `en_US` on English pages and `es_ES` on Spanish pages, with `alternateLocale` set
    - All URLs derive from one shared helper over the confirmed origin and `ROUTE_PATHS`
    - No page file was modified; no font, viewport or provider wiring in the layout was changed
    - Spanish descriptions written to fit result-snippet length, not literally translated
  </acceptance_criteria>
  <done>Every page tells a crawler what language it is, what its counterpart is, and which version is the default — from an origin that was verified rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Emit a both-locale sitemap and re-assert the static build</name>
  <read_first>src/lib/seo.ts, src/i18n/routing.ts, e2e/support/pages.ts</read_first>
  <files>src/app/sitemap.ts</files>
  <action>
Create `src/app/sitemap.ts` — at the app root, NOT under `[locale]`; a sitemap is
a single site-wide file and putting it under the locale segment would produce two
competing sitemaps.

Emit one entry per route per locale, 16 in total, each with its absolute URL and
an `alternates.languages` map naming its counterpart in the other locale plus
`x-default`. Next's `MetadataRoute.Sitemap` type supports the alternates field
natively, which is what makes the sitemap agree with the per-page `hreflang`
annotations instead of contradicting them.

Derive both the paths and the URLs from `ROUTE_PATHS` and the same URL helper
Task 1 wrote, over the same confirmed origin. A sitemap with its own hardcoded
route list is a sitemap that will silently miss the next route added — and
`e2e/support/pages.ts`'s `EN_ROUTES` is the other list of the same eight paths,
so cross-check the two agree rather than letting a third copy appear.

Confirm no route was dropped from the static build. Adding `generateMetadata`
bodies that call `getTranslations` is exactly the kind of change that can pull a
page into dynamic rendering — `07-RESEARCH.md` §3.2's warning applies to metadata
generation as much as to page rendering, and it fails silently. Re-run the
prerender-manifest assertion from plan 07-01 and confirm all 16 route paths are
still listed, plus `/sitemap.xml`.

Do not add `robots.ts`. It is not part of I18N-05 and Phase 6 owns response-level
crawler concerns; adding it here would create a second owner for the same
surface.
  </action>
  <verify>
    <automated>npm run build && node -e "const r=Object.keys(require('./.next/prerender-manifest.json').routes);const en=['/','/features','/manifesto','/changelog','/mcp','/pricing','/privacy','/terms'];const want=[...en,...en.map(p=>p==='/'?'/es':'/es'+p)];const missing=want.filter(x=>!r.includes(x));if(missing.length){console.error('NOT PRERENDERED:',missing);process.exit(1)}console.log('prerendered '+want.length+'/'+want.length)" && npm run verify:routing</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/sitemap.ts` exists at the app root, not under `[locale]`
    - The sitemap lists all 16 URLs, each with `alternates.languages` for `en`, `es` and `x-default`
    - Paths and URLs derive from `ROUTE_PATHS` and the shared URL helper, with no second route list; the eight English paths were cross-checked against `EN_ROUTES` in `e2e/support/pages.ts`
    - All 16 route paths still present in `.next/prerender-manifest.json`
    - `npm run verify:routing` still passes — metadata generation did not change routing behaviour
    - No `robots.ts` added
  </acceptance_criteria>
  <done>Both locales are listed, cross-referenced and still statically prerendered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| site config -> emitted absolute URLs | metadata and sitemap publish canonical URLs crawlers follow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15-01 | Spoofing | canonical and alternate URLs | mitigate | All URLs are built from `NEXT_PUBLIC_SITE_URL` plus the static `ROUTE_PATHS` table through one helper; no request-derived host or path reaches a canonical, so a Host-header injection cannot rewrite the site's own canonical URLs |
| T-07-15-02 | Tampering | pointing canonicals at the application origin | mitigate | Task 1's first gate asserts the site origin is set, https, and NOT the same origin as `NEXT_PUBLIC_APP_URL`, and halts the plan otherwise; no hardcoded fallback origin exists in `src/lib/seo.ts` |
| T-07-15-03 | Information disclosure | metadata catalogue content | accept | Titles and descriptions are public marketing copy by definition; no internal path, staging host or configuration value is emitted. The site origin is itself a public value, so echoing it in the summary discloses nothing |
| T-07-15-04 | Denial of Service | metadata generation forcing dynamic rendering | mitigate | Prerender-manifest assertion re-run after the change; a route silently becoming dynamic would show up as a missing entry rather than as a passing build |
| T-07-15-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
1. The origin gate prints `SITE_ORIGIN_CONFIRMED` with the value used, and
   `SITE_ORIGIN_CONTRACT_OK` confirming the workflow variable wiring and the
   `E_MISSING_SITE_ORIGIN` assertion are both in place.
1b. `env -u NEXT_PUBLIC_SITE_URL npm run build` fails with `E_MISSING_SITE_ORIGIN`;
   the normal build succeeds.
2. `npx tsc --noEmit`, `npm run lint`, `npm run i18n:gates` pass.
3. `npm run build` succeeds; all 16 route paths in `.next/prerender-manifest.json`.
4. Rendered HTML for a sample of English and Spanish pages contains `hreflang`
   for `en`, `es` and `x-default`, and the correct `og:locale`; `/`'s `x-default`
   points at `/`, not `/es`.
5. `/sitemap.xml` lists 16 URLs with alternates.
6. `npm run verify:routing` still passes.
</verification>

<success_criteria>
- The site origin is confirmed at execution time and no fallback origin exists in code
- No page inherits English metadata while rendering Spanish
- hreflang plus x-default on every page, agreeing with the sitemap, with x-default on the English URL
- Sitemap covers both locales from a single route table
- Static prerendering intact for all 16 routes
</success_criteria>

<output>
Create `.planning/phases/07-spanish-localization/07-15-SUMMARY.md` when done.
Record: the confirmed site origin and how it was read, the URL helper's shape,
the `x-default` target chosen for `/` and for a prefixed route, and confirmation
that the prerender count was unchanged before and after metadata generation.
</output>
