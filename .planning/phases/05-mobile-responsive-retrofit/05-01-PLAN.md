---
phase: 05-mobile-responsive-retrofit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/components/MotionProvider.tsx
  - src/app/globals.css
autonomous: true
requirements: [RESP-04, RESP-05, RESP-06, RESP-07, RESP-08]

must_haves:
  truths:
    - "Mobile browser chrome renders dark (#02030a) instead of light against the near-black page"
    - "A visitor can pinch-zoom every page (no zoom-blocking viewport properties exist)"
    - "Framer Motion transform/layout animations stop when the OS reduced-motion preference is set"
    - "Tapping a card or the footer wordmark on a touch device does not leave its hover state latched on"
    - "Full-height sections resolve against the small viewport, so iOS Safari's address bar does not clip them"
    - "Desktop rendering at 1440px is unchanged"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "viewport export with themeColor + colorScheme, MotionProvider wiring, no Google Fonts CDN link"
      contains: "export const viewport"
    - path: "src/components/MotionProvider.tsx"
      provides: "Client MotionConfig reducedMotion=user wrapper"
      exports: ["MotionProvider"]
    - path: "src/app/globals.css"
      provides: "hover-gated hand-written rules, svh cascade on body, min-h-viewport utility"
      contains: "@utility min-h-viewport"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/MotionProvider.tsx"
      via: "import + wrapping {children} inside <body>"
      pattern: "MotionProvider"
    - from: "src/app/globals.css"
      to: "src/app/*/page.tsx"
      via: "min-h-viewport utility consumed by the eight page roots in plan 05-04"
      pattern: "@utility min-h-viewport"
---

<objective>
Establish the mobile contract at the app shell: the `viewport` export, a root reduced-motion
config, hover gating for hand-written CSS, the `svh` cascade, and the `min-h-viewport` utility
that plan 05-04 applies to the eight page roots.

Purpose: RESP-04/05/06/07 are pure app-shell concerns. Every one of them is inert at >=768px,
so they carry near-zero desktop risk and they unblock the page-level work.

Output: `src/app/layout.tsx` (viewport export + MotionProvider wiring + dead CDN link removed),
`src/components/MotionProvider.tsx` (new), `src/app/globals.css` (hover gating, svh cascade,
`min-h-viewport` utility).

**This plan does NOT remove `overflow-x-hidden` from `<body>`.** That removal is plan 05-05,
after the overflow sources it masks are fixed in plans 05-03 and 05-04. Removing it here would
leave the live site visibly broken between plans.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-responsive-retrofit/05-CONTEXT.md
@.planning/phases/05-mobile-responsive-retrofit/05-RESEARCH.md
@.planning/codebase/design/RESPONSIVE.md
@.planning/responsive/SURFACES.md
@CLAUDE.md
</context>

<interfaces>
Contracts the executor needs. Do not go hunting for these in the codebase.

Next.js viewport type (Next 16.1.6):
`import type { Viewport } from "next"` — fields used here: `width`, `initialScale`,
`themeColor`, `colorScheme`. Exported from a layout as `export const viewport: Viewport`.
`maximumScale` and `userScalable` also exist and are BANNED here (WCAG 1.4.4).

Framer Motion (framer-motion 12.34.3):
`import { MotionConfig } from "framer-motion"` — `<MotionConfig reducedMotion="user">` takes
`children`. It is client-only, so it must live in a `"use client"` module. RSC `children`
passed through it as a prop stay server-rendered.

Tailwind v4.2.1 custom utility syntax (core, no plugin required):
`@utility <name> { <declarations> }` at the top level of `globals.css`. Repeated declarations
of the same property are emitted in source order, which is exactly the cascade fallback needed.

Current `<body>` className in `src/app/layout.tsx:50` (font variables elided):
`... bg-[#02030a] text-slate-300 font-sans antialiased selection:bg-primary/30 selection:text-white overflow-x-hidden`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the viewport export and delete the unused Google Fonts CDN link</name>
  <read_first>
    - `src/app/layout.tsx` (all 56 lines)
    - `.planning/codebase/design/RESPONSIVE.md` — the "But DO add the viewport export" block and the safe-area decision
    - `.planning/phases/05-mobile-responsive-retrofit/05-CONTEXT.md` — "Locked — technical"
  </read_first>
  <files>src/app/layout.tsx</files>
  <action>
    Add `import type { Viewport } from "next"` alongside the existing `Metadata` type import on
    line 1, and export a `viewport` object immediately after the `metadata` export (after line 36)
    with exactly four fields: `width: "device-width"`, `initialScale: 1`,
    `themeColor: "#02030a"` (the value of `--color-ep-bg-base`), and `colorScheme: "dark"`.

    Do NOT add `maximumScale`, `userScalable`, or `viewportFit`. `maximumScale: 1` and
    `userScalable: false` block pinch-zoom (WCAG 1.4.4 failure). `viewportFit: "cover"` is
    rejected by 05-CONTEXT.md: the default `auto` letterboxes into the safe area for free, and
    opting in would force `pt-safe` / `px-safe` / `pb-safe` plumbing across Navbar, Footer and the
    mobile menu for no visual gain.

    Delete the `<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet" />`
    on line 47. `grep -rn "material-symbols" src` returns zero hits — this stylesheet loads on
    every route and is used by nothing. It is a render-blocking third-party request on mobile and
    is already banned for new work by CLAUDE.md (finding S-07). Keep the `apple-touch-icon` link
    on line 46.

    Leave `overflow-x-hidden` on `<body>` exactly as it is — plan 05-05 removes it.
  </action>
  <verify>
    <automated>grep -q "export const viewport" src/app/layout.tsx && grep -q 'themeColor' src/app/layout.tsx && grep -q 'colorScheme' src/app/layout.tsx && ! grep -qE "maximumScale|userScalable|viewportFit" src/app/layout.tsx && ! grep -q "fonts.googleapis.com" src/app/layout.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` contains `export const viewport`
    - `src/app/layout.tsx` contains `themeColor: "#02030a"` and `colorScheme: "dark"`
    - `src/app/layout.tsx` does NOT contain `maximumScale`, `userScalable`, or `viewportFit`
    - `src/app/layout.tsx` does NOT contain `fonts.googleapis.com`
    - `src/app/layout.tsx` STILL contains `overflow-x-hidden` (its removal belongs to plan 05-05)
    - `npx tsc --noEmit` exits 0
    - Desktop protection: the diff changes no `className` string and no element; the only
      rendered-DOM deltas at 1440px are one removed `<link>` to an unused stylesheet and one
      added `<meta name="theme-color">`. Confirm by reading `git diff -- src/app/layout.tsx`.
  </acceptance_criteria>
  <done>The viewport export exists with themeColor + colorScheme and no zoom-blocking properties; the dead Material Symbols stylesheet is gone; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire a root MotionConfig reducedMotion="user" provider</name>
  <read_first>
    - `src/app/layout.tsx` (as edited in Task 1)
    - `src/components/Footer.tsx` — an existing `"use client"` + framer-motion consumer; follow its import convention
    - `.planning/codebase/design/RESPONSIVE.md` — "Motion on mobile", rule 1
    - `.planning/responsive/SURFACES.md` — finding S-16
    - `e2e/motion.spec.ts` — what the `reduced-motion` project actually asserts
  </read_first>
  <files>src/components/MotionProvider.tsx, src/app/layout.tsx</files>
  <action>
    Create `src/components/MotionProvider.tsx` as a `"use client"` module exporting a named
    `MotionProvider({ children }: { children: React.ReactNode })` that returns
    `<MotionConfig reducedMotion="user">{children}</MotionConfig>`. Import `MotionConfig` from
    `"framer-motion"` — the package specifier used everywhere else in this repo. Do not import
    from `"motion/react"`.

    In `src/app/layout.tsx`, import `MotionProvider` and wrap `{children}` inside `<body>` with
    it. `children` crosses the client boundary as a prop, so every page stays an RSC and all 8
    routes must still prerender static.

    Why (S-16): Framer Motion's default is NOT `reducedMotion: "user"`, so today only the
    `.wordmark` CSS honours the OS setting — every entrance and scroll animation ignores it.
    `reducedMotion="user"` disables transform and layout animations while preserving opacity and
    colour.

    IMPORTANT — do not over-read the automated check. `e2e/motion.spec.ts` asserts only that no
    element carries `animation-iteration-count: infinite` (the ANTI-PATTERNS perpetual-motion ban).
    That is a CSS-animation assertion and is **unaffected by whether `MotionConfig` exists** — it
    passes identically either way. It is therefore a regression guard, NOT proof of RESP-07.
    RESP-07 is verified structurally here (the provider exists, is a client component, and wraps
    `children` in the root layout), and its runtime behaviour is listed as unverified in
    `05-05-SUMMARY.md`. Do not claim the harness proves reduced-motion works.

    Do not pass a `transition` prop, do not use `reducedMotion="always"`, and do not branch on
    `window.innerWidth` anywhere — RESPONSIVE.md forbids gating motion by rendering different
    trees (hydration mismatch, double payload, layout shift).
  </action>
  <verify>
    <automated>grep -q '"use client"' src/components/MotionProvider.tsx && grep -q 'reducedMotion="user"' src/components/MotionProvider.tsx && grep -q 'MotionProvider' src/app/layout.tsx && npx tsc --noEmit && npm run build 2>&1 | tee /tmp/build-05-01.log && test "$(grep -cE '^\s*(○|◐)' /tmp/build-05-01.log)" -ge 8 && npx playwright test --project=reduced-motion motion.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/MotionProvider.tsx` exists, its first line is `"use client"`, and it contains `reducedMotion="user"`
    - `src/app/layout.tsx` imports `MotionProvider` and `{children}` is nested inside it
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 and the route table shows `○ (Static)` for all 8 routes
      (`/`, `/features`, `/manifesto`, `/changelog`, `/mcp`, `/pricing`, `/privacy`, `/terms`).
      If any route flipped to `ƒ (Dynamic)`, the provider was wired wrong — fix it, do not accept it.
    - `npx playwright test --project=reduced-motion motion.spec.ts` exits 0
    - Desktop protection: `MotionConfig` renders no DOM element of its own; with the OS
      reduced-motion preference OFF, rendering and animation at 1440px are unchanged.
  </acceptance_criteria>
  <done>A root MotionProvider is in place, the build is green, all 8 routes still prerender static, and the reduced-motion project passes.</done>
</task>

<task type="auto">
  <name>Task 3: Gate hand-written :hover, add the svh cascade, add the min-h-viewport utility</name>
  <read_first>
    - `src/app/globals.css` (all 424 lines — you need lines 184, 209-212, 230, 378, 415 plus the top-level block structure)
    - `.planning/codebase/design/RESPONSIVE.md` — "Viewport units", "Hover on touch", and the `globals.css:211` DEBT block
    - `.planning/responsive/SURFACES.md` — findings S-02 and S-06
    - `.planning/phases/05-mobile-responsive-retrofit/05-RESEARCH.md` — section 1 (Tailwind v4 auto-gates `hover:`) and section 3 (svh vs dvh)
  </read_first>
  <files>src/app/globals.css</files>
  <action>
    Three edits, all inert at >=768px on a hover-capable pointer.

    (a) Hover gating (RESP-06 / S-06). Wrap each of the four hand-written `:hover` rules in
    `@media (hover: hover) { ... }`, preserving their declarations byte-for-byte:
      - line 184 `::-webkit-scrollbar-thumb:hover { background: #334155; }`
      - line 230 `.card-glow:hover::before { opacity: 1; }`
      - line 378 `.wordmark:hover .wordmark__outline { opacity: 0.6; }`
      - line 415 `.wordmark:hover .wordmark__fill { --wordmark-reveal: 117%; }`
    Do NOT touch any `hover:` utility in any `.tsx` file. Tailwind v4.2.1 already compiles
    `hover:` to `@media (hover: hover) { &:hover }` — this was an opt-in
    `future.hoverOnlyWhenSupported` flag in v3 and is core behaviour in v4. The ~40 utility
    hovers in TSX are already touch-safe; changing them is wrong and pollutes the diff.

    (b) svh cascade (RESP-05 / S-02). In the `body` rule at lines 209-212, keep
    `min-height: 100vh;` and add `min-height: 100svh;` on the line directly after it. Two
    declarations, no `@supports` — browsers without `svh` drop the second and keep the first
    (`svh` has been Baseline Widely Available since June 2025, ~95%). Use `svh`, never `dvh`:
    `dvh` re-resolves while the address bar animates, and because that is not a user input those
    shifts are charged in full to CLS.

    (c) Add a `@utility min-h-viewport { ... }` block emitting the same two declarations in the
    same order (`min-height: 100vh;` then `min-height: 100svh;`). Place it near the `.display`
    utility — top level, outside `@theme inline` and outside `@layer base`. Plan 05-04 swaps the
    eight page roots from `min-h-screen` (which compiles to a single `min-height: 100vh`) onto
    this utility. Do NOT introduce a `--container-*` or any other `@theme` variable for this:
    `--container-*` is a shared namespace that drives both `max-w-*` and the `@md:` container
    variants, so adding one silently mints both.
  </action>
  <verify>
    <automated>test "$(grep -c ':hover' src/app/globals.css)" -eq 4 && test "$(grep -c '@media (hover: hover)' src/app/globals.css)" -eq 4 && grep -q 'min-height: 100svh' src/app/globals.css && grep -q '@utility min-h-viewport' src/app/globals.css && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ':hover' src/app/globals.css` returns exactly 4, and each of those 4 lines sits
      inside an `@media (hover: hover)` block (verify by reading each site, not by count alone)
    - `grep -c '@media (hover: hover)' src/app/globals.css` returns exactly 4
    - `git diff -- src` shows zero changes to any `.tsx` file in this task
    - `src/app/globals.css` `body` rule contains BOTH `min-height: 100vh;` and `min-height: 100svh;`, in that order
    - `src/app/globals.css` contains `@utility min-h-viewport` whose body has both declarations in that order
    - `npm run build` exits 0 (proves the `@utility` block compiles under Tailwind 4.2.1 / lightningcss)
    - The compiled CSS contains a `.min-h-viewport` class: after the build,
      `grep -rq 'min-h-viewport' .next/static/css` succeeds
    - Desktop protection: on a hover-capable pointer `@media (hover: hover)` always matches, so
      all four hover behaviours are identical at 1440px; `svh` equals `vh` on desktop, so
      `min-height` resolves to the same pixel value.
  </acceptance_criteria>
  <done>The four hand-written hover rules are gated, `body` carries the svh cascade, and `min-h-viewport` compiles and is available to plan 05-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> static CDN origin | All 8 routes are prerendered static assets; no server-side input handling exists on this site |
| page -> third-party font CDN (`fonts.googleapis.com`) | An uncontrolled external stylesheet executed with full page privileges; being removed in Task 1 |
| page -> client component boundary | `MotionProvider` is the only new client boundary; it accepts `children` only, no user-controlled data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `src/app/layout.tsx` Google Fonts CDN `<link>` | mitigate | Task 1 deletes the unused stylesheet, removing a third-party CSS injection surface and a render-blocking request |
| T-05-02 | Information Disclosure | all routes | accept | Static marketing site: no auth, no DB, no forms, no PII. Nothing to disclose |
| T-05-03 | Denial of Service | `MotionProvider` client boundary | accept | Renders no DOM, holds no state, accepts only `children`; no attacker-reachable input |
| T-05-04 | Spoofing | clickjacking via iframe embedding | transfer | `frame-ancestors 'none'` is Phase 6 (SEC-01), explicitly out of scope here |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. `package.json` is not modified; the Package Legitimacy Gate does not apply |
</threat_model>

<verification>
After all three tasks:

1. `npx tsc --noEmit` exits 0
2. `npm run lint` reports no new errors
3. `npm run build` exits 0 and all 8 routes show `○ (Static)`
4. `npx playwright test --project=reduced-motion motion.spec.ts` exits 0
5. `npx playwright test --project=desktop-1440` exits with no NEW failures relative to the
   pre-plan baseline (record the baseline before starting)
6. `src/app/layout.tsx` still contains `overflow-x-hidden` — plan 05-05 owns its removal
</verification>

<success_criteria>
- RESP-04 satisfied: `viewport` exports `themeColor` + `colorScheme: 'dark'` with no zoom-blocking properties
- RESP-05 partially satisfied: `body` uses the `vh` -> `svh` cascade and `min-h-viewport` exists (page roots land in plan 05-04)
- RESP-06 satisfied: all 4 hand-written `:hover` rules in `globals.css` are wrapped in `@media (hover: hover)`; zero `.tsx` hover utilities touched
- RESP-07 satisfied: a root `<MotionConfig reducedMotion="user">` wraps the whole app
- RESP-08 upheld: no `className` value that applied at >=768px was changed or removed
- Build stays green and all 8 routes stay static
</success_criteria>

<output>
Create `.planning/phases/05-mobile-responsive-retrofit/05-01-SUMMARY.md` when done.
</output>
