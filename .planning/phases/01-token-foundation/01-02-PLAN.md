---
phase: 01-token-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/page.tsx
  - src/components/Hero.tsx
  - src/components/Navbar.tsx
  - src/components/hero/VCDSection.tsx
  - src/components/hero/McpIntegrationSection.tsx
  - src/components/SystemBlueprintSection.tsx
  - src/components/MethodologyPreviewSection.tsx
  - src/components/CTASection.tsx
  - src/components/DashboardPreview.tsx
  - src/components/animations/PerformanceMetric.tsx
  - src/components/CalloutCard.tsx
  - src/components/ui/button.tsx
autonomous: false
requirements: [TOKEN-01, TOKEN-02, TOKEN-04, QUAL-03]

must_haves:
  truths:
    - "The home page (/) is visually and behaviorally IDENTICAL before and after — same colors, same glows, same layout"
    - "All animations are UNCHANGED — the animate-pulse in DashboardPreview and the Framer Motion textShadow animation in PerformanceMetric still run exactly as before (only the color STRING inside the textShadow target changed to a var, never the animate prop structure, transition, or delay)"
    - "Every migrated hardcoded hex/rgba in the in-scope files now references a var(--color-ep-*) / var(--shadow-ep-*) token that resolves to the IDENTICAL value"
    - "No template literal produces invalid CSS — every CONST+hexalpha pattern was replaced with a dedicated alpha-variant var, never a var() reference followed by a hex-alpha suffix"
    - "No NEW banned anti-pattern was introduced; pre-existing violations (bg-clip-text, raw button, font-serif italic, animate-pulse, bg-[#0a0a0a]) are left exactly as-is"
    - "tsc, ESLint, and next build all pass with no new errors"
  artifacts:
    - path: "src/components/hero/VCDSection.tsx"
      provides: "Section colors via var(--color-ep-*) constants instead of hex constants"
      contains: "var(--color-ep-accent)"
    - path: "src/app/page.tsx"
      provides: "Page wrapper + grid texture using var tokens"
      contains: "var(--color-ep-bg-base)"
    - path: "src/components/animations/PerformanceMetric.tsx"
      provides: "Glow values tokenized; animation intact"
      contains: "var(--color-ep-glow-blue-40)"
  key_links:
    - from: "in-scope components"
      to: "src/app/globals.css --color-ep-* tokens"
      via: "var() references (inline style, Tailwind arbitrary, top-of-file constants)"
      pattern: "var(--color-ep-"
    - from: "PerformanceMetric textShadow animate target"
      to: "--color-ep-glow-blue-40 + --color-ep-text-glow-white"
      via: "Framer Motion animate prop (value swapped, structure untouched)"
      pattern: "textShadow"
---

<objective>
Replace every hardcoded hex/rgba color literal in the home-page render tree with a `var(--color-ep-*)` / `var(--shadow-ep-*)` reference to the value-equal tokens added in plan 01-01. The migration is purely mechanical and value-preserving: each replaced value resolves to the IDENTICAL color, so the home page is pixel- and motion-identical before and after. Then prove it with typecheck/lint/build plus a before/after visual review.

Purpose: TOKEN-01 (components reference tokens, not raw hex), TOKEN-02 (glow values tokenized at current value), TOKEN-04 (value-preserving, pixel/motion-identical), QUAL-03 (no new banned anti-patterns). This is the invisible-refactor phase whose single most important constraint is: nothing changes how it looks or moves.

Output: 12 migrated source files; a clean typecheck/lint/build; a confirmed before/after visual match of `/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-token-foundation/01-RESEARCH.md
@.planning/phases/01-token-foundation/01-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Tokens created in plan 01-01. Use these names directly. Read 01-01-SUMMARY.md for the authoritative final list. -->

Color tokens in globals.css @theme inline: --color-ep-bg-base (#02030a), --color-ep-section-bg (#04060c), --color-ep-fg-strong (#f4f6fb), --color-ep-fg-strong-white (#ffffff), --color-ep-fg (#aab3c5), --color-ep-fg-body (#d4d9e3), --color-ep-fg-muted (#a8b0c0), --color-ep-fg-muted-2 (#8590a8), --color-ep-fg-subtle (#6f7889), --color-ep-fg-subtle-2 (#5a6478), --color-ep-accent (#4487D6), --color-ep-accent-light (#77B7ED), --color-ep-code-bg (#0d1117), --color-ep-callout-bg (#0a0a0a), --color-ep-fade-black (#000000), --color-ep-white-strong, --color-ep-white, --color-ep-white-dim, --color-ep-white-track, --color-ep-active-highlight, --color-ep-separator, --color-ep-dot-glow, --color-ep-rule, --color-ep-hairline, --color-ep-divider, --color-ep-pill-bg, --color-ep-pill-border, --color-ep-pill-btn-bg, --color-ep-frame-border, --color-ep-inset-top, --color-ep-shadow-dark, --color-ep-nav-bg, --color-ep-nav-shadow, --color-ep-mobile-menu-bg, --color-ep-ring-border-outer, --color-ep-ring-bg-outer, --color-ep-ring-border-inner, --color-ep-ring-bg-inner, --color-ep-grid-line, --color-ep-chart-line (#3B82F6), --color-ep-glow-blue-40, --color-ep-glow-blue-30, --color-ep-glow-blue-25, --color-ep-glow-blue-03, --color-ep-text-glow-white, --color-ep-atmos-1..6.

Alpha-variant tokens (for template literals) — defined in 01-01 as exact 8-digit hex, referenced here as plain var() with NO appended hex-alpha suffix: --color-ep-accent-alpha-12 (#4487d61f, = former ${ACCENT}1f), --color-ep-accent-alpha-08 (#4487d614, = former ${ACCENT}14), --color-ep-accent-light-alpha-80 (#77b7edcc, = former ${ACCENT_LIGHT}cc).

Shadow shorthands in the .dark block: --shadow-ep-glow-blue-20 (0 0 40px -10px rgba(30,64,175,0.2)), --shadow-ep-habit-bar (0 0 15px -3px rgba(30,64,175,0.6)).

The full per-line mapping is in 01-RESEARCH.md §Hardcoded Color Inventory — that table is the authoritative migration spec.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate constant-pattern and inline-style components</name>
  <files>src/components/hero/VCDSection.tsx, src/components/SystemBlueprintSection.tsx, src/components/MethodologyPreviewSection.tsx, src/components/CTASection.tsx, src/components/Hero.tsx, src/components/Navbar.tsx, src/app/page.tsx</files>
  <read_first>
    - src/components/hero/VCDSection.tsx (file being modified — top-of-file constants and template-literal radial/atmosphere strings)
    - src/components/SystemBlueprintSection.tsx (file being modified)
    - src/components/MethodologyPreviewSection.tsx (file being modified)
    - src/components/CTASection.tsx (file being modified)
    - src/components/Hero.tsx (file being modified)
    - src/components/Navbar.tsx (file being modified)
    - src/app/page.tsx (file being modified)
    - src/app/globals.css (to confirm exact token names added in 01-01)
    - .planning/phases/01-token-foundation/01-RESEARCH.md (§Hardcoded Color Inventory; §Template Literal Alpha Inventory; §Architecture Patterns)
    - .planning/phases/01-token-foundation/01-01-SUMMARY.md (authoritative token list)
  </read_first>
  <action>
    For the four constant-driven sections (VCDSection.tsx, SystemBlueprintSection.tsx, MethodologyPreviewSection.tsx, CTASection.tsx): change ONLY the VALUE of each top-of-file color constant from its hex/rgba literal to the matching var(--color-ep-*) reference per 01-RESEARCH.md §Hardcoded Color Inventory. In VCDSection: SECTION_BG to var(--color-ep-section-bg); FG_STRONG (#ffffff) to var(--color-ep-fg-strong-white); FG (#d4d9e3) to var(--color-ep-fg-body); FG_MUTED (#a8b0c0) to var(--color-ep-fg-muted); ACCENT to var(--color-ep-accent); ACCENT_LIGHT to var(--color-ep-accent-light); WHITE_STRONG, WHITE, WHITE_DIM, WHITE_TRACK to their --color-ep-white-* tokens. In SystemBlueprint: BG to var(--color-ep-section-bg); FG_STRONG to var(--color-ep-fg-strong-white); FG to var(--color-ep-fg-body); FG_MUTED to var(--color-ep-fg-muted); FG_SUBTLE (#6f7889) to var(--color-ep-fg-subtle); RULE to var(--color-ep-rule); CardDecorator LINE rgba to var(--color-ep-white-dim). In MethodologyPreview and CTA: SECTION_BG to var(--color-ep-section-bg); FG_STRONG (#f4f6fb) to var(--color-ep-fg-strong); FG (#aab3c5) to var(--color-ep-fg); FG_MUTED (#8590a8) to var(--color-ep-fg-muted-2); ACCENT to var(--color-ep-accent); MethodologyPreview FG_SUBTLE (#5a6478) to var(--color-ep-fg-subtle-2); CTA top hairline rgba to var(--color-ep-hairline). Keep every constant NAME unchanged — only the string value changes. Replace remaining inline rgba constants in VCDSection (tick to var(--color-ep-separator), active-highlight to var(--color-ep-active-highlight), today-dot glow to var(--color-ep-dot-glow)). For the LabMockup friction dot (#ef4444) in SystemBlueprint: leave it untouched (no ep token covers it; do not introduce a new token). CRITICAL — template-literal alpha trap: every constant-plus-hex-alpha-suffix pattern (VCDSection radial near line 120 and today-dot near line 313, MethodologyPreview near line 41, CTA near line 34) MUST be replaced with the dedicated alpha-variant var, referenced as a PLAIN var() with NO trailing hex-alpha suffix — the ACCENT plus 1f pattern becomes var(--color-ep-accent-alpha-12) (the 01-01 token already bakes the 1f alpha into its 8-digit-hex value #4487d61f), the ACCENT plus 14 pattern becomes var(--color-ep-accent-alpha-08) (token value #4487d614), the ACCENT_LIGHT plus cc pattern becomes var(--color-ep-accent-light-alpha-80) (token value #77b7edcc). NEVER leave a var() reference with a trailing two-character hex-alpha suffix (that is invalid CSS), and NEVER append the alpha suffix to the var name — the suffix lives inside the token value, not the reference. Audit ALL template literals in these files against 01-RESEARCH.md §Template Literal Alpha Inventory before finishing. For Hero.tsx: inline style bg #02030a (3 occurrences) to var(--color-ep-bg-base); atmospheric radial stops to --color-ep-atmos-1..6; the `rgba(8,56,133,0)` transparent stop must stay BYTE-IDENTICAL — do NOT change it to the `transparent` keyword and do NOT substitute any var for it; leave that one stop exactly as the literal `rgba(8,56,133,0)` (no migration on this stop). Bottom-fade #000 to var(--color-ep-fade-black); pill bg/border/separator/divider/arrow-bg to --color-ep-pill-bg / --color-ep-pill-border / --color-ep-separator / --color-ep-divider / --color-ep-pill-btn-bg; pill text #aab3c5 to var(--color-ep-fg), accent #77B7ED to var(--color-ep-accent-light); arrow icon and H1 #f4f6fb to var(--color-ep-fg-strong); subheading #aab3c5 to var(--color-ep-fg); mockup frame border rgba(255,255,255,0.08) to var(--color-ep-frame-border), frame bg #02030a to var(--color-ep-bg-base), frame box-shadow rgba(0,0,0,0.4) to var(--color-ep-shadow-dark). For Navbar.tsx: nav bg rgba(10,16,32,0.6) to var(--color-ep-nav-bg); drop shadow rgba(0,0,0,0.5) to var(--color-ep-nav-shadow); active/inactive link and icon colors #f4f6fb/#aab3c5 to var(--color-ep-fg-strong)/var(--color-ep-fg); mobile menu bg rgba(4,6,12,0.95) to var(--color-ep-mobile-menu-bg); leave the border-white/[0.05] Tailwind class as-is (out of scope). For page.tsx: both bg-[#02030a] to bg-[var(--color-ep-bg-base)]; in the grid-texture arbitrary bg-[linear-gradient(...)] class, replace ONLY the two #ffffff1a color tokens inside the gradient with var(--color-ep-grid-line), keeping the rest of the gradient string and the mask-image radial byte-identical (the #000 inside the mask is a mask stop — leave it as-is). Do NOT touch any className that is not a color (sizing, layout, transition-* classes stay). Do NOT change any animation, transition, or Framer Motion prop. Do NOT alter slate-*/zinc-* Tailwind utility classes (deferred). Match existing formatting exactly.
  </action>
  <verify>
    <automated>cd "C:/Users/20252128/dev/Projects/el-portal-hero" && npx tsc --noEmit</automated>
    <human-check>Reviewer confirms via `git diff` of the seven files that (a) only color strings changed, (b) no var(--color-ep-...) string is immediately followed by two hex characters, (c) the `rgba(8,56,133,0)` transparent stop in Hero.tsx is byte-identical (unchanged), (d) no animation/transition/layout/Framer-Motion prop and no slate-*/zinc-* class was altered.</human-check>
  </verify>
  <acceptance_criteria>
    - Every color constant in VCDSection/SystemBlueprint/MethodologyPreview/CTASection now holds a var(--color-ep-*) string (constant NAMES unchanged); values map exactly to 01-RESEARCH.md §Hardcoded Color Inventory.
    - Zero invalid CSS: searching the seven files for a var(--color-ep-...) occurrence immediately followed by two hex characters returns NOTHING. Every former constant-plus-hex-alpha template literal now uses a plain var(--color-ep-accent-alpha-12), var(--color-ep-accent-alpha-08), or var(--color-ep-accent-light-alpha-80) with NO appended hex-alpha suffix.
    - The Hero.tsx `rgba(8,56,133,0)` transparent radial stop is byte-identical to before — NOT replaced with `transparent`, NOT replaced with a var.
    - Hero.tsx, Navbar.tsx, and page.tsx have every in-scope hardcoded hex/rgba replaced with the matching var token; the page.tsx mask-image radial geometry and gradient structure are byte-identical except the two grid-line colors.
    - `git diff` for these files shows ONLY color-string changes — no animation prop, transition class, layout class, Framer Motion prop, or slate-*/zinc-* utility altered.
    - `npx tsc --noEmit` passes with no new errors.
  </acceptance_criteria>
  <done>The seven constant/inline-style/page files reference value-equal var tokens, all template-literal alpha traps resolved with dedicated alpha vars (referenced as plain var(), no hex suffix), the rgba(8,56,133,0) stop left byte-identical, no animation or non-color code changed, and typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Tailwind-arbitrary, shadow, and glow components</name>
  <files>src/components/hero/McpIntegrationSection.tsx, src/components/DashboardPreview.tsx, src/components/animations/PerformanceMetric.tsx, src/components/CalloutCard.tsx, src/components/ui/button.tsx</files>
  <read_first>
    - src/components/hero/McpIntegrationSection.tsx (file being modified)
    - src/components/DashboardPreview.tsx (file being modified — note pre-existing violations to NOT touch)
    - src/components/animations/PerformanceMetric.tsx (file being modified — note the Framer Motion textShadow animation)
    - src/components/CalloutCard.tsx (file being modified — note pre-existing bg-[#0a0a0a] and font-serif italic)
    - src/components/ui/button.tsx (file being modified — brand and brand-link variants)
    - src/app/globals.css (to confirm exact token names, including --shadow-ep-* in the .dark block)
    - .planning/phases/01-token-foundation/01-RESEARCH.md (§Hardcoded Color Inventory rows for these files; §Glow Value Tokens; §Motion — Do Not Touch; §Pre-Existing Issues; §Tailwind Arbitrary Value Handling; §Assumptions Log A4)
    - .planning/phases/01-token-foundation/01-01-SUMMARY.md (authoritative token list)
  </read_first>
  <action>
    For McpIntegrationSection.tsx: outer ring border-top rgba(119,183,237,0.30) to var(--color-ep-ring-border-outer); outer ring radial bg rgba(68,135,214,0.18) to var(--color-ep-ring-bg-outer); inner ring border-top rgba(119,183,237,0.22) to var(--color-ep-ring-border-inner); inner ring radial bg rgba(56,103,214,0.14) to var(--color-ep-ring-bg-inner); MCP logo icon #f4f6fb to var(--color-ep-fg-strong). For DashboardPreview.tsx migrate ONLY the inline style values, NOT the slate-*/Tailwind class colors and NOT the Tailwind arbitrary shadow on the logo (leave the logo shadow as-is per research): SVG chart line stroke="#3B82F6" to stroke="var(--color-ep-chart-line)"; SVG gradient area-fill stop rgba(30,64,175,0.3) to var(--color-ep-glow-blue-30); the transparent stop rgba(30,64,175,0) stays as-is. Leave the bg-clip-text gradient (line 11), the animate-pulse (lines 63/108), and the raw button elements (lines 204/208) EXACTLY as they are — pre-existing violations, out of scope (QUAL-03 forbids only NEW violations). For PerformanceMetric.tsx: code-area bg-[#0d1117] to bg-[var(--color-ep-code-bg)]; code-block hover Tailwind arbitrary shadow becomes hover:shadow-[var(--shadow-ep-glow-blue-20)]; habit-bar 1 Tailwind arbitrary shadow becomes shadow-[var(--shadow-ep-habit-bar)] (keep the bg-primary class). For the Framer Motion textShadow animation (around lines 100-105): swap ONLY the color strings inside the animate target value — rgba(30,64,175,0.4) to var(--color-ep-glow-blue-40) and rgba(255,255,255,0.1) to var(--color-ep-text-glow-white) — yielding a target like "0 0 40px var(--color-ep-glow-blue-40), 0 0 12px var(--color-ep-text-glow-white)". Do NOT change the animate prop structure, the conditional, the "none" branch, the transition, or the delay. BLOCKER GUARD (RESEARCH Assumption A4 — Framer Motion may not resolve a CSS var embedded in a string-tween textShadow animate target; tsc/eslint cannot catch this at runtime): immediately after substituting these two color strings, run the dev server (npm run dev) and scroll the PerformanceMetric P_daily score into view to confirm the glow STILL animates on scroll-in. If the glow animates exactly as before, keep the var references. If the glow does NOT animate (stays dark/static, or the text-shadow never appears), REVERT those two strings ONLY back to the literal rgba(30,64,175,0.4) and rgba(255,255,255,0.1) values — motion-identical (TOKEN-04) takes strict priority over tokenizing this single textShadow property; the rest of PerformanceMetric's tokenization (code-bg, hover shadow, habit-bar shadow) stays regardless. Record the outcome (var kept, or reverted to literals) and the reason in the 01-02-SUMMARY. Leave bg-zinc-950/80, bg-white/[0.03], bg-zinc-900 Tailwind classes untouched (deferred). For CalloutCard.tsx: bg-[#0a0a0a] to bg-[var(--color-ep-callout-bg)] (the VALUE stays #0a0a0a; this tokenizes the pre-existing pure-black, it does NOT fix it); inline boxShadow rgba(30,64,175,0.25) to var(--color-ep-glow-blue-25), inset rgba(255,255,255,0.04) to var(--color-ep-inset-top), inset rgba(30,64,175,0.03) to var(--color-ep-glow-blue-03). Leave line 42 font-serif italic and the from-blue-700/50 gradient-border Tailwind class untouched. For button.tsx: brand variant text-[#f4f6fb] to text-[var(--color-ep-fg-strong)]; brand-link text-[#aab3c5] to text-[var(--color-ep-fg)] and hover:text-[#f4f6fb] to hover:text-[var(--color-ep-fg-strong)]; leave the white/20, white/[0.05], white/[0.08], white/25 alpha utilities untouched (deferred). Across all files: do NOT introduce any new raw button, bg-clip-text, pure-black literal, transition: all, useAnimationFrame, or perpetual animation. Match existing formatting; change only color strings.
  </action>
  <verify>
    <automated>cd "C:/Users/20252128/dev/Projects/el-portal-hero" && npx tsc --noEmit && npx eslint src/components/hero/McpIntegrationSection.tsx src/components/DashboardPreview.tsx src/components/animations/PerformanceMetric.tsx src/components/CalloutCard.tsx src/components/ui/button.tsx</automated>
    <human-check>Reviewer confirms via `git diff` that the animate-pulse, bg-clip-text, raw buttons, and font-serif italic are byte-identical to before; the PerformanceMetric textShadow animate prop changed ONLY its two color strings (or was reverted to literals per the A4 smoke-test, with the outcome recorded in the SUMMARY); the PerformanceMetric scroll-in glow was visually confirmed to STILL animate; CalloutCard background var resolves to #0a0a0a (unchanged color).</human-check>
  </verify>
  <acceptance_criteria>
    - McpIntegrationSection ring borders/bgs and logo icon use the matching var(--color-ep-*) tokens.
    - DashboardPreview's inline SVG stroke and gradient-fill stop use var(--color-ep-chart-line) and var(--color-ep-glow-blue-30); the slate-* classes, logo Tailwind shadow, bg-clip-text, animate-pulse, and raw buttons are unchanged.
    - PerformanceMetric code-bg, hover shadow, and habit-bar shadow use the var tokens. The Framer Motion textShadow scroll-in animation was smoke-tested in the running dev server and CONFIRMED to still animate: either the animate target now references var(--color-ep-glow-blue-40) and var(--color-ep-text-glow-white) (animation verified working), OR — if Framer Motion did not resolve the vars at runtime (Assumption A4 realized) — those two strings were reverted to the literal rgba(30,64,175,0.4) and rgba(255,255,255,0.1) to preserve TOKEN-04 motion-identity. The animate prop structure, transition, delay, and "none" branch are byte-identical to before either way. The outcome (kept-as-var vs reverted-to-literal) and the smoke-test result are recorded in 01-02-SUMMARY.
    - CalloutCard background uses var(--color-ep-callout-bg) (value still #0a0a0a) and the boxShadow uses the three glow/inset var tokens; font-serif italic untouched.
    - button.tsx brand/brand-link text colors use var tokens; the white-alpha utilities are untouched.
    - No var(--color-ep-...) occurrence is immediately followed by two hex characters in any of these files.
    - `npx tsc --noEmit` and `npx eslint` over the five files pass with no new errors.
  </acceptance_criteria>
  <done>The five remaining components reference value-equal var/shadow tokens, the PerformanceMetric textShadow animation was smoke-tested and confirmed motion-identical (var kept or reverted to literals per A4), every pre-existing violation and every animation is byte-identical, and typecheck + lint pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Whole-page verification — value-preserving proof</name>
  <read_first>
    - .planning/phases/01-token-foundation/01-RESEARCH.md (§Verification Approach; §Grep verification command; the value-equality mathematical argument)
    - CLAUDE.md (Anti-Patterns list — QUAL-03)
  </read_first>
  <action>
    This is the verification gate. Before requesting human review, the executor runs all automated gates and pastes their output: (1) `npx tsc --noEmit` passes; (2) `npm run lint` (or `npx eslint`) passes; (3) `npx next build` completes; (4) residual-hex grep over the 12 migrated files (mirrors 01-RESEARCH.md §Grep verification command) — `grep -rEn "#[0-9a-fA-F]{3,8}|rgba\(" src/app/page.tsx src/components/Hero.tsx src/components/Navbar.tsx src/components/hero/VCDSection.tsx src/components/hero/McpIntegrationSection.tsx src/components/SystemBlueprintSection.tsx src/components/MethodologyPreviewSection.tsx src/components/CTASection.tsx src/components/DashboardPreview.tsx src/components/animations/PerformanceMetric.tsx src/components/CalloutCard.tsx src/components/ui/button.tsx` — and account for EVERY remaining match as a justified exception (documented deferrals: slate-*/zinc-* are class-based not inline hex; the #ef4444 friction dot; the rgba(8,56,133,0) transparent stop in Hero.tsx left byte-identical; the rgba(30,64,175,0) transparent stop in DashboardPreview; the #000 mask stop in page.tsx; the DashboardPreview logo Tailwind shadow; button white-alpha utilities; AND — if the A4 smoke-test forced a revert — the two literal rgba strings in PerformanceMetric's textShadow animate target). Any UNEXPECTED hex/rgba is a defect to fix before requesting the checkpoint; (5) invalid-CSS guard — confirm there is NO occurrence of `var(--color-ep-` followed immediately by two hex characters anywhere in the 12 files. Only after all five gates pass does the executor present the human-verify steps below.
  </action>
  <what-built>
    All 12 home-page files (page.tsx, Hero, Navbar, VCDSection, McpIntegrationSection, SystemBlueprintSection, MethodologyPreviewSection, CTASection, DashboardPreview, PerformanceMetric, CalloutCard, button) now reference the value-equal `--color-ep-*` / `--shadow-ep-*` tokens instead of hardcoded hex/rgba (the PerformanceMetric textShadow animate target either references vars or retains its literals per the A4 smoke-test). The five automated gates in `<action>` have passed and their output is pasted into the summary.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/ in a browser at 1440px width.
    2. Compare against the pre-migration appearance (use git stash on the working tree, or a baseline screenshot captured before plan 01-02 ran, or the live page from visual memory). Scroll the full page: hero, VCD section, MCP rings, dashboard preview, system blueprint, methodology preview, CTA, footer.
    3. Confirm pixel-identical: same background tones, same announcement-pill styling, same atmospheric radial glows, same MCP ring colors, same chart line and area fill, same CalloutCard glow, same headings/body text tones.
    4. Confirm motion-identical: the DashboardPreview "CYCLE 1 ACTIVE" / flame `animate-pulse` still pulses; the PerformanceMetric P_daily score text-glow still animates on scroll-in (this was already smoke-tested in Task 2 per Assumption A4 — re-confirm here as part of the whole-page pass); all Hero entrance/stagger animations behave as before.
    5. In DevTools, inspect one migrated element and confirm e.g. `color: var(--color-ep-fg-strong)` computes to `rgb(244, 246, 251)` (= #f4f6fb), proving the var resolves to the identical value.
    6. (Optional, developer discretion) A `npx playwright screenshot` before/after diff is the strongest proof, but Playwright is NOT a project dependency — only do this if comfortable with the on-demand npx download; it is not required. The value-equality argument (var resolves to the identical literal at paint time, no color-space conversion) is the mathematical guarantee per 01-RESEARCH.md.
  </how-to-verify>
  <resume-signal>Type "approved" once the home page is confirmed visually and behaviorally identical (including the PerformanceMetric scroll-in glow animation) and all five automated gates passed; otherwise describe the specific visual/motion difference observed (which section, what changed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static marketing-site CSS/token refactor. No user input, no new dependency, no network surface, no backend. No new attack surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | dependency installs | accept | No package-manager installs (the optional Playwright screenshot is developer-discretion and not required; no package added to the project). No new attack surface. App/backend security is out of scope — this is the marketing site. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (no new errors).
- `npm run lint` / `npx eslint` passes (no new errors).
- `npx next build` completes successfully.
- PerformanceMetric textShadow scroll-in glow smoke-tested in dev server (Assumption A4): confirmed still animating (var kept, or reverted to literals if Framer Motion did not resolve the var).
- Residual-hex grep over the 12 files yields only the documented justified exceptions.
- No `var(--color-ep-` followed by a two-char hex suffix anywhere in the 12 files.
- Human before/after visual + motion review of `/` confirms pixel- and motion-identical (TOKEN-04).
- DevTools computed-color spot check confirms a var resolves to its identical hex (value-equality proof).
</verification>

<success_criteria>
All 12 home-page files reference value-equal `--color-ep-*` / `--shadow-ep-*` tokens; the home page is pixel- and motion-identical before and after (including the smoke-tested PerformanceMetric scroll-in glow); both glow families are tokenized at their current value (TOKEN-02); no new banned anti-pattern was introduced and every pre-existing violation is byte-identical (QUAL-03); typecheck, lint, and build pass (supports QUAL-02 in Phase 4); and a human visual/motion review confirms no perceptible change (TOKEN-04).
</success_criteria>

<output>
Create `.planning/phases/01-token-foundation/01-02-SUMMARY.md` when done. Record: per-file what changed, the PerformanceMetric textShadow A4 smoke-test outcome (var kept vs reverted to literals, and why), the residual-hex grep matches and why each is justified, the gate outputs (tsc/lint/build), and the human-verify result.
</output>
</task>
</output>
