---
phase: 01-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
autonomous: true
requirements: [TOKEN-02]

must_haves:
  truths:
    - "Every distinct hardcoded color value used by the home-page render tree has a value-equal CSS variable defined in globals.css holding the IDENTICAL hex/rgba string"
    - "Both glow families (rgba(30,64,175,...) and #3B82F6/rgb(59,130,246)) are tokenized at their CURRENT value — no desaturation, no color change"
    - "The two animated-glow shadow shorthands live in the .dark block (NOT in @theme inline) so Tailwind does not mint unexpected shadow utilities"
    - "next build still succeeds with the new tokens present (no parse errors in globals.css)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Phase 1 value-equal token block (--color-ep-* color vars + --shadow-ep-* shadow vars)"
      contains: "--color-ep-bg-base"
  key_links:
    - from: "src/app/globals.css @theme inline block"
      to: "downstream component migration (plan 01-02)"
      via: "var(--color-ep-*) references"
      pattern: "--color-ep-"
---

<objective>
Add a complete block of value-equal CSS custom properties to `globals.css` so that the home-page components in plan 01-02 have tokens to reference. Each new token holds the EXACT hex/rgba string currently hardcoded in the components — same notation, same value. This plan creates the contract; it touches no component and changes nothing visible.

Purpose: A pixel-identical refactor requires the variable to resolve to the identical literal at paint time. Remapping onto the existing OKLCH tokens is forbidden because OKLCH and hex are different color spaces and not provably byte-identical (see 01-RESEARCH.md §The Critical Issue — Value Equality). The new `--color-ep-*` tokens are deliberately defined in hex/rgba so no color-space conversion occurs.

Output: ~52 new `--color-ep-*` color variables inside `@theme inline`, plus 2 `--shadow-ep-*` shadow shorthands inside the `.dark` block. No component edits in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-token-foundation/01-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Current structure of globals.css the executor must respect. Do NOT reorder or restyle existing lines. -->

`@theme inline { ... }` block spans lines 32-89. Existing custom brand tokens (lines 33-43) include:
- `--color-primary: #1E40AF;`
- `--color-primary-glow: #3B82F6;`  (banned per CONCERNS.md — do NOT reuse for chart line; create --color-ep-chart-line instead)
- `--color-glass-surface: rgba(255, 255, 255, 0.03);`
- `--color-glass-border: rgba(255, 255, 255, 0.06);`
- `--color-glass-highlight: rgba(255, 255, 255, 0.08);`
The block closes with `}` on line 89.

The `.dark { ... }` block spans lines 197-229 and holds the shadcn dark-mode values (hex/rgba/hsl). It closes with `}` on line 229. Shadow shorthands go here, NOT in @theme inline (Tailwind v4 @theme inline mints utilities from its entries — a full box-shadow string there can generate an unexpected `shadow-ep-*` utility or fail silently; see 01-RESEARCH.md Pitfall 2 and Open Question 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Phase 1 value-equal token block to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (the file being modified — read the @theme inline block lines 32-89 and the .dark block lines 197-229)
    - .planning/phases/01-token-foundation/01-RESEARCH.md (§Consolidation Map (Final Token List), §Template Literal Alpha Inventory, §Glow Value Tokens — these tables ARE the source of truth for every token name and value)
  </read_first>
  <action>
    Insert a clearly commented `/* Phase 1: value-equal tokens — IDENTICAL to current component hardcoded values. NOT the aspirational OKLCH palette; renaming/retiring is deferred to a later phase. */` block at the END of the existing `@theme inline { ... }` block in src/app/globals.css (just before the closing `}` on line 89), adding the COLOR variables exactly as listed in 01-RESEARCH.md §Consolidation Map. Use the research's exact token names and exact values verbatim. The color tokens to add (name = value): --color-ep-bg-base = #02030a; --color-ep-section-bg = #04060c; --color-ep-fg-strong = #f4f6fb; --color-ep-fg-strong-white = #ffffff; --color-ep-fg = #aab3c5; --color-ep-fg-body = #d4d9e3; --color-ep-fg-muted = #a8b0c0; --color-ep-fg-muted-2 = #8590a8; --color-ep-fg-subtle = #6f7889; --color-ep-fg-subtle-2 = #5a6478; --color-ep-accent = #4487D6; --color-ep-accent-light = #77B7ED; --color-ep-code-bg = #0d1117; --color-ep-callout-bg = #0a0a0a; --color-ep-fade-black = #000000; --color-ep-white-strong = rgba(255,255,255,0.92); --color-ep-white = rgba(255,255,255,0.55); --color-ep-white-dim = rgba(255,255,255,0.22); --color-ep-white-track = rgba(255,255,255,0.08); --color-ep-active-highlight = rgba(255,255,255,0.18); --color-ep-separator = rgba(255,255,255,0.20); --color-ep-dot-glow = rgba(255,255,255,0.6); --color-ep-rule = rgba(255,255,255,0.14); --color-ep-hairline = rgba(255,255,255,0.12); --color-ep-divider = rgba(255,255,255,0.15); --color-ep-pill-bg = rgba(255,255,255,0.04); --color-ep-pill-border = rgba(255,255,255,0.10); --color-ep-pill-btn-bg = rgba(255,255,255,0.06); --color-ep-frame-border = rgba(255,255,255,0.08); --color-ep-inset-top = rgba(255,255,255,0.04); --color-ep-shadow-dark = rgba(0,0,0,0.4); --color-ep-nav-bg = rgba(10,16,32,0.6); --color-ep-nav-shadow = rgba(0,0,0,0.5); --color-ep-mobile-menu-bg = rgba(4,6,12,0.95); --color-ep-ring-border-outer = rgba(119,183,237,0.30); --color-ep-ring-bg-outer = rgba(68,135,214,0.18); --color-ep-ring-border-inner = rgba(119,183,237,0.22); --color-ep-ring-bg-inner = rgba(56,103,214,0.14); --color-ep-grid-line = rgba(255,255,255,0.10); --color-ep-chart-line = #3B82F6; --color-ep-glow-blue-40 = rgba(30,64,175,0.4); --color-ep-glow-blue-30 = rgba(30,64,175,0.3); --color-ep-glow-blue-25 = rgba(30,64,175,0.25); --color-ep-glow-blue-03 = rgba(30,64,175,0.03); --color-ep-text-glow-white = rgba(255,255,255,0.1); --color-ep-atmos-1 = rgba(96,165,235,0.05); --color-ep-atmos-2 = rgba(68,135,214,0.02); --color-ep-atmos-3 = rgba(150,200,245,0.028); --color-ep-atmos-4 = rgba(68,135,214,0.01); --color-ep-atmos-5 = rgba(180,215,250,0.02); --color-ep-atmos-6 = rgba(96,165,235,0.008). ALSO add the three template-literal alpha-variant tokens from 01-RESEARCH.md §Template Literal Alpha Inventory (these are the dedicated tokens that replace the ACCENT-plus-hex-alpha-suffix pattern, which becomes invalid CSS once ACCENT is a var). Define these three using the EXACT 8-digit hex form of the original `${CONSTANT}<suffix>` literal — NOT a rounded rgba decimal — so the output is byte-identical with zero precision drift: --color-ep-accent-alpha-12 = #4487d61f (this is `${ACCENT}1f` = `#4487D6` lowercased + the `1f` alpha suffix); --color-ep-accent-alpha-08 = #4487d614 (this is `${ACCENT}14` = `#4487D6` lowercased + the `14` alpha suffix); --color-ep-accent-light-alpha-80 = #77b7edcc (this is `${ACCENT_LIGHT}cc` = `#77B7ED` lowercased + the `cc` alpha suffix). Before writing each token, verify the 8-digit hex equals the original concatenation: lowercase the base hex and append the two-character alpha hex (e.g. `#4487D6` → `#4487d6`, then `+ 1f` = `#4487d61f`). Do NOT use the rounded rgba decimal forms `rgba(68,135,214,0.122)` / `rgba(68,135,214,0.078)` / `rgba(119,183,237,0.8)` — the 8-digit hex eliminates the 0.122-vs-0.12157 precision drift entirely and is the form 01-02's template-literal-trap fix references as a plain `var(--…)` (no appended hex-alpha suffix). Then add the SHADOW shorthands to the `.dark { ... }` block (lines 197-229), NOT to @theme inline: --shadow-ep-glow-blue-20 = 0 0 40px -10px rgba(30,64,175,0.2); --shadow-ep-habit-bar = 0 0 15px -3px rgba(30,64,175,0.6). Do not modify, reorder, or restyle any existing line. Do not touch the OKLCH tokens, the scrollbar block, html/body rules, or any utility class. This is purely additive (TOKEN-02 tokenizes the glow VALUES at their current value — no desaturation, no remap to --color-accent-subtle).
  </action>
  <verify>
    <automated>cd "C:/Users/20252128/dev/Projects/el-portal-hero" && node -e "const c=require('fs').readFileSync('src/app/globals.css','utf8'); const need=['--color-ep-bg-base','--color-ep-section-bg','--color-ep-accent','--color-ep-accent-light','--color-ep-glow-blue-40','--color-ep-glow-blue-30','--color-ep-glow-blue-25','--color-ep-glow-blue-03','--color-ep-text-glow-white','--color-ep-accent-alpha-12','--color-ep-accent-alpha-08','--color-ep-accent-light-alpha-80','--color-ep-atmos-6','--color-ep-grid-line','--color-ep-chart-line','--color-ep-fg-strong','--color-ep-nav-bg','--color-ep-mobile-menu-bg','--shadow-ep-glow-blue-20','--shadow-ep-habit-bar']; const miss=need.filter(t=>!c.includes(t)); if(miss.length){console.error('MISSING NAME:',miss.join(', '));process.exit(1)} const values=['--color-ep-accent-alpha-12: #4487d61f','--color-ep-accent-alpha-08: #4487d614','--color-ep-accent-light-alpha-80: #77b7edcc','--color-ep-glow-blue-40: rgba(30, 64, 175, 0.4)','--color-ep-text-glow-white: rgba(255, 255, 255, 0.1)','--shadow-ep-glow-blue-20: 0 0 40px -10px rgba(30, 64, 175, 0.2)','--shadow-ep-habit-bar: 0 0 15px -3px rgba(30, 64, 175, 0.6)']; const norm=c.replace(/[ \t]+/g,' '); const badv=values.filter(v=>!norm.includes(v)); if(badv.length){console.error('WRONG/MISSING VALUE:',badv.join(' | '));process.exit(1)} console.log('all',need.length,'sentinel tokens present with',values.length,'exact values verified')"</automated>
  </verify>
  <acceptance_criteria>
    - All ~52 `--color-ep-*` color tokens from 01-RESEARCH.md §Consolidation Map exist inside the `@theme inline` block with values byte-identical to the research table (hex/rgba notation preserved, e.g. `--color-ep-bg-base: #02030a;`, `--color-ep-atmos-3: rgba(150, 200, 245, 0.028);`).
    - The three template-literal alpha-variant tokens exist using the EXACT 8-digit hex form (NOT rounded rgba decimals): the file contains the literal strings `--color-ep-accent-alpha-12: #4487d61f;`, `--color-ep-accent-alpha-08: #4487d614;`, and `--color-ep-accent-light-alpha-80: #77b7edcc;`. Each 8-digit hex equals its source `${CONSTANT}<suffix>` concatenation (`#4487d6`+`1f`, `#4487d6`+`14`, `#77b7ed`+`cc`) — zero precision drift.
    - The two `--shadow-ep-*` shadow shorthands exist inside the `.dark` block (NOT inside `@theme inline`): `--shadow-ep-glow-blue-20: 0 0 40px -10px rgba(30, 64, 175, 0.2);` and `--shadow-ep-habit-bar: 0 0 15px -3px rgba(30, 64, 175, 0.6);`.
    - `git diff src/app/globals.css` shows ONLY additions (new lines) — zero modifications or deletions to any pre-existing line (the diff contains no `-` lines other than trailing context of the inserted block).
    - `npx next build` completes with no new errors (globals.css parses; no unexpected Tailwind utility generation failure).
    - The sentinel-token verify command above exits 0 — it asserts both token PRESENCE and the EXACT VALUE of the three alpha-variant tokens, both glow tokens, and both shadow tokens, so a wrong-precision value fails the check instead of passing on name-only.
  </acceptance_criteria>
  <done>globals.css contains the complete Phase 1 value-equal token block (color vars in @theme inline, shadow vars in .dark), the three alpha-variant tokens use exact 8-digit hex, all existing lines untouched, and the project builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This is a static marketing-site CSS token addition. No user input, no new dependency, no network surface, no backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | dependency installs | accept | No package-manager installs in this plan (purely editing globals.css). No new attack surface introduced. App/backend security is out of scope (this is the marketing site). |
</threat_model>

<verification>
- `npx next build` succeeds.
- Sentinel-token presence AND value check passes (verify command above asserts exact values of alpha-variant, glow, and shadow tokens).
- `git diff` confirms additive-only change to globals.css.
</verification>

<success_criteria>
The complete value-equal token block exists in globals.css (color vars in `@theme inline`, shadow vars in `.dark`), every value byte-identical to the 01-RESEARCH.md tables (alpha-variant tokens use exact 8-digit hex), no existing line altered, and `next build` passes. Nothing visible changed because no component references the new tokens yet.
</success_criteria>

<output>
Create `.planning/phases/01-token-foundation/01-01-SUMMARY.md` when done. List every token added (name + value) so plan 01-02 can reference the exact names.
</output>
</task>
</output>
