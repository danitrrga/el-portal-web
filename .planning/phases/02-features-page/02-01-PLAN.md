---
phase: 02-features-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Navbar.tsx
  - src/components/Footer.tsx
autonomous: true
requirements: [FEAT-01, QUAL-01]
must_haves:
  truths:
    - "A Features link appears in the desktop and mobile site navigation and points to /features (D-11)"
    - "The Footer Product-column Features link points to /features (no longer to /) (D-11)"
  artifacts:
    - path: "src/components/Navbar.tsx"
      provides: "Features entry in navLinks"
      contains: '{ href: "/features", label: "Features" }'
    - path: "src/components/Footer.tsx"
      provides: "Repointed Product-column Features link"
      contains: '{ label: "Features", href: "/features" }'
  key_links:
    - from: "src/components/Navbar.tsx"
      to: "/features"
      via: "navLinks array entry"
      pattern: 'href: "/features"'
    - from: "src/components/Footer.tsx"
      to: "/features"
      via: "footerColumns Product link"
      pattern: 'label: "Features", href: "/features"'
---

<objective>
Wire the new Features route into the site chrome: add a Features link to the Navbar `navLinks` and fix the Footer Product-column Features link (currently points to `/`) to point to `/features`. Implements D-11; satisfies FEAT-01 and the navigation half of QUAL-01.

Purpose: The /features page is only reachable if the two global navigation touch-points point to it. This wiring is independent of the page's content, so it ships in Wave 1 alongside the section components.
Output: Edited `Navbar.tsx` and `Footer.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-features-page/02-CONTEXT.md
@.planning/phases/02-features-page/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Features link to Navbar navLinks</name>
  <read_first>
    - src/components/Navbar.tsx (the file being modified — `navLinks` array, lines 12-17)
    - .planning/phases/02-features-page/02-PATTERNS.md (§ "Navbar.tsx — navLinks edit" — exact required array)
    - .planning/phases/02-features-page/02-CONTEXT.md (D-11)
  </read_first>
  <files>src/components/Navbar.tsx</files>
  <action>
    In the module-level `navLinks` array, insert a new entry `{ href: "/features", label: "Features" }` immediately before the existing `{ href: "/pricing", label: "Pricing" }` entry, so the order is Manifesto, Changelog, Methodology, Features, Pricing. The array is consumed by both the desktop and mobile menu maps, so no further edits are needed. Do NOT touch any other line in the file (the `"use client"` directive, `APP_URL`, the desktop/mobile render maps, the Sign Up / Log in buttons all stay as-is). Match the existing two-space-key object style of the array.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/Navbar.tsx','utf8'); const m=s.match(/navLinks\s*=\s*\[([\s\S]*?)\]/); if(!m) process.exit(1); const a=m[1]; const fi=a.indexOf('/features'); const pi=a.indexOf('/pricing'); if(fi<0||pi<0||fi>pi){console.error('Features missing or not before Pricing');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Navbar.tsx` `navLinks` array contains an entry with `href: "/features"` and `label: "Features"`.
    - The `/features` entry appears before the `/pricing` entry in the array.
    - No other lines in `Navbar.tsx` are changed (diff is limited to the inserted array entry).
  </acceptance_criteria>
  <done>navLinks contains a Features entry ordered before Pricing; the rest of the file is byte-for-byte unchanged except the inserted line.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the Footer Product-column Features link to /features</name>
  <read_first>
    - src/components/Footer.tsx (the file being modified — `footerColumns` Product column, lines 6-15)
    - .planning/phases/02-features-page/02-PATTERNS.md (§ "Footer.tsx — footerColumns edit" — exact required change)
    - .planning/phases/02-features-page/02-CONTEXT.md (D-11 / QUAL-01)
  </read_first>
  <files>src/components/Footer.tsx</files>
  <action>
    In the `footerColumns` array, inside the `Product` column's `links`, change the Features entry from `{ label: "Features", href: "/" }` to `{ label: "Features", href: "/features" }`. This is the only change — leave every other footer link, the Framer Motion variants, the brand watermark, and all styling untouched. (Note: the existing `zinc-*` classes and `font-serif` watermark in this file are legacy out-of-phase styling — do NOT migrate them here; this plan is link-wiring only.)
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/Footer.tsx','utf8'); if(!/label:\s*\"Features\",\s*href:\s*\"\/features\"/.test(s)){console.error('Features href not repointed');process.exit(1)} if(/label:\s*\"Features\",\s*href:\s*\"\/\"/.test(s)){console.error('old href:\"/\" still present');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Footer.tsx` Product column Features link has `href: "/features"`.
    - No remaining `{ label: "Features", href: "/" }` entry exists.
    - No other footer links or styling are modified (diff limited to the one href value).
  </acceptance_criteria>
  <done>Footer Product Features link resolves to /features; no other footer changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | Edits are to internal route-string constants in static client components. No untrusted input, no auth, no DB, no external resource fetch introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | navLinks / footerColumns route strings | accept | Hardcoded internal relative paths (`/features`); no user input flows into the href. No realistic threat at this surface. |

No external links, `target="_blank"`, or package installs are introduced by this plan, so no high-severity threats apply. (The pre-existing `APP_URL` external link in Navbar is unchanged and out of scope here.)
</threat_model>

<verification>
- `npm run lint` exits 0 for the two touched files (no new ESLint errors).
- `npm run build` is exercised by plan 02-04 after the route exists; this plan does not need a full build (the links target a route created in Wave 2, which is expected and not a build error — Next.js does not fail builds on internal `<Link href>` to a not-yet-existing route).
</verification>

<success_criteria>
- Features link present in `navLinks`, ordered before Pricing.
- Footer Product Features link repointed from `/` to `/features`.
- Both files otherwise unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-features-page/02-01-SUMMARY.md` when done.
</output>
