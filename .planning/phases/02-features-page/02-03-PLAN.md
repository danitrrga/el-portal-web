---
phase: 02-features-page
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/DeepDiveRow.tsx
autonomous: true
requirements: [FEAT-02, FEAT-03, FEAT-06]
must_haves:
  truths:
    - "Two deep-dive rows render the headline differentiators: Pulse and Trends/Insights (D-06)"
    - "Each row uses an asymmetric two-column layout with a line-style visual (correlation/line chart for Trends; typographic line treatment for Pulse) — D-08"
    - "Deep-dive content is grounded in features.md (Pulse = words not numeric scores; Trends = correlation/pattern analysis) with no overstated claims"
    - "Entrance animation uses GPU-composited transform/opacity, custom easing, staggered whileInView-once — no perpetual decorative motion (FEAT-06)"
  artifacts:
    - path: "src/components/features/DeepDiveRow.tsx"
      provides: "Client-island deep-dive row component (Framer Motion) rendering Pulse and Trends rows"
      contains: '"use client"'
      min_lines: 70
  key_links:
    - from: "src/components/features/DeepDiveRow.tsx"
      to: "framer-motion"
      via: "motion.section whileInView once with [0.22,1,0.36,1] easing"
      pattern: 'whileInView'
    - from: "src/components/features/DeepDiveRow.tsx"
      to: "--color-ep-accent / --color-ep-chart-line SVG"
      via: "inline static/Framer SVG line visual (NOT Remotion)"
      pattern: '<svg'
---

<objective>
Build the two deep-dive rows for the headline differentiators — Pulse and Trends/Insights (D-06) — as a client-island component using Framer Motion entrance animation. Each row is an asymmetric two-column layout (sticky intro + line-style visual), with line/blueprint-vocabulary SVG visuals authored fresh (NOT imported from the Remotion `AsymptoticAnimation`/`CyclesAnimation` components). Implements D-06, D-08; satisfies FEAT-06 motion requirements; contributes to FEAT-02/FEAT-03.

Purpose: Pulse and Trends are the differentiators that earn depth (per CONTEXT D-06); they need the asymmetric deep-dive treatment and motion that the flat highlight grid does not. Isolating Framer Motion here keeps the page shell and other sections as RSC.
Output: `src/components/features/DeepDiveRow.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-features-page/02-CONTEXT.md
@.planning/phases/02-features-page/02-PATTERNS.md
@CLAUDE.md
@src/app/globals.css
@src/components/Footer.tsx
@src/app/methodology/page.tsx

<interfaces>
<!-- Contracts the executor needs — extracted from codebase. -->

Canonical Framer Motion entrance variants (Footer.tsx lines 32-50) — REUSE these exact values:
  containerVariants: hidden {opacity:0} → visible {opacity:1, transition:{staggerChildren:0.08}}
  itemVariants: hidden {opacity:0, y:16} → visible {opacity:1, y:0,
    transition:{duration:0.5, ease:[0.22,1,0.36,1]}}
  Trigger: initial="hidden" whileInView="visible" viewport={{ once: true, margin: "-60px" }}

Tokens (src/app/globals.css):
  --color-ep-fg-strong, --color-ep-fg, --color-ep-fg-muted, --color-ep-rule,
  --color-ep-hairline, --color-ep-accent, --color-ep-accent-light,
  --color-ep-accent-light-alpha-80, --color-ep-chart-line

DO NOT import (PATTERNS.md "No Analog Found"):
  src/components/animations/AsymptoticAnimation.tsx  — Remotion (useCurrentFrame/useVideoConfig)
  src/components/animations/CyclesAnimation.tsx       — Remotion, perpetual rotation loop
  Reuse the SVG VOCABULARY (axis lines, curved correlation path, chart-line color) as fresh static/Framer SVG only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DeepDiveRow client island — reusable asymmetric deep-dive row</name>
  <read_first>
    - src/app/methodology/page.tsx (`PulseLoopSection` lines 363-548 asymmetric two-column layout; `TrendsInsightsSection` lines 703-853 visual + annotation card; left-border callout lines 672-694)
    - src/components/Footer.tsx (lines 32-50 — canonical containerVariants/itemVariants to reuse verbatim)
    - .planning/phases/02-features-page/02-PATTERNS.md (§ "DeepDiveRow.tsx" — two-column layout, "use client" rule, line-chart SVG vocabulary, annotation/insight card, left-border accent)
    - .planning/phases/02-features-page/02-CONTEXT.md (D-06 two rows Pulse + Trends, D-08 line visuals not Remotion, D-03 mono accent)
    - C:\Users\20252128\dev\Projects\el-portal\docs\features.md (AUTHORITATIVE — "Pulse" section: words not numeric scores, mood/energy/sleep/freeform, feeds trends engine; "Trends" section: performance charts, wellbeing composites, habit reliability, heatmaps, mood calendars, correlation analysis)
    - .planning/research/SUMMARY.md (Pulse + Trends/Insights framing; Gemini/AI is Pro-tier opt-in — do not overstate)
    - src/app/globals.css (`--color-ep-*` tokens incl. `--color-ep-chart-line`)
  </read_first>
  <files>src/components/features/DeepDiveRow.tsx</files>
  <action>
    Create a `"use client"` component file that exports the two deep-dive rows for the page. Define the canonical `containerVariants` and `itemVariants` verbatim from `Footer.tsx` (easing `[0.22, 1, 0.36, 1] as [number, number, number, number]`, duration 0.5, staggerChildren 0.08). Implement a reusable `DeepDiveRow` building block: a `motion.section` with `initial="hidden" whileInView="visible" viewport={{ once: true, margin: "-60px" }}` wrapping a `grid gap-10 md:grid-cols-[1fr_1.3fr] md:gap-14`. Left column is a `md:sticky md:top-32 md:self-start` `motion.div` with a `.display` H2 (`clamp(26px, 2.6vw, 34px)`, `var(--color-ep-fg-strong)`) + intro `<p>` (`var(--color-ep-fg)`) + an optional mono left-border accent annotation (`border-l-2`, `var(--color-ep-accent-light-alpha-80)`). Right column is a `motion.div` holding the line-style visual. Provide a way to render exactly two rows (D-06): default-export a component that composes a Pulse row and a Trends row (either by mapping a local data array or by rendering two `DeepDiveRow` instances — your choice). 

    PULSE row (D-08 "typographic line treatment"): the visual is a typographic/line treatment, NOT a chart — e.g. the freeform words/mood/energy/sleep vocabulary set as mono/typographic elements on hairline rules. Copy: Pulse is the daily check-in in WORDS not numeric scores (mood, energy, sleep, freeform reflection), feeding the trends engine — straight from features.md. 

    TRENDS row (D-08 "correlation/line chart"): the visual is a fresh inline `<svg>` line/correlation chart authored from scratch reusing the AsymptoticAnimation VOCABULARY ONLY — axis lines in `var(--color-ep-hairline)` at low `strokeWidth`, a curved trend/correlation `<path>` stroked in `var(--color-ep-accent)` or `var(--color-ep-chart-line)`. Do NOT import the Remotion components. The SVG may be static, or use a one-shot Framer Motion `pathLength` entrance (no looping `repeat: Infinity`, no `useAnimationFrame`, no `animate-pulse`). Mark the SVG `aria-hidden`. Copy: Trends surfaces performance over cycle/version, wellbeing composites, habit reliability, heatmaps, mood calendars, and correlation analysis — grounded in features.md; if AI/Insights is mentioned, frame it as Pro-tier, opt-in (do not present as always-on/free). 

    Token-only inline styling, no raw hex. No perpetual/decorative motion (no `animate-pulse`/`animate-bounce`/`animate-ping`/`repeat: Infinity`/`useAnimationFrame`). No `transition: all`. No gradient-clipped text. No raw `<button>`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/features/DeepDiveRow.tsx','utf8'); if(!/^[\s\S]{0,40}use client/.test(s)){console.error('must start with use client');process.exit(1)} if(/AsymptoticAnimation|CyclesAnimation/.test(s)){console.error('Remotion component imported — forbidden');process.exit(1)} if(/repeat:\s*Infinity|animate-pulse|animate-bounce|animate-ping|useAnimationFrame/.test(s)){console.error('perpetual/decorative motion banned');process.exit(1)} if(/transition:\s*all|transition-all/.test(s)){console.error('transition:all banned');process.exit(1)} if(!/whileInView/.test(s)||!/0\.22,\s*1,\s*0\.36,\s*1/.test(s)){console.error('missing canonical entrance motion');process.exit(1)} if(!/<svg/.test(s)){console.error('missing line-style SVG visual');process.exit(1)} if(/#[0-9a-fA-F]{6}/.test(s)){console.error('raw hex found');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/features/DeepDiveRow.tsx` begins with `"use client"` and imports from `framer-motion`.
    - Default export renders exactly two deep-dive rows (Pulse + Trends — D-06).
    - Entrance uses `whileInView` + `viewport={{ once: true }}` + easing `[0.22,1,0.36,1]` (no perpetual motion).
    - Contains an inline `<svg>` line/correlation visual; does NOT import `AsymptoticAnimation` or `CyclesAnimation` (Remotion).
    - No `animate-pulse`/`animate-bounce`/`animate-ping`/`repeat: Infinity`/`useAnimationFrame`; no `transition: all`/`transition-all`; no raw 6-digit hex.
    - Pulse copy = words-not-numbers check-in; Trends copy = correlation/pattern analytics — both traceable to features.md.
  </acceptance_criteria>
  <done>Two on-brand deep-dive rows (Pulse, Trends) render with asymmetric layout, fresh line-style SVG visual, and one-shot brand entrance motion — RSC-safe client island, token-only, no perpetual motion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | Self-contained presentational client component. No props from untrusted input, no network calls, no external resource loads, no `dangerouslySetInnerHTML`. SVG content is static/author-controlled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Deep-dive feature copy (truthfulness) | mitigate | Copy traced to features.md; AI/Insights framed as Pro-tier opt-in (no always-on/free overstatement). Build-time guard blocks Remotion-import and perpetual-motion anti-patterns. |
| T-02-05 | Denial of Service | Animation main-thread cost | mitigate | Entrance is one-shot `whileInView` opacity/transform (GPU-composited) with `once: true`; `useAnimationFrame`, `repeat: Infinity`, and `animate-*` perpetual classes are grep-blocked, preventing main-thread repaint loops. |

No external links, package installs, or untrusted input — no high-severity threats at this surface.
</threat_model>

<verification>
- `npx tsc --noEmit` reports no errors from `DeepDiveRow.tsx`.
- `npm run lint` exits 0.
- The automated `node` guard passes (use-client boundary, no Remotion import, no perpetual motion, canonical easing present, SVG present, token-only).
</verification>

<success_criteria>
- Two deep-dive rows (Pulse + Trends) render with the asymmetric sticky-intro + line-visual layout.
- Trends visual is a fresh inline SVG line/correlation chart (Remotion components not imported).
- Entrance motion is GPU-composited, custom-eased, staggered, whileInView-once — no perpetual decorative motion (FEAT-06).
- Copy is grounded in features.md; AI/Insights framed as Pro-tier opt-in.
</success_criteria>

<output>
Create `.planning/phases/02-features-page/02-03-SUMMARY.md` when done.
</output>
