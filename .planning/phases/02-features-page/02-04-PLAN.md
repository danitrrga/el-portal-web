---
phase: 02-features-page
plan: 04
type: execute
wave: 2
depends_on: ["02-02", "02-03"]
files_modified:
  - src/app/features/page.tsx
autonomous: true
requirements: [FEAT-03, FEAT-05, QUAL-01]
must_haves:
  truths:
    - "Visiting /features renders the hybrid layout in order: hero intro → highlight grid → deep-dive rows → closing CTA (FEAT-03)"
    - "The page is the architect/technical-drawing frame — two vertical margin rules + horizontal hairlines between sections (D-01/D-02)"
    - "The route exports correct metadata (title + description) (FEAT-05)"
    - "All site routes resolve and /features builds with no errors (QUAL-01)"
  artifacts:
    - path: "src/app/features/page.tsx"
      provides: "RSC /features route composing all sections, metadata, vertical-rule frame"
      exports: ["metadata", "default"]
      contains: "export const metadata"
  key_links:
    - from: "src/app/features/page.tsx"
      to: "FeaturesHeroSection / HighlightGrid / DeepDiveRow / FeaturesCTASection"
      via: "section imports composed in hybrid order"
      pattern: "FeaturesHeroSection"
    - from: "src/app/features/page.tsx"
      to: "vertical margin rules"
      via: "two aria-hidden absolute w-px decorative divs (D-01)"
      pattern: "w-px"
---

<objective>
Assemble the `/features` route: an RSC page that exports `metadata` (FEAT-05), composes the four sections in the locked hybrid order — hero → highlight grid → deep-dive rows → closing CTA (FEAT-03) — inside the methodology page-shell, and adds the defining architect/technical-drawing frame (D-01/D-02): two vertical hairline rules at the page margins plus horizontal hairline dividers between sections. Then verify the full build and route resolution (QUAL-01).

Purpose: This is the integration step that turns the Wave-1 section components into a reachable, on-brand page and proves the whole milestone surface still builds and every route resolves.
Output: `src/app/features/page.tsx` + a clean `npm run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-features-page/02-CONTEXT.md
@.planning/phases/02-features-page/02-PATTERNS.md
@CLAUDE.md
@src/app/globals.css
@src/app/methodology/page.tsx
@src/app/privacy/page.tsx
@.planning/phases/02-features-page/02-02-SUMMARY.md
@.planning/phases/02-features-page/02-03-SUMMARY.md

<interfaces>
<!-- Section components created in Wave 1 (plans 02 + 03). Default exports, no props. -->
src/components/features/FeaturesHeroSection.tsx  → export default FeaturesHeroSection()   (RSC)
src/components/features/HighlightGrid.tsx        → export default HighlightGrid()          (RSC)
src/components/features/DeepDiveRow.tsx          → export default <component> (renders Pulse + Trends rows) (client island)
src/components/features/FeaturesCTASection.tsx   → export default FeaturesCTASection()     (RSC)

Page shell (methodology/page.tsx lines 859-961) + metadata (privacy/page.tsx lines 6-9) — see PATTERNS.md.
Confirm the exact default-export names/paths from the Wave-1 SUMMARY files before importing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose the /features route with metadata + architect frame</name>
  <read_first>
    - src/app/methodology/page.tsx (page shell lines 859-961: wrapper, atmospheric radial, `<Navbar/>`, `<main className="... max-w-5xl ...">`, `<Footer/>`; section spacing `space-y-*`; hairline `h-px` divider lines 905-908)
    - src/app/privacy/page.tsx (lines 1-9 — the only safe `export const metadata` analog)
    - .planning/phases/02-features-page/02-PATTERNS.md (§ "features/page.tsx" — page shell, metadata, section divider, vertical-rules motif, token convention)
    - .planning/phases/02-features-page/02-CONTEXT.md (D-01 vertical margin rules + horizontal section hairlines, D-02 restraint on boxes, D-04 tokens, FEAT-03 hybrid order, FEAT-05 metadata)
    - .planning/phases/02-features-page/02-02-SUMMARY.md and 02-03-SUMMARY.md (confirm the exact default-export names of the four section components)
    - src/app/globals.css (`--color-ep-*` tokens)
  </read_first>
  <files>src/app/features/page.tsx</files>
  <action>
    Create `src/app/features/page.tsx` as an RSC (no `"use client"`). Import `type { Metadata } from "next"`, `Navbar` from `@/components/Navbar`, `Footer` from `@/components/Footer`, and the four section components from `@/components/features/*` (confirm exact export names via the Wave-1 SUMMARY files). Export `const metadata: Metadata` with `title: "Features — El Portal"` and a truthful description (e.g. "Everything El Portal tracks, analyzes, and surfaces — shipped and in your hands today."). Default-export `FeaturesPage` using the methodology page shell: a relative `min-h-screen w-full` wrapper with `background: "var(--color-ep-section-bg)"`, an `aria-hidden` atmospheric top radial (`var(--color-ep-accent-alpha-12)`), `<Navbar />`, a `<main className="relative z-10 mx-auto w-full max-w-5xl px-6 pt-28 pb-24 md:pt-36 md:px-8">`, and `<Footer />`. 

    Inside the wrapper (NOT inside `<main>`), add the D-01 architect frame: two `aria-hidden pointer-events-none absolute inset-y-0 w-px` decorative `<div>`s positioned at the content margins (e.g. `left-[calc(50%-320px)]` and `right-[calc(50%-320px)]`, gated `hidden xl:block` so they hug the `max-w-5xl` column on wide viewports), background `var(--color-ep-hairline)`. 

    Inside `<main>`, compose the sections in the LOCKED hybrid order (FEAT-03): `<FeaturesHeroSection />` → horizontal hairline divider → `<HighlightGrid />` → horizontal hairline divider → `<DeepDiveRow />` (renders both Pulse + Trends rows) → horizontal hairline divider → `<FeaturesCTASection />`. Each horizontal divider is the methodology pattern: `<div aria-hidden className="h-px" style={{ background: "var(--color-ep-rule)" }} />`, with section spacing via a `space-y-20 md:space-y-28` wrapper or explicit margins. Token-only inline styling, no SCREAMING_SNAKE hex constants (D-04), no raw hex, no `transition: all`, no raw `<button>`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/features/page.tsx','utf8'); if(/use client/.test(s)){console.error('page must be RSC');process.exit(1)} if(!/export const metadata/.test(s)){console.error('missing metadata export');process.exit(1)} if(!/Features — El Portal/.test(s)){console.error('missing/incorrect title');process.exit(1)} for(const c of ['FeaturesHeroSection','HighlightGrid','DeepDiveRow','FeaturesCTASection']){ if(!s.includes(c)){console.error('missing section: '+c);process.exit(1)} } const hero=s.indexOf('<FeaturesHeroSection'), grid=s.indexOf('<HighlightGrid'), deep=s.indexOf('<DeepDiveRow'), cta=s.indexOf('<FeaturesCTASection'); if(!(hero<grid&&grid<deep&&deep<cta)){console.error('hybrid section order wrong');process.exit(1)} if((s.match(/w-px/g)||[]).length<2){console.error('missing two vertical margin rules (D-01)');process.exit(1)} if(/#[0-9a-fA-F]{6}/.test(s)){console.error('raw hex found');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/features/page.tsx` exports `metadata` (title "Features — El Portal" + description) and a default RSC component.
    - JSX composes the sections in order: FeaturesHeroSection → HighlightGrid → DeepDiveRow → FeaturesCTASection (FEAT-03).
    - Contains two `aria-hidden` `w-px` vertical margin rules (D-01) and `h-px` horizontal hairline dividers between sections.
    - Uses the `max-w-5xl px-6 md:px-8` main wrapper; token-only inline styling, no raw 6-digit hex, no `"use client"`.
  </acceptance_criteria>
  <done>/features composes all four sections in hybrid order inside the architect frame, with correct metadata, RSC and token-only.</done>
</task>

<task type="auto">
  <name>Task 2: Build + full route resolution verification</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 success criterion 6 + Phase 4 route list — the canonical route set)
    - .planning/REQUIREMENTS.md (QUAL-01 — the full route list that must resolve)
    - src/app/features/page.tsx (the file just created)
  </read_first>
  <files>src/app/features/page.tsx</files>
  <action>
    Run `npm run build` (Next.js production build) and confirm it completes with exit code 0 and that `/features` appears in the emitted route/page manifest output. Confirm via the build's route listing that every required route is present: `/`, `/features`, `/manifesto`, `/methodology`, `/changelog`, `/pricing`, `/mcp`, `/terms`, `/privacy` (QUAL-01). Also run `npm run lint` and confirm exit 0 (no new ESLint errors). If the build surfaces an error originating in the new files (e.g. a client/RSC boundary violation because `DeepDiveRow` is imported into the RSC page — which is allowed, since DeepDiveRow carries its own `"use client"`), fix only the offending new file; do NOT modify unrelated pages. This task adds no new source beyond fixes required to make the build green.
  </action>
  <verify>
    <automated>npm run build 2>&1 | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{ const need=['/features','/manifesto','/methodology','/changelog','/pricing','/mcp','/terms','/privacy']; const miss=need.filter(r=>!d.includes(r)); if(!/Compiled successfully|Generating static pages|Route \(app\)/.test(d)){console.error('build did not report success');process.exit(1)} if(!d.includes('/features')){console.error('/features not in build output');process.exit(1)} if(miss.length){console.error('routes missing from build: '+miss.join(', '));process.exit(1)} console.log('BUILD OK — all routes present'); })"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and lists `/features` plus all of `/`, `/manifesto`, `/methodology`, `/changelog`, `/pricing`, `/mcp`, `/terms`, `/privacy` in its route output (QUAL-01).
    - `npm run lint` exits 0 with no new errors.
    - No source files outside `src/app/features/` and `src/components/features/` were modified to achieve the green build.
  </acceptance_criteria>
  <done>Production build is green, /features and all required routes resolve, lint is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → external app host | Inherited via `FeaturesCTASection`'s `APP_URL` link (composed here). No new boundary introduced by assembly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | page metadata description | accept | Static, author-controlled, truthful description string; no PII, no untrusted interpolation. |
| T-02-07 | Tampering | route/build integrity | mitigate | `npm run build` gate confirms the route compiles and every required route resolves; lint gate blocks introduced anti-patterns. |

No package installs, no untrusted input, no `target="_blank"`, no `dangerouslySetInnerHTML` introduced — no high-severity threats at this static-marketing surface.
</threat_model>

<verification>
- `npm run build` exits 0; build output includes `/features` and all eight other required routes.
- `npm run lint` exits 0.
- `node` guard on `page.tsx` passes (metadata present, hybrid section order correct, two vertical rules, RSC, token-only).
</verification>

<success_criteria>
- /features renders the hybrid layout (hero → grid → deep-dives → CTA) inside the architect frame (FEAT-03, D-01/D-02).
- Correct metadata exported (FEAT-05).
- Full production build green; all required routes resolve (QUAL-01).
</success_criteria>

<output>
Create `.planning/phases/02-features-page/02-04-SUMMARY.md` when done.
</output>
