---
phase: 02-features-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/FeaturesHeroSection.tsx
  - src/components/features/HighlightGrid.tsx
  - src/components/features/FeaturesCTASection.tsx
autonomous: true
requirements: [FEAT-02, FEAT-03, FEAT-04, FEAT-05]
must_haves:
  truths:
    - "The hero presents a feature-overview intro in the brand voice (straightforward 'here's what El Portal does' framing)"
    - "A line-ruled open grid (no cards, hairline dividers only) presents the supporting feature highlights with accurate vocabulary"
    - "Version → Cycle → Day appears only briefly in the grid (not re-explained at length)"
    - "The closing CTA offers a primary 'Open El Portal' (APP_URL) action and a secondary link to /pricing"
    - "All feature copy traces to the app's features.md — no PWA claim, no shipped Calendar/Todoist sync, biometrics + focus timer not claimed as shipped"
  artifacts:
    - path: "src/components/features/FeaturesHeroSection.tsx"
      provides: "RSC hero intro with .display H1 + subhead"
      contains: "className=\"display"
    - path: "src/components/features/HighlightGrid.tsx"
      provides: "RSC line-ruled open grid of feature highlights + SectionEyebrow"
      min_lines: 60
    - path: "src/components/features/FeaturesCTASection.tsx"
      provides: "RSC closing CTA, primary APP_URL + secondary /pricing"
      contains: "/pricing"
  key_links:
    - from: "src/components/features/FeaturesCTASection.tsx"
      to: "APP_URL (app signup) and /pricing"
      via: "brand + brand-link Button asChild Link"
      pattern: 'variant="brand"'
    - from: "src/components/features/HighlightGrid.tsx"
      to: "@phosphor-icons/react"
      via: "Phosphor Light icons weight=\"light\""
      pattern: '@phosphor-icons/react'
---

<objective>
Build the three RSC content sections of the Features page: the hero intro (D-09), the line-ruled open highlight grid (D-05/D-07, the "bento" reconciled as hairline-divided cells with no card backgrounds), and the closing CTA (D-10). All copy is grounded in the app's `features.md`; all styling uses `--color-ep-*` tokens directly inline (no SCREAMING_SNAKE hardcoded-hex constants) and Phosphor Light icons.

Purpose: These are the truthful, on-brand content blocks the page assembles. They are pure presentational RSC components with no client interactivity, so they ship in Wave 1 and are composed by the page in Wave 2.
Output: `FeaturesHeroSection.tsx`, `HighlightGrid.tsx`, `FeaturesCTASection.tsx` under `src/components/features/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-features-page/02-CONTEXT.md
@.planning/phases/02-features-page/02-PATTERNS.md
@CLAUDE.md
@src/app/globals.css
@src/components/CTASection.tsx
@src/app/methodology/page.tsx

<interfaces>
<!-- Contracts the executor needs — extracted from codebase. Use directly, no exploration needed. -->

Button variants (src/components/ui/button.tsx) — ONLY these two are permitted for CTAs:
  variant="brand"        // primary pill CTA
  variant="brand-link"   // secondary text link CTA
Usage: <Button asChild variant="brand" size="lg"><Link href={...}>…</Link></Button>

APP_URL constant (CTASection.tsx line 9, Navbar.tsx line 10):
  const APP_URL = process.env.NEXT_PUBLIC_APP_URL ?? "https://app.el-portal.app";

Tokens (src/app/globals.css) used by this plan:
  --color-ep-section-bg, --color-ep-fg-strong, --color-ep-fg, --color-ep-fg-muted,
  --color-ep-fg-muted-2, --color-ep-fg-subtle-2, --color-ep-hairline, --color-ep-rule,
  --color-ep-accent, --color-ep-accent-alpha-12

Phosphor Light icon API (@phosphor-icons/react): <Icon size={18} weight="light" /> (uses `weight`, NOT strokeWidth)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FeaturesHeroSection — feature-overview hero intro (RSC)</name>
  <read_first>
    - src/app/methodology/page.tsx (hero `<header>` block, lines 877-896 — the H1/subhead analog)
    - .planning/phases/02-features-page/02-PATTERNS.md (§ "FeaturesHeroSection.tsx" — core pattern + display-font rule)
    - .planning/phases/02-features-page/02-CONTEXT.md (D-09 hero framing, D-03 mono-accent, D-04 tokens)
    - C:\Users\20252128\dev\Projects\el-portal\docs\features.md ("The Temporal System" + room overview — to keep the intro truthful and high-level)
    - src/app/globals.css (`.display` utility + `--color-ep-*` tokens)
  </read_first>
  <files>src/components/features/FeaturesHeroSection.tsx</files>
  <action>
    Create an RSC (no `"use client"`) default-export `FeaturesHeroSection` returning a `<header>`. Follow the methodology hero analog: an H1 with the `.display` utility class and `style={{ fontSize: "clamp(42px, 4.2vw, 58px)", color: "var(--color-ep-fg-strong)" }}`, followed by a `max-w-2xl` subhead `<p>` colored `var(--color-ep-fg)`. The headline copy is D-09: a straightforward feature-overview framing ("here's what El Portal does"), NOT the "the system reads, you decide" angle. Above the H1, add a single mono eyebrow line (D-03) using `font-mono text-[11px] uppercase tracking-[0.22em]` colored `var(--color-ep-fg-muted)` (e.g. a short "FEATURES" / coordinate-style annotation) to seed the technical-drawing voice. Subhead summarizes the temporal system + rooms at a high level, grounded in features.md, without overstating. Use `--color-ep-*` tokens inline only — do NOT introduce SCREAMING_SNAKE hardcoded-hex constants (D-04). No gradient-clipped text, no Instrument Serif, no raw hex. Keep it a single section component; the page wrapper (added in plan 04) owns the background and vertical-rule motif.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "FeaturesHeroSection" || true); node -e "const s=require('fs').readFileSync('src/components/features/FeaturesHeroSection.tsx','utf8'); if(/use client/.test(s)){console.error('must be RSC');process.exit(1)} if(!/className=\"display/.test(s)){console.error('missing .display H1');process.exit(1)} if(/#[0-9a-fA-F]{6}/.test(s)){console.error('raw hex found — use tokens');process.exit(1)} if(!/var\(--color-ep-/.test(s)){console.error('missing ep tokens');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/features/FeaturesHeroSection.tsx` default-exports an RSC component (no `"use client"` directive).
    - Contains an H1 with the `.display` class and a subhead `<p>`.
    - Uses `var(--color-ep-*)` tokens; contains no raw 6-digit hex literals and no SCREAMING_SNAKE hardcoded-hex constants.
    - Headline framing is feature-overview (D-09), not the "system reads, you decide" angle.
    - Typecheck reports no errors originating from this file.
  </acceptance_criteria>
  <done>Hero section renders an on-brand .display headline + grounded subhead, RSC, token-only.</done>
</task>

<task type="auto">
  <name>Task 2: HighlightGrid — line-ruled open grid of feature highlights (RSC)</name>
  <read_first>
    - src/app/methodology/page.tsx (`SectionEyebrow` lines 20-49; `TemporalHierarchySection` lines 144-172; `ScaleTextContent` cell anatomy lines 102-142)
    - .planning/phases/02-features-page/02-PATTERNS.md (§ "HighlightGrid.tsx" — SectionEyebrow, open-grid cell anatomy, grid layout, icon convention)
    - .planning/phases/02-features-page/02-CONTEXT.md (D-05 open grid no cards, D-07 highlight selection + V→C→D brief, D-03 mono accent, D-04 tokens + Phosphor)
    - C:\Users\20252128\dev\Projects\el-portal\docs\features.md (AUTHORITATIVE — room descriptions: Lab, Goals, Archives, Cinema; Daily Flow; Pulse; Storage/consent; Onboarding; the "Planned" section for the truthfulness boundary)
    - .planning/research/SUMMARY.md (truthfulness guardrails: no PWA, no shipped Calendar/Todoist, planned vs shipped, i18n 5 languages, configurable tempo)
    - src/app/globals.css (`--color-ep-*` tokens)
  </read_first>
  <files>src/components/features/HighlightGrid.tsx</files>
  <action>
    Create an RSC (no `"use client"`) default-export `HighlightGrid`. Copy the `SectionEyebrow` helper pattern from methodology (mono number · label + flex hairline) but using `--color-ep-*` tokens inline (NOT methodology's hardcoded-hex constants). Render a `SectionEyebrow` (e.g. num "02" / label "HIGHLIGHTS") then a responsive CSS grid — `grid gap-8 md:grid-cols-3 md:gap-12` (you may use `md:col-span-2` on a featured cell for bento-style variation per PATTERNS.md). Each cell is the open-grid anatomy from D-05: a `border-t` hairline (`borderColor: "var(--color-ep-hairline)"`) with `pt-6`, NO card background, NO full border box, NO `bg-[...]` surface. Each cell has: a Phosphor Light icon (`import` from `@phosphor-icons/react`, rendered `weight="light"` — NOT lucide-react), a mono uppercase label, and a short body `<p>`. Define the cell content as a local array grounded in features.md — select and order from: Version→Cycle→Day (KEPT BRIEF per D-07 — one terse cell, do NOT re-explain the model at length; it is already deep-covered on home + methodology), Lab, Goals, Archives, Cinema, the daily flow (morning boot → evening shutdown), configurable tempo (cycle length + cycles-per-version), privacy / 3-bucket consent, multilingual (5 languages). Final selection/ordering is your discretion grounded in features.md. TRUTHFULNESS IS A HARD GATE: every cell's claim must trace to features.md; do NOT claim an installable PWA, do NOT claim shipped Calendar or Todoist sync, and do NOT present biometric integrations or the focus-workstation timer as shipped (omit them, or label clearly as "planned/coming" using features.md's "Planned" section). Pulse and Trends are NOT given full cells here — they are the deep-dive rows owned by plan 03; you may reference them lightly but the depth lives there. Token-only styling, no raw hex, no gradient-clipped text, no `transition: all`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/features/HighlightGrid.tsx','utf8'); if(/use client/.test(s)){console.error('must be RSC');process.exit(1)} if(/lucide-react/.test(s)){console.error('lucide-react banned — use Phosphor');process.exit(1)} if(!/@phosphor-icons\/react/.test(s)){console.error('missing Phosphor import');process.exit(1)} if(/transition:\s*all|transition-all/.test(s)){console.error('transition:all banned');process.exit(1)} if(/#[0-9a-fA-F]{6}/.test(s)){console.error('raw hex found');process.exit(1)} const banned=/installable PWA|Todoist|Calendar sync/i; if(banned.test(s)){console.error('overstated/unshipped claim found: '+s.match(banned)[0]);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/features/HighlightGrid.tsx` default-exports an RSC component (no `"use client"`).
    - Imports icons from `@phosphor-icons/react` rendered with `weight="light"`; contains NO `lucide-react` import.
    - Grid cells use hairline `border-t` dividers (`var(--color-ep-hairline)`) with no card background / full bordered box (D-05).
    - Contains a brief Version→Cycle→Day cell, not an extended re-explanation (D-07).
    - Contains no `installable PWA`, no shipped `Todoist`/`Calendar sync` claim; biometrics/focus timer not presented as shipped.
    - No raw 6-digit hex, no `transition: all`/`transition-all`; uses `var(--color-ep-*)` tokens.
  </acceptance_criteria>
  <done>An open, hairline-divided highlight grid renders truthful features.md-grounded cells with Phosphor Light icons and mono labels, no cards, no overstated claims.</done>
</task>

<task type="auto">
  <name>Task 3: FeaturesCTASection — closing CTA (RSC), primary signup + secondary pricing</name>
  <read_first>
    - src/components/CTASection.tsx (lines 1-72 — exact analog; note it uses SCREAMING_SNAKE token-valued constants which you will inline instead)
    - .planning/phases/02-features-page/02-PATTERNS.md (§ "FeaturesCTASection.tsx" — full pattern + Button variants)
    - .planning/phases/02-features-page/02-CONTEXT.md (D-10 primary "Open El Portal" → APP_URL, secondary → /pricing)
    - src/components/ui/button.tsx (brand / brand-link variants — lines 22-25)
    - src/app/globals.css (`--color-ep-*` tokens)
  </read_first>
  <files>src/components/features/FeaturesCTASection.tsx</files>
  <action>
    Create an RSC default-export `FeaturesCTASection` adapting `CTASection.tsx`. Keep the structure: relative `section` with `var(--color-ep-section-bg)` background, top hairline gradient rule, atmospheric `radial-gradient(... var(--color-ep-accent-alpha-12) ...)` backdrop, centered `max-w-3xl` content, `.display` H2 (`clamp(36px, 4.6vw, 58px)`, `var(--color-ep-fg-strong)`), and a subhead `<p>` (`var(--color-ep-fg)`). Define `const APP_URL = process.env.NEXT_PUBLIC_APP_URL ?? "https://app.el-portal.app";`. Per D-10 render TWO actions in the button row: a primary `<Button asChild variant="brand" size="lg">` wrapping `<Link href={APP_URL}>` labeled "Open El Portal", and a secondary `<Button asChild variant="brand-link" size="lg">` wrapping `<Link href="/pricing">` labeled e.g. "See pricing". Reference token values inline (NOT the SCREAMING_SNAKE constant block from the original CTASection — D-04). Heading/subhead copy in brand voice, grounded (no overstated claims). No raw `<button>`, only `brand`/`brand-link` variants; no raw hex; no gradient-clipped text.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/features/FeaturesCTASection.tsx','utf8'); if(/use client/.test(s)){console.error('should be RSC');process.exit(1)} if(!/variant=\"brand\"/.test(s)||!/variant=\"brand-link\"/.test(s)){console.error('need brand + brand-link');process.exit(1)} if(!/href=\"\/pricing\"/.test(s)){console.error('missing /pricing secondary link');process.exit(1)} if(!/APP_URL/.test(s)){console.error('missing APP_URL primary');process.exit(1)} if(/<button[\s>]/.test(s)){console.error('raw button banned');process.exit(1)} if(/#[0-9a-fA-F]{6}/.test(s)){console.error('raw hex found');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/features/FeaturesCTASection.tsx` default-exports an RSC component.
    - Primary action is a `variant="brand"` Button linking to `APP_URL`; secondary is a `variant="brand-link"` Button linking to `/pricing` (D-10).
    - No raw `<button>` element; no raw 6-digit hex; uses `var(--color-ep-*)` tokens inline.
  </acceptance_criteria>
  <done>Closing CTA drives to app signup (primary) and /pricing (secondary), on-brand and token-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → external app host | The CTA `<Link href={APP_URL}>` navigates to the external app domain. No data is passed; it is a plain navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | FeaturesCTASection external `APP_URL` link | accept | Uses Next.js `<Link>` (client-side nav / standard anchor) without `target="_blank"`, so no reverse-tabnabbing (`window.opener`) risk and no `rel` needed. Mirrors the existing, reviewed `CTASection.tsx` pattern. |
| T-02-03 | Tampering | Feature copy accuracy (truthfulness) | mitigate | All copy traced to `features.md`; build-time grep gate in Task 2 verify blocks `installable PWA` / `Todoist` / `Calendar sync` strings. Not a security threat per se, but the truthfulness guardrail is enforced as a hard gate. |

No package installs, no untrusted input, no `dangerouslySetInnerHTML`, no `target="_blank"` introduced — no high-severity threats apply at this static-marketing surface.
</threat_model>

<verification>
- `npx tsc --noEmit` reports no errors from the three new files.
- `npm run lint` exits 0 (no new ESLint errors in `src/components/features/`).
- Each file's automated `node` guard passes (RSC boundary, token-only, Phosphor not lucide, no overstated claims, correct CTA links).
</verification>

<success_criteria>
- Three RSC section components exist under `src/components/features/`, token-only, Phosphor-Light icons, no banned anti-patterns.
- All highlight/feature copy traces to features.md; no PWA / shipped Calendar-Todoist / shipped-biometrics-or-focus-timer claims.
- CTA offers primary APP_URL signup + secondary /pricing link (D-10).
- V→C→D is brief in the grid (D-07).
</success_criteria>

<output>
Create `.planning/phases/02-features-page/02-02-SUMMARY.md` when done.
</output>
