---
quick_id: 260613-716
slug: footer-wordmark
date: 2026-06-13
status: complete
commit: 6ccaba4
---

# Quick Task 260613-716: Footer wordmark rebrand — Summary

## Outcome

Footer rebranded with an editorial, grain-fill `EL PORTAL` wordmark. Delivered
through a live, iterative design loop with the user (Playwright-verified at each
step).

## Changes

- `src/app/globals.css` — added `.wordmark` system: `@property --wordmark-reveal`,
  ghost-outline layer, volumetric grain-fill layer (radial blooms + directional
  ramp + soft-light fractal grain, two feTurbulence textures), left→right masked
  reveal, reduced-motion fallback.
- `src/components/Footer.tsx` — replaced serif watermark with the two-layer
  wordmark in the copyright row (right-aligned, spans Product→Legal); swapped the
  brand-column serif "Portal" for the canonical `ElPortalWordmark`; copyright now
  "El Portal".

## Verification

- ESLint clean on `Footer.tsx`.
- Playwright: rest = chunky ghost outline; hover drives `--wordmark-reveal` 0→117%
  and fills L→R with the volumetric grain surface; reverses on mouse-leave.
- Dev server compiled with 0 errors.

## Notes

- **Pushed back** on the user's initial idea to bake the hover effect as a
  Higgsfield-generated video — native SVG/CSS is crisper, responsive, reversible,
  lighter, and interactive. User agreed.
- **Deviation flagged & approved:** `background-clip: text` (CLAUDE.md anti-pattern
  for gradient headings) used deliberately for this decorative watermark.
- Final feel: ~122px, depth via gradient contrast (not brightness), quiet grain.

## Commit

- `6ccaba4` — feat(footer): grain-fill EL PORTAL wordmark + canonical brand lockup
